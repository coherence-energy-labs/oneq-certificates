{
  "targets": [
    "src\\oneq\\certified_weight.py",
    "src\\oneq\\soft_estimator.py",
    "src\\oneq\\dem_calibration.py",
    "src\\oneq\\fragility_map.py",
    "src\\oneq\\device_laws.py",
    "src\\oneq\\dual_envelope.py",
    "src\\oneq\\face_annihilator.py",
    "src\\oneq\\dem_families.py",
    "src\\oneq\\dem_measured.py",
    "src\\oneq\\noise_correlation.py",
    "src\\oneq\\leakage_hmm.py",
    "src\\oneq\\exchangeability.py",
    "src\\oneq\\way.py",
    "src\\oneq\\arr.py",
    "src\\oneq\\pauli_defer.py",
    "src\\oneq\\code_trajectory.py",
    "src\\oneq\\transparency.py",
    "src\\oneq\\bakeoff.py",
    "src\\oneq\\distance_portfolio.py",
    "src\\oneq\\latent_parity.py",
    "src\\oneq\\branch_certificate.py",
    "src\\oneq\\matching_cert_strict.py",
    "src\\oneq\\quotient_reduction.py",
    "src\\oneq\\git_pin.py",
    "src\\oneq\\capability_plan.py",
    "src\\oneq\\dwell.py",
    "src\\oneq\\exchange_rate.py",
    "src\\oneq\\rl_wrap.py",
    "src\\oneq\\policy_gap.py",
    "src\\oneq\\checkability.py",
    "src\\oneq\\composition.py",
    "src\\oneq\\shift_detector.py",
    "src\\oneq\\voi.py",
    "src\\oneq\\freeze_detector.py",
    "src\\oneq\\identifiability.py",
    "src\\oneq\\open_world.py",
    "src\\oneq\\shield.py",
    "src\\oneq\\shield_check.py",
    "src\\oneq\\core.py",
    "src\\oneq\\device_dem.py",
    "src\\oneq\\forgeries.py",
    "src\\oneq\\certifying_decoder.py",
    "src\\oneq\\moat_growth.py",
    "src\\oneq\\exact_lp.py",
    "src\\oneq\\exact_decode.py",
    "src\\oneq\\coset_oracle.py",
    "src\\oneq\\robustness.py",
    "src\\oneq\\bayes_coset.py",
    "src\\oneq\\logical_cut.py",
    "src\\oneq\\fixed_point_bridge.py",
    "src\\oneq\\decomposition.py",
    "src\\oneq\\coset_cert.py",
    "src\\oneq\\rehearsal.py",
    "src\\oneq\\beacon.py",
    "src\\oneq\\coset_matching.py",
    "src\\oneq\\cert_format.py",
    "src\\oneq\\passport.py",
    "src\\oneq\\isd.py",
    "src\\oneq\\gbb.py",
    "src\\oneq\\bz.py",
    "src\\oneq\\mitm.py",
    "src\\oneq\\satdist.py",
    "src\\oneq\\certify.py",
    "src\\oneq\\coverage.py",
    "src\\oneq\\ledger.py",
    "src\\oneq\\transversal.py",
    "src\\oneq\\survival.py",
    "src\\oneq\\lattice.py",
    "src\\oneq\\guard.py",
    "src\\oneq\\modelgate.py",
    "src\\oneq\\evidence.py",
    "src\\oneq\\admissibility.py",
    "src\\oneq\\matching_cert.py",
    "src\\oneq\\moat_dual.py",
    "src\\oneq\\qldpc_cert.py",
    "src\\oneq\\qldpc_check.py",
    "src\\oneq\\tjoin_exact.py",
    "src\\oneq\\coset_lower_check.py",
    "src\\oneq\\coset_bnb_check.py",
    "src\\oneq\\logical_contrast.py",
    "src\\oneq\\robust_radius.py",
    "src\\oneq\\dem_cert.py",
    "src\\oneq\\capability.py",
    "src\\oneq\\capability_probe.py",
    "src\\oneq\\coset_enclosure.py",
    "src\\oneq\\coset_enclosure_check.py",
    "src\\oneq\\enclosure_forgery.py",
    "src\\oneq\\coset_e7.py",
    "src\\oneq\\certified_selection.py",
    "src\\oneq\\evidence_algebra.py",
    "src\\oneq\\dem_admission.py",
    "src\\oneq\\abft_l0.py",
    "src\\oneq\\evidence_frontier.py",
    "src\\oneq\\soundness_budget.py",
    "src\\oneq\\enclosure_budget.py",
    "src\\oneq\\error_budget.py",
    "src\\oneq\\dual_forgery.py",
    "tools\\mutation_outcome.py",
    "tools\\mutation_sandbox.py",
    "tools\\gate_all.py",
    "tools\\roadmap_gate.py",
    "tools\\doc_claims_gate.py",
    "tools\\verify_closures.py",
    "tools\\claims_provenance_gate.py",
    "tools\\error_budget_ledger.py",
    "tools\\certificate_robustness_corpus.py",
    "tools\\dem_cert_run.py",
    "tools\\push_guard.py",
    "experiments\\voi\\ga3_voi_gate.py",
    "experiments\\enclosure\\e7_cost_profile.py",
    "experiments\\ibm_live\\rep_code_cert.py",
    "experiments\\g4_hardware\\erasure_decoder.py",
    "experiments\\g4_hardware\\ve_reweighted.py",
    "experiments\\g4_hardware\\h1_reweighted.py",
    "experiments\\m0_prior_art\\qldpc_heavyhex_transpiler.py",
    "experiments\\m0_prior_art\\qldpc_symmetry_decoding.py",
    "experiments\\way\\ga11_hardware_iq.py",
    "experiments\\ibm_live\\hardware_lambda.py",
    "experiments\\g3_abft\\c2_l2prime_class_attestation.py"
  ],
  "mutated": "sandbox-copy",
  "total": 500,
  "killed": 500,
  "survived": 0,
  "skipped": 0,
  "timed_out": 0,
  "results": [
    {
      "id": "M506-e7-fold-cross-term",
      "why": "the mask-fold uses a0*a where it must use a0*b, so any two mechanisms sharing a mask combine to the wrong odds",
      "find": "            folded[mask] = (a0 * b + b0 * a, b0 * b + a0 * a)",
      "replace": "            folded[mask] = (a0 * a + b0 * b, b0 * b + a0 * a)",
      "must_fail": "test_the_FOLD_MATCHES_THE_UNFOLDED_TRANSFER_at_HIGH_MULTIPLICITY",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M507-e7-fold-drops-all-but-last",
      "why": "mechanisms sharing a mask overwrite instead of folding, so all but the last are silently dropped from the transfer",
      "find": "        if mask in folded:\n            a0, b0 = folded[mask]\n            folded[mask] = (a0 * b + b0 * a, b0 * b + a0 * a)\n        else:\n            folded[mask] = (a, b)",
      "replace": "        folded[mask] = (a, b)",
      "must_fail": "test_the_FOLD_MATCHES_THE_UNFOLDED_TRANSFER_at_HIGH_MULTIPLICITY",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M508-e7-width-cap-inert",
      "why": "component_mass stops refusing over-wide components, so a 2**n transfer is attempted instead of falling back to E6",
      "find": "    if n > MAX_COMPONENT:",
      "replace": "    if False:",
      "must_fail": "test_the_WIDTH_CAP_still_REFUSES_rather_than_approximating",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M509-e7-divisibility-unchecked",
      "why": "price() stops checking that den divides phat_t, so the integer division silently FLOORS -- an UNDER-price, the one direction that breaks soundness",
      "find": "        detail.append({\"detectors\": comp, \"mechanisms\": len(inner),\n                       \"odd_mass\": S, \"total\": A})\n    if phat_t % den:",
      "replace": "        detail.append({\"detectors\": comp, \"mechanisms\": len(inner),\n                       \"odd_mass\": S, \"total\": A})\n    if False:",
      "must_fail": "test_a_PHAT_NOT_DIVISIBLE_BY_DEN_IS_REFUSED_NOT_FLOORED",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M510-e7-partition-never-merges",
      "why": "components_via_index examines no candidate mechanisms, so nothing ever merges and E7 silently degrades to one detector per component",
      "find": "        for i in idx.get(c, ()):",
      "replace": "        for i in ():",
      "must_fail": "test_the_INDEXED_PARTITION_IS_IDENTICAL_to_the_scanning_one",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M511-e7-index-returns-everything",
      "why": "touching_via_index ignores the component and returns the whole remaining list, so the transfer runs over mechanisms it must not",
      "find": "    return [dets_and_odds[i] for i in sorted(ids)]",
      "replace": "    return list(dets_and_odds)",
      "must_fail": "test_the_INDEX_RETURNS_THE_SAME_LIST_ELEMENT_FOR_ELEMENT",
      "target": "coset_e7",
      "outcome": "killed"
    },
    {
      "id": "M512-e7-profile-note-retypes-its-percentage",
      "why": "the per-shot cache note hard-codes the component hit rate instead of interpolating it, so prose can outlive the measurement again",
      "find": "        f\"COMPONENT instead serves {comp_rate * 100:.1f}% within one shot, \"",
      "replace": "        \"COMPONENT instead serves ~37% within one shot, \"",
      "must_fail": "test_the_NOTE_BUILDER_QUOTES_ITS_ARGUMENTS_and_not_a_frozen_number",
      "target": "e7_cost_profile",
      "outcome": "killed"
    },
    {
      "id": "M513-qldpc-exact-facet-parity-unchecked",
      "why": "the exact facet scan accepts a right-parity subset as a facet",
      "find": "        if len(Fs) % 2 == par:\n            return None, 0, (f\"facet ({jname},{sorted(Fs)}) has the RIGHT \"",
      "replace": "        if False:\n            return None, 0, (f\"facet ({jname},{sorted(Fs)}) has the RIGHT \"",
      "must_fail": "test_wrong_parity_facet_refused",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M514-qldpc-exact-safe-dual-slack-deleted",
      "why": "the exact bound omits the derived slacks, so y.b is reported raw",
      "find": "        if slack > 0:\n            z_total += slack",
      "replace": "        if False:\n            z_total += slack",
      "must_fail": "test_inflated_dual_stays_sound",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M517-qldpc-exact-duplicate-facet-counted-twice",
      "why": "the exact scan no longer refuses a facet listed twice",
      "find": "        if key in seen:\n            return None, 0, f\"facet ({jname},{sorted(Fs)}) listed twice\"",
      "replace": "        if False:\n            return None, 0, f\"facet ({jname},{sorted(Fs)}) listed twice\"",
      "must_fail": "test_mutant_8_duplicate_facet_refused_exact",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M518-qldpc-exact-lattice-rule-loosened",
      "why": "the exact verdict accepts a bound two lattice levels below U",
      "find": "    if L > U - step or L == U:",
      "replace": "    if L > U - 2 * step or L == U:",
      "must_fail": "test_lattice_rule_certifies_across_float_dust",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M240-a-relabelling-can-buy-information",
      "why": "the generator-set honesty clause stops refusing post-processing relabellings",
      "find": "    if not c.changes_physical and (c.evi > 0.0 or c.d_p_vcc > 0.0):",
      "replace": "    if False:",
      "must_fail": "test_a_relabelling_claiming_benefit_is_REFUSED_not_scored_low",
      "target": "voi",
      "outcome": "killed"
    },
    {
      "id": "M241-a-refused-candidate-can-still-be-selected",
      "why": "select stops filtering on admission, so a relabelling wins a weak field",
      "find": "    ok = [s for s in scored if s.admitted]",
      "replace": "    ok = scored",
      "must_fail": "test_a_relabelling_cannot_win_when_the_honest_field_is_empty",
      "target": "voi",
      "outcome": "killed"
    },
    {
      "id": "M242-a-free-action-scores-infinity",
      "why": "a zero-cost candidate is priced instead of refused as unpriced",
      "find": "    if den <= 0.0:",
      "replace": "    if False:",
      "must_fail": "test_a_FREE_action_is_REFUSED_as_unpriced",
      "target": "voi",
      "outcome": "killed"
    },
    {
      "id": "M243-EVI-falls-back-to-nats-without-a-value-function",
      "why": "EVI is computed with no alpha-vectors, so it measures entropy not value",
      "find": "    if not alpha_vectors:",
      "replace": "    if False:",
      "must_fail": "test_EVI_without_alpha_vectors_is_REFUSED",
      "target": "voi",
      "outcome": "killed"
    },
    {
      "id": "M244-the-protected-table-is-empty",
      "why": "no ref is protected, so the guard allows every push and still reports success on all of them",
      "find": "    \"refs/heads/master\": (",
      "replace": "    \"refs/heads/DISABLED\": (",
      "must_fail": "test_source_history_aimed_at_the_release_ref_is_REFUSED",
      "target": "push_guard",
      "outcome": "killed"
    },
    {
      "id": "M245-a-clobbering-push-is-allowed",
      "why": "the ancestry test is inverted, so exactly the disjoint-history overwrite this exists to stop becomes the allowed case",
      "find": "        if not is_ancestor(remote_sha, local_sha):",
      "replace": "        if is_ancestor(remote_sha, local_sha):",
      "must_fail": "test_source_history_aimed_at_the_release_ref_is_REFUSED",
      "target": "push_guard",
      "outcome": "killed"
    },
    {
      "id": "M246-deleting-a-protected-ref-is-allowed",
      "why": "the deletion branch falls through to the ancestry test, which a zero sha passes vacuously",
      "find": "        if set(local_sha) == {\"0\"}:",
      "replace": "        if False:",
      "must_fail": "test_DELETING_the_release_ref_is_REFUSED",
      "target": "push_guard",
      "outcome": "killed"
    },
    {
      "id": "M247-only-the-first-ref-line-is-judged",
      "why": "a push carrying an innocent ref first is allowed wholesale -- the guard reports on a ref that was never the danger",
      "find": "    for raw in lines:",
      "replace": "    for raw in list(lines)[:1]:",
      "must_fail": "test_every_ref_line_gets_its_own_verdict",
      "target": "push_guard",
      "outcome": "killed"
    },
    {
      "id": "M286-the-break-point-loses-its-anchor",
      "why": "gamma_binding stops dividing by the measured C3, so the exponent at which latency binds is computed against nothing and the robustness margin becomes arbitrary",
      "find": "    return math.log(c_break / C3) / math.log(w / W3)",
      "replace": "    return math.log(c_break) / math.log(w / W3)",
      "must_fail": "test_the_break_point_is_far_below_any_plausible_exponent",
      "target": "dwell",
      "outcome": "killed"
    },
    {
      "id": "M287-the-separation-is-measured-against-the-wrong-quantity",
      "why": "separation is taken against rounds instead of seconds, so the syndrome cycle drops out and the orders-of-magnitude claim stops comparing two times",
      "find": "    return math.log10(dwell(gamma, w).seconds / reaction_s)",
      "replace": "    return math.log10(dwell(gamma, w).rounds / reaction_s)",
      "must_fail": "test_the_stated_separation_is_slightly_optimistic_at_the_worst_corner",
      "target": "dwell",
      "outcome": "killed"
    },
    {
      "id": "M288-an-ambiguous-printed-precision-is-guessed",
      "why": "a trailing-zero integer stops being refused, so the gate invents a tolerance for '300' and reports a comparison whose bar was assumed rather than read",
      "find": "    if float(published) == int(published) and int(published) % 10 == 0:",
      "replace": "    if False:",
      "must_fail": "test_a_trailing_zero_integer_is_REFUSED_not_guessed",
      "target": "dwell",
      "outcome": "killed"
    },
    {
      "id": "M289-the-power-law-stops-being-anchored",
      "why": "C(w) drops the measured anchor, so the curve no longer passes through C3 and the published table stops reproducing",
      "find": "    return anchor_c * (w / anchor_w) ** gamma",
      "replace": "    return (w / anchor_w) ** gamma",
      "must_fail": "test_the_power_law_is_ANCHORED_at_the_measured_C3",
      "target": "dwell",
      "outcome": "killed"
    },
    {
      "id": "M282-the-exchange-rate-loses-its-factor-of-4",
      "why": "the denominator drops to 2 d^2 ln2, doubling every recommended synthesis budget -- the corpus's own worked 40x becomes 80x",
      "find": "            / (4.0 * w.d * w.d * math.log(2.0)))",
      "replace": "            / (2.0 * w.d * w.d * math.log(2.0)))",
      "must_fail": "test_the_law_reproduces_the_corpus_worked_example",
      "target": "exchange_rate",
      "outcome": "killed"
    },
    {
      "id": "M283-the-ratio-stops-depending-on-lnLambda",
      "why": "the split becomes a constant instead of a TRAJECTORY quantity, which is the entire claim of Sec 3.2.7",
      "find": "    return ((w.rot_fraction * w.c_t * math.log(w.lam))",
      "replace": "    return ((w.rot_fraction * w.c_t * 1.0)",
      "must_fail": "test_the_ratio_scales_as_ln_lambda",
      "target": "exchange_rate",
      "outcome": "killed"
    },
    {
      "id": "M284-the-exact-law-collapses-to-the-asymptotic",
      "why": "the 8d+2 correction is dropped, so the exact form stops differing from the asymptotic and the numerical calibration can no longer distinguish them",
      "find": "    denom = 2.0 * math.log(2.0) * (6.0 * w.d * w.d + 8.0 * w.d + 2.0)",
      "replace": "    denom = 2.0 * math.log(2.0) * (6.0 * w.d * w.d)",
      "must_fail": "test_the_corpus_form_is_an_ASYMPTOTIC_and_is_high",
      "target": "exchange_rate",
      "outcome": "killed"
    },
    {
      "id": "M285-a-non-suppressing-code-is-priced-anyway",
      "why": "Lambda <= 1 stops being refused, so an exchange rate is quoted for a code that does not suppress and has no exchange to make",
      "find": "        if self.lam <= 1.0:",
      "replace": "        if False:",
      "must_fail": "test_a_non_suppressing_code_is_REFUSED",
      "target": "exchange_rate",
      "outcome": "killed"
    },
    {
      "id": "M278-two-inert-arms-are-scored-as-additive",
      "why": "the non-vacuity floor is removed, so gA=gB=0 satisfies 'additive to within 30%' EXACTLY and two controls that did nothing are reported as composing perfectly",
      "find": "    if gain_a < min_gain or gain_b < min_gain:",
      "replace": "    if False:",
      "must_fail": "test_two_inert_arms_are_REFUSED_not_scored_as_additive",
      "target": "rl_wrap",
      "outcome": "killed"
    },
    {
      "id": "M279-the-bar-is-reported-as-discriminating",
      "why": "the gate stops saying that its +/-30% band contains the multiplicative prediction, so a pass that separates neither composition law is quoted as if it established additivity",
      "find": "    discriminates = not (band_lo <= mul <= band_hi)",
      "replace": "    discriminates = True",
      "must_fail": "test_the_additive_band_does_NOT_discriminate_at_the_measured_gains",
      "target": "rl_wrap",
      "outcome": "killed"
    },
    {
      "id": "M280-the-verdict-is-the-first-band-tested-not-the-nearest",
      "why": "ADDITIVE is printed for a measurement closer to the multiplicative prediction and below BOTH -- the label then contradicts the numbers beside it",
      "find": "    verdict = (\"ADDITIVE\" if abs(r_add) <= abs(r_mul)",
      "replace": "    verdict = (\"ADDITIVE\" if abs(r_add) <= 1e9",
      "must_fail": "test_the_verdict_is_the_NEAREST_reference_not_the_first_one_tested",
      "target": "rl_wrap",
      "outcome": "killed"
    },
    {
      "id": "M281-the-multiplicative-reference-collapses-to-additive",
      "why": "the interaction term is dropped, so independent non-interfering composition is scored as a shortfall it does not have",
      "find": "    mul = gain_a + gain_b - gain_a * gain_b",
      "replace": "    mul = gain_a + gain_b",
      "must_fail": "test_independent_controls_land_on_the_MULTIPLICATIVE_prediction",
      "target": "rl_wrap",
      "outcome": "killed"
    },
    {
      "id": "M268-a-linear-policy-may-lose-to-static",
      "why": "the Delta > 1 guard is removed, so a class can be reported as worse than the constant policies it contains -- exactly the 1.0595 a live run produced",
      "find": "    if j_linear + 1e-9 < j_static:",
      "replace": "    if False:",
      "must_fail": "test_a_linear_policy_losing_to_STATIC_is_REFUSED",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M269-a-degenerate-denominator-still-yields-a-number",
      "why": "Delta is computed where adaptation is worth nothing, so noise over noise can grant the kernel actuation authority",
      "find": "    if headroom < min_headroom:",
      "replace": "    if False:",
      "must_fail": "test_a_degenerate_denominator_is_REFUSED",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M270-a-truncated-alpha-set-is-silently-accepted",
      "why": "capping instead of refusing makes J(alpha) a LOWER bound, which shrinks Delta and grants authority that was not earned",
      "find": "        if len(gamma) > max_alphas:",
      "replace": "        if False:",
      "must_fail": "test_a_truncated_alpha_set_is_REFUSED_not_capped",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M271-the-alpha-prune-drops-a-uniquely-optimal-vector",
      "why": "pointwise domination becomes strict inequality on one coordinate, so vectors optimal in some belief region are discarded and J(alpha) falls",
      "find": "        if any(all(a[i] <= b[i] + 1e-12 for i in range(len(a)))",
      "replace": "        if any(any(a[i] <= b[i] + 1e-12 for i in range(len(a)))",
      "must_fail": "test_alpha_vectors_agree_with_brute_force_belief_tree_search",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M273-TP2-reports-a-pass-by-ignoring-negative-minors",
      "why": "violations are never recorded, so the structural precondition of the threshold theorem is asserted rather than decided",
      "find": "            if minor < -tol:",
      "replace": "            if False:",
      "must_fail": "test_tp2_reports_WHERE_it_fails_not_merely_that_it_did",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M274-a-demotion-is-raised-instead-of-returned",
      "why": "Delta > 0.05 stops being a PASS-with-demotion and becomes an error, which is the asymmetry the corpus calls the honest part",
      "find": "    granted = d <= bar",
      "replace": "    granted = True",
      "must_fail": "test_a_large_gap_DEMOTES_and_that_is_a_PASS_not_an_error",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M275-every-verdict-is-reported-as-certified",
      "why": "a DEMOTION stops being labelled provisional, so a failed search is dressed as a measured gap -- Delta is an UPPER bound, so only a grant actually follows from it",
      "find": "        \"CERTIFIED\" if granted else \"PROVISIONAL\",",
      "replace": "        \"CERTIFIED\",",
      "must_fail": "test_a_grant_is_CERTIFIED_and_a_demotion_is_PROVISIONAL",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M276-the-class-is-sampled-instead-of-enumerated",
      "why": "pair enumeration returns only the constants, so the search falls back to a lattice covering 29% of the class -- which INFLATES Delta and demotes a kernel that may deserve authority",
      "find": "    for i in range(n):",
      "replace": "    for i in range(0):",
      "must_fail": "test_pair_enumeration_finds_more_behaviours_than_a_grid",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M277-the-winner-is-never-refined-on-the-scored-beliefs",
      "why": "the final refinement is skipped, so a member tuned on a SUBSET is reported against the full set without being a local optimum of the objective it is scored on",
      "find": "    step = 1.0 / max(2, grid)",
      "replace": "    step = 0.0",
      "must_fail": "test_the_union_result_is_a_LOCAL_OPTIMUM_on_the_scored_beliefs",
      "target": "policy_gap",
      "outcome": "killed"
    },
    {
      "id": "M260-an-uncheckable-class-still-gets-an-operating-point",
      "why": "C4-C6 stop being refused, so the gate reports an advantage for a workload with no check and no retry loop to move",
      "find": "    if workload_class in UNCHECKABLE:",
      "replace": "    if False:",
      "must_fail": "test_an_UNCHECKABLE_class_is_REFUSED",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M261-false-accept-exposure-ignores-the-shot-failure-rate",
      "why": "delivered error stops scaling with (1-q), so running at q* costs nothing in exposure and the whole trade-off vanishes",
      "find": "    r = (1.0 - q) * self.eps_fa / denom",
      "replace": "    r = self.eps_fa / denom",
      "must_fail": "test_running_at_q_star_multiplies_false_accept_exposure",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M262-k-repetitions-stop-suppressing-the-error",
      "why": "k agreeing acceptances are scored as one, so repetition costs shots and buys nothing",
      "find": "        return r ** k",
      "replace": "        return r",
      "must_fail": "test_k_agreeing_acceptances_suppress_the_delivered_error",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M263-a-false-accept-does-not-end-the-retry-loop",
      "why": "shots are charged on q instead of on acceptance, overstating the low-q arm's shot count -- an error in OUR OWN FAVOUR",
      "find": "    p_accept = q + (1.0 - q) * check.eps_fa",
      "replace": "    p_accept = q",
      "must_fail": "test_the_retry_loop_is_charged_on_ACCEPTANCE_not_on_success",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M264-an-unreachable-target-returns-the-least-bad-point",
      "why": "the refusal becomes a best-effort answer, so two arms delivering DIFFERENT correctness get compared as if they were equal",
      "find": "    if best is None:",
      "replace": "    if False:",
      "must_fail": "test_an_unreachable_target_is_REFUSED_not_approximated",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M265-suppression-accepts-an-undeclared-source",
      "why": "the number that decides the whole result no longer has to say where it came from",
      "find": "        if not self.source.strip():\n            raise CheckabilityRefusal(\n",
      "replace": "        if False:\n            raise CheckabilityRefusal(\n",
      "must_fail": "test_a_suppression_with_no_declared_source_is_REFUSED",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M296-a-check-accepts-an-undeclared-source",
      "why": "the CHECK's provenance guard is removed, so a false-alarm rate with no stated origin sets the operating point",
      "find": "        if not self.source.strip():\n            raise CheckabilityRefusal(\"a check with no declared source\")",
      "replace": "        if False:\n            raise CheckabilityRefusal(\"a check with no declared source\")",
      "must_fail": "test_a_check_with_no_declared_source_is_REFUSED",
      "target": "checkability",
      "outcome": "killed"
    },
    {
      "id": "M266-an-absent-suite-maps-back-to-killed",
      "why": "the NO_TEST_RAN arm is removed, so pytest collecting NOTHING is scored as a mutant killed by a test that does not exist",
      "find": "    if suite_green is NO_TEST_RAN:",
      "replace": "    if False:",
      "must_fail": "test_the_recorded_outcomes_are_exactly_what_the_tally_accepts",
      "target": "mutation_outcome",
      "outcome": "killed"
    },
    {
      "id": "M267-a-vanished-killer-leaves-the-denominator",
      "why": "no_test_ran drops out of the total, so a mutant whose killer could not be found RAISES the score -- the shrinking-denominator shape the skip rule already carries a scar for",
      "find": "    return killed + survived + skipped + timed_out + no_test_ran",
      "replace": "    return killed + survived + skipped + timed_out",
      "must_fail": "test_a_skipped_or_timed_out_mutant_stays_in_the_denominator",
      "target": "mutation_outcome",
      "outcome": "killed"
    },
    {
      "id": "M248-unknown-mass-is-renormalised-away",
      "why": "the open-world branch is given the library's average accuracy instead of zero, which is exactly what GA-2 forbids",
      "find": "    val = (e.p_complete_lb * (1.0 - e.unknown_mass) * worst",
      "replace": "    val = (e.p_complete_lb * 1.0 * worst",
      "must_fail": "test_unknown_mass_is_SUBTRACTED_not_renormalised_away",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M249-the-credible-set-is-averaged-not-worst-cased",
      "why": "a posterior-weighted mean over models the run cannot separate: a false SEPARABLE arriving as arithmetic",
      "find": "    worst = min(e.accuracy_lb[m] for m in e.credible)",
      "replace": "    worst = max(e.accuracy_lb[m] for m in e.credible)",
      "must_fail": "test_the_credible_set_is_taken_at_its_WORST_not_its_mean",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M250-detector-latency-is-free",
      "why": "the lag charge is dropped, so the bound trusts a posterior that is stale exactly when the adversary made it stale",
      "find": "           * (1.0 - e.lag_damage) * (1.0 - e.stress_damage))",
      "replace": "           * 1.0 * (1.0 - e.stress_damage))",
      "must_fail": "test_detector_latency_is_CHARGED",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M251-an-UNKNOWN-library-still-certifies",
      "why": "MODEL_STATUS = UNKNOWN no longer collapses the bound: the machine certifies accuracy for a device it has just said it cannot model",
      "find": "    if e.model_status_unknown:",
      "replace": "    if False:",
      "must_fail": "test_MODEL_STATUS_UNKNOWN_collapses_the_bound_to_zero",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M252-a-noisy-inversion-is-called-fatal",
      "why": "the significance test drops to the point estimate, so sampling noise is reported as the fatal class and the gate cries wolf",
      "find": "        return self.bound > hi",
      "replace": "        return self.bound > self.p_hat",
      "must_fail": "test_a_strict_violation_can_be_explained_by_noise",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M253-a-confound-is-pooled-instead-of-stratified",
      "why": "per-stratum correlations are replaced by one pooled figure, which measures the nuisance factor rather than the tracking",
      "find": "    rhos = [spearman([x for x, _ in s], [y for _, y in s])",
      "replace": "    rhos = [spearman([x for t in strata for x, _ in t],\n                     [y for t in strata for _, y in t])",
      "must_fail": "test_stratification_recovers_a_signal_a_CONFOUND_destroys",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M254-a-dead-stratum-hides-behind-the-median",
      "why": "the worst stratum is reported as the median, so one stratum that tracks nothing is invisible",
      "find": "    return rhos, median, min(rhos)",
      "replace": "    return rhos, median, median",
      "must_fail": "test_the_WORST_stratum_is_returned_so_a_median_cannot_hide_it",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M255-the-bootstrap-ignores-cluster-structure",
      "why": "resamples individual observations, treating 8 certificates from one scenario as 8 independent samples of a device",
      "find": "        sample = [clusters[rng.randrange(n)] for _ in range(n)]",
      "replace": "        sample = list(clusters)",
      "must_fail": "test_the_bootstrap_lower_bound_is_STRICTLY_below_the_point_estimate",
      "target": "composition",
      "outcome": "killed"
    },
    {
      "id": "M256-the-null-is-scored-against-one-model",
      "why": "the composite min becomes a max, so data any library member explains still reads as a shift -- the false alarms that inflated the calibrated threshold from 9.75 to 37.1",
      "find": "            s = min(per_model)",
      "replace": "            s = max(per_model)",
      "must_fail": "test_data_from_ANY_library_member_does_not_fire",
      "target": "shift_detector",
      "outcome": "killed"
    },
    {
      "id": "M257-an-uncalibrated-detector-speaks-anyway",
      "why": "a default threshold is invented, so a detector with no measured false-alarm rate is charged against by the certified bound",
      "find": "        if self.threshold is None:",
      "replace": "        if False:",
      "must_fail": "test_an_UNCALIBRATED_detector_refuses_to_speak",
      "target": "shift_detector",
      "outcome": "killed"
    },
    {
      "id": "M258-the-darkness-witness-records-the-post-floor-mass",
      "why": "records unknown mass AFTER the floor, where 'M_? lost' and 'M_? never competed' are numerically identical",
      "find": "        self.raw_unknown_max = max(self.raw_unknown_max,\n                                   post[UNKNOWN_MODEL])",
      "replace": "        self.raw_unknown_max = max(self.raw_unknown_max,\n                                   float(self.alpha_floor))",
      "must_fail": "test_a_posterior_whose_unknown_DID_compete_is_not_dark",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M259-the-darkness-flag-does-not-travel-with-the-verdict",
      "why": "the verdict is issued with the flag defaulted to False, so a caller reads KNOWN and is never handed the reason it may mean nothing -- the invariant reverts to advice",
      "find": "            leader_mass=self._p[lead], detector_dark=self.is_dark())",
      "replace": "            leader_mass=self._p[lead])",
      "must_fail": "test_the_darkness_flag_TRAVELS_with_the_verdict",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M237-a-frozen-policy-is-scored-as-a-win",
      "why": "the completion bar stops being checked, so protecting harder always wins",
      "find": "    if outcome.p_complete < p_min:",
      "replace": "    if False:",
      "must_fail": "test_RUL_grow_is_REJECTED_despite_winning_on_the_metric",
      "target": "freeze_detector",
      "outcome": "killed"
    },
    {
      "id": "M238-best-win-returns-the-best-scorer-regardless",
      "why": "the top-scoring candidate is returned even when nothing is a win",
      "find": "    return wins[0] if wins else None",
      "replace": "    return rank(outcomes, baseline, p_min)[0]",
      "must_fail": "test_best_win_returns_None_rather_than_the_best_scorer",
      "target": "freeze_detector",
      "outcome": "killed"
    },
    {
      "id": "M239-the-reported-win-audit-finds-nothing",
      "why": "a frozen policy presented as a win passes the audit silently",
      "find": "    return [v for v in verdicts if v.is_win and v.p_complete < p_min]",
      "replace": "    return []",
      "must_fail": "test_the_audit_catches_a_frozen_policy_presented_as_a_win",
      "target": "freeze_detector",
      "outcome": "killed"
    },
    {
      "id": "M232-an-unmeasured-pair-is-declared-separable",
      "why": "a pair with no measured entry certifies as SEPARABLE",
      "find": "            if row is None:",
      "replace": "            if False:",
      "must_fail": "test_a_pair_with_no_measured_entry_is_UNIDENTIFIABLE",
      "target": "identifiability",
      "outcome": "killed"
    },
    {
      "id": "M233-a-guessed-KL-rate-enters-the-table",
      "why": "a rate not marked measured is accepted, so a correlation statistic can be estimated",
      "find": "        if not r.measured:",
      "replace": "        if False:",
      "must_fail": "test_a_GUESSED_rate_is_REFUSED_at_the_table",
      "target": "identifiability",
      "outcome": "killed"
    },
    {
      "id": "M234-a-zero-separation-rate-becomes-finite",
      "why": "two models predicting the same thing get a finite T_sep",
      "find": "    if kl_rate <= 0.0:",
      "replace": "    if False:",
      "must_fail": "test_a_zero_KL_rate_gives_INFINITY_not_a_big_number",
      "target": "identifiability",
      "outcome": "killed"
    },
    {
      "id": "M235-the-disjunction-merge-becomes-unconditional",
      "why": "inseparable models stop constraining each other -- THE fatal class",
      "find": "        for m in members[1:]:\n            acc &= actions_by_model[m]",
      "replace": "        for m in members[1:]:\n            acc |= actions_by_model[m]",
      "must_fail": "test_inseparable_models_constrain_you_to_their_INTERSECTION",
      "target": "identifiability",
      "outcome": "killed"
    },
    {
      "id": "M236-a-promised-pair-that-never-separated-passes-the-audit",
      "why": "silence about a pair promised SEPARABLE is read as success",
      "find": "        if got is None or got > slack * v.t_sep:",
      "replace": "        if got is not None and got > slack * v.t_sep:",
      "must_fail": "test_a_pair_that_never_separated_at_all_is_CAUGHT",
      "target": "identifiability",
      "outcome": "killed"
    },
    {
      "id": "M228-the-open-world-floor-is-renormalised-away",
      "why": "the posterior erases M_? like any other unlikely component",
      "find": "        u = max(post[UNKNOWN_MODEL], float(self.alpha_floor))",
      "replace": "        u = post[UNKNOWN_MODEL]",
      "must_fail": "test_overwhelming_evidence_cannot_renormalise_the_floor_away",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M229-the-caller-may-tune-the-open-world-component",
      "why": "M_?'s max-entropy likelihood can be supplied from outside",
      "find": "        if UNKNOWN_MODEL in loglik:",
      "replace": "        if False:",
      "must_fail": "test_a_caller_cannot_supply_the_unknown_likelihood",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M230-BY-degrades-to-BH-under-dependence",
      "why": "the log-m dependence price is dropped on correlated detectors",
      "find": "    harmonic = sum(1.0 / i for i in range(1, m + 1))",
      "replace": "    harmonic = 1.0",
      "must_fail": "test_BY_is_strictly_more_conservative_than_BH",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M231-the-adaptive-threshold-stops-adapting",
      "why": "tau becomes the frozen quantile split-conformal cannot justify",
      "find": "        if gamma <= 0.0:",
      "replace": "        if False:",
      "must_fail": "test_a_zero_gamma_threshold_is_REFUSED",
      "target": "open_world",
      "outcome": "killed"
    },
    {
      "id": "M227-the-independent-checker-accepts-a-forged-verdict",
      "why": "shield_check stops refusing a verdict its own re-derivation contradicts",
      "find": "    return not any(d.kind in (\"SOUNDNESS\", \"STRUCTURE\")\n                   for d in verify(verdict, action, machine))",
      "replace": "    return True",
      "must_fail": "test_the_checker_reports_a_SOUNDNESS_disagreement_by_name",
      "target": "shield_check",
      "outcome": "killed"
    },
    {
      "id": "M219-shield-as2-leg-removed",
      "why": "the shield stops enforcing distance (AS2)",
      "find": "def leg_as2(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_as2(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"AS2\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_AS2_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M220-shield-leak-leg-removed",
      "why": "the shield stops enforcing containment (LEAK)",
      "find": "def leg_leak(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_leak(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"LEAK\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_LEAK_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M221-shield-dem-leg-removed",
      "why": "the shield stops enforcing model existence (DEM)",
      "find": "def leg_dem(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_dem(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"DEM\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_DEM_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M222-shield-prog-leg-removed",
      "why": "the shield stops enforcing the freeze detector (PROG)",
      "find": "def leg_prog(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_prog(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"PROG\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_PROG_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M223-shield-id-leg-removed",
      "why": "the shield stops enforcing identifiability (ID)",
      "find": "def leg_id(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_id(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"ID\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_ID_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M224-shield-par-leg-removed",
      "why": "the shield stops enforcing the set cover (PAR)",
      "find": "def leg_par(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_par(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"PAR\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_PAR_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M225-shield-anc-leg-removed",
      "why": "the shield stops enforcing supply counting (ANC)",
      "find": "def leg_anc(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_anc(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"ANC\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_ANC_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M226-shield-dl-leg-removed",
      "why": "the shield stops enforcing the backlog bound (DL)",
      "find": "def leg_dl(a: Action, m: Machine) -> LegResult:",
      "replace": "def leg_dl(a: Action, m: Machine) -> LegResult:\n    return LegResult(\"DL\", \"PASS\", \"MUTANT: leg removed\")",
      "must_fail": "test_the_DL_leg_is_load_bearing",
      "target": "shield",
      "outcome": "killed"
    },
    {
      "id": "M218-every-outcome-is-tallied-as-a-kill",
      "why": "the tally credits a kill for a skipped or surviving mutant",
      "find": "        tally[o] += 1",
      "replace": "        tally[\"killed\"] += 1",
      "must_fail": "test_a_skip_is_never_tallied_as_a_kill",
      "target": "mutation_outcome",
      "outcome": "killed"
    },
    {
      "id": "M217-a-skipped-mutant-leaves-the-denominator",
      "why": "the score rises as coverage falls: non-kills drop out of the total",
      "find": "    return killed + survived + skipped + timed_out",
      "replace": "    return killed",
      "must_fail": "test_a_skipped_or_timed_out_mutant_stays_in_the_denominator",
      "target": "mutation_outcome",
      "outcome": "killed"
    },
    {
      "id": "M216-a-surviving-mutant-is-scored-as-killed",
      "why": "the gate reports a kill for a mutant its test did not catch",
      "find": "    return \"survived\" if suite_green else \"killed\"",
      "replace": "    return \"killed\"",
      "must_fail": "test_a_surviving_mutant_is_never_scored_as_killed",
      "target": "mutation_outcome",
      "outcome": "killed"
    },
    {
      "id": "M209-the-doc-gate-cannot-see-a-hyphenated-total",
      "why": "a stale `N-mutant` headline passes the doc-claims gate",
      "find": "    (re.compile(r\"\\b(\\d[\\d,]*)-mutants?\\b\", re.I), \"mutants_total\"),",
      "replace": "    (re.compile(r\"THIS PATTERN MATCHES NOTHING\"), \"mutants_total\"),",
      "must_fail": "test_a_hyphenated_bare_total_is_checked_too",
      "target": "doc_claims_gate",
      "outcome": "killed"
    },
    {
      "id": "M207-an-out-of-graph-syndrome-is-decoded-anyway",
      "why": "exact_decode stops refusing a syndrome its graph cannot contain",
      "find": "    if any(t not in adj for t in T):\n        return None, None, \"syndrome names a vertex absent from the graph\"",
      "replace": "    if False:\n        return None, None, \"syndrome names a vertex absent from the graph\"",
      "must_fail": "test_unknown_vertex_is_refused",
      "target": "exact_decode",
      "outcome": "killed"
    },
    {
      "id": "M208-a-fractional-lp-optimum-is-returned-as-a-repair",
      "why": "qldpc_cert rounds a pseudocodeword and calls it a correction (both LP paths: they now share one rule)",
      "find": "    if np.max(np.abs(x - np.round(x))) >= INTEGRALITY_TOL:\n        return None",
      "replace": "    if False:\n        return None",
      "must_fail": "test_a_fractional_optimum_yields_NO_repair_rather_than_a_rounded_one",
      "target": "qldpc_cert",
      "outcome": "killed"
    },
    {
      "id": "M205-a-promoted-lookup-runs-without-an-identity",
      "why": "a passport is assembled from another code's certificate",
      "find": "    if not cid:\n        raise ValueError(",
      "replace": "    if False:\n        raise ValueError(",
      "must_fail": "test_a_promoted_lookup_with_no_identity_is_REFUSED",
      "target": "core",
      "outcome": "killed"
    },
    {
      "id": "M206-a-malformed-pinned-edge-key-is-parsed-anyway",
      "why": "the pinned-edge parser stops naming a malformed artifact",
      "find": "    if not sep or not head or not tail:\n        raise ValueError(",
      "replace": "    if False:\n        raise ValueError(",
      "must_fail": "test_a_malformed_edge_key_is_REFUSED_BY_NAME",
      "target": "device_dem",
      "outcome": "killed"
    },
    {
      "id": "M86-cert-format-trusts-the-declared-graph",
      "why": "graph fingerprint compared against itself: replay across graphs accepted",
      "find": "    if doc.get(\"graph_sha256\") != fp:",
      "replace": "    if False:",
      "must_fail": "test_a_certificate_cannot_be_replayed_against_a_different_graph",
      "target": "cert_format",
      "outcome": "killed"
    },
    {
      "id": "M84-matching-drops-unreachable-terminals",
      "why": "terminals unregistered: an unpairable syndrome silently scores 0",
      "find": "    for u in terms:",
      "replace": "    for u in []:",
      "must_fail": "test_a_disconnected_terminal_yields_no_bound_rather_than_a_wrong_one",
      "target": "coset_matching",
      "outcome": "killed"
    },
    {
      "id": "M85-matching-treats-a-free-flip-as-impossible",
      "why": "zero-cost parity flips excluded, so exact ties lose a coset",
      "find": "        if g is not None:",
      "replace": "        if g is not None and g > 0:",
      "must_fail": "test_it_reproduces_a_constructed_tie",
      "target": "coset_matching",
      "outcome": "killed"
    },
    {
      "id": "M81-beacon-accepts-a-current-round",
      "why": "future-round guard dropped: a published round can be committed to",
      "find": "    if lead_seconds <= 0:",
      "replace": "    if False:",
      "must_fail": "test_committing_to_a_current_or_past_round_is_REFUSED",
      "target": "beacon",
      "outcome": "killed"
    },
    {
      "id": "M82-beacon-parameters-not-bound",
      "why": "declared parameters dropped from the derivation domain",
      "find": "    return f\"{domain}|shots={shots}|q={q.numerator}/{q.denominator}\" \\\n           f\"|mech={n_mechanisms}\"",
      "replace": "    return domain",
      "must_fail": "test_every_declared_parameter_changes_the_whole_stream",
      "target": "beacon",
      "outcome": "killed"
    },
    {
      "id": "M83-beacon-verify-accepts-anything",
      "why": "schedule verification stops comparing against the beacon",
      "find": "    if got == expect:",
      "replace": "    if True:",
      "must_fail": "test_every_tamper_is_refused",
      "target": "beacon",
      "outcome": "killed"
    },
    {
      "id": "M75-merkle-no-domain-separation",
      "why": "receipt leaves hashed without the RFC-6962 leaf prefix",
      "find": "    return sha256_hex(_LEAF + canon(receipt))",
      "replace": "    return sha256_hex(canon(receipt))",
      "must_fail": "test_domain_separation_leaf_is_not_a_bare_hash",
      "target": "rehearsal",
      "outcome": "killed"
    },
    {
      "id": "M76-merkle-odd-node-duplicated",
      "why": "odd tail node duplicated instead of promoted",
      "find": "            nxt.append(level[-1])          # odd node promoted, RFC-6962",
      "replace": "            nxt.append(_pair(level[-1], level[-1]))",
      "must_fail": "test_every_index_of_every_tree_size_proves_inclusion",
      "target": "rehearsal",
      "outcome": "killed"
    },
    {
      "id": "M77-anomaly-lane-ignores-threshold",
      "why": "anomaly lane admits every model failure regardless of Delta_L",
      "find": "        if Fraction(d) >= thr:",
      "replace": "        if True:",
      "must_fail": "test_anomaly_lane_threshold_is_inclusive_and_strict_below",
      "target": "rehearsal",
      "outcome": "killed"
    },
    {
      "id": "M78-anomaly-lane-accepts-any-class",
      "why": "anomaly lane no longer restricts itself to the model class",
      "find": "        if r.get(\"class\") != \"model\":",
      "replace": "        if False:",
      "must_fail": "test_anomaly_lane_selects_only_large_delta_model_failures",
      "target": "rehearsal",
      "outcome": "killed"
    },
    {
      "id": "M79-freeze-drops-the-analysis-pins",
      "why": "freeze manifest omits the analysis section it exists to seal",
      "find": "            \"analysis\": dict(sorted(analysis.items()))}",
      "replace": "            \"analysis\": {}}",
      "must_fail": "test_freeze_manifest_commitment_moves_when_the_threshold_moves",
      "target": "rehearsal",
      "outcome": "killed"
    },
    {
      "id": "M80-coset-cert-accepts-wrong-coset-candidate",
      "why": "H' tier accepts a candidate from the opposite coset",
      "find": "        if sum(1 for i in candidate if i in set(cut_vars)) % 2 \\\n                != int(target_parity) % 2:",
      "replace": "        if False:",
      "must_fail": "test_wrong_coset_candidate_is_refused",
      "target": "coset_cert",
      "outcome": "killed"
    },
    {
      "id": "M67-decomposition-tolerance-restored",
      "why": "search test uses a float tolerance instead of exact >",
      "find": "    if w > u_same:",
      "replace": "    if float(w) - float(u_same) > 1e-12:",
      "must_fail": "test_sub_tolerance_excess_is_search_not_agree",
      "target": "decomposition",
      "outcome": "killed"
    },
    {
      "id": "M68-decomposition-float-tie",
      "why": "ambiguity test compares floats instead of exact Fractions",
      "find": "    if u_same == u_opp:",
      "replace": "    if float(u_same) == float(u_opp):",
      "must_fail": "test_float_indistinguishable_optima_are_not_a_tie",
      "target": "decomposition",
      "outcome": "killed"
    },
    {
      "id": "M69-decomposition-invariant-dropped",
      "why": "correction lighter than its own coset optimum is classified, not refused",
      "find": "    if w < u_same:",
      "replace": "    if False:",
      "must_fail": "test_correction_lighter_than_its_own_optimum_refuses",
      "target": "decomposition",
      "outcome": "killed"
    },
    {
      "id": "M70-decomposition-sector-is-decoder-parity",
      "why": "certified sector taken from the decoder instead of the lighter coset",
      "find": "    sector = corr_parity if u_same < u_opp else 1 - corr_parity",
      "replace": "    sector = corr_parity",
      "must_fail": "test_certified_sector_follows_the_lighter_coset_not_the_decoder",
      "target": "decomposition",
      "outcome": "killed"
    },
    {
      "id": "M71-logical-cut-agreement-not-enforced",
      "why": "two-source cut disagreement accepted instead of refused",
      "find": "    if a != b:",
      "replace": "    if False:",
      "must_fail": "test_disagreement_is_refused_loudly",
      "target": "logical_cut",
      "outcome": "killed"
    },
    {
      "id": "M72-logical-cut-ignores-separators",
      "why": "DEM separator components not split: dominant computed over an incomplete set",
      "find": "            if t.is_separator():",
      "replace": "            if False:",
      "must_fail": "test_the_two_sources_agree_on_a_real_surface_code",
      "target": "logical_cut",
      "outcome": "killed"
    },
    {
      "id": "M73-bridge-transfers-without-margin",
      "why": "fixed-point bridge promotes every decision regardless of the margin",
      "find": "    transferred = eps_q < rho",
      "replace": "    transferred = True",
      "must_fail": "test_fragile_decision_is_never_promoted_constructed",
      "target": "fixed_point_bridge",
      "outcome": "killed"
    },
    {
      "id": "M74-bridge-zero-weight-not-clamped",
      "why": "quantization allows a weight to reach zero, voiding the lemma's precondition",
      "find": "        if wq <= 0:",
      "replace": "        if False:",
      "must_fail": "test_zero_rounding_is_clamped_and_counted",
      "target": "fixed_point_bridge",
      "outcome": "killed"
    },
    {
      "id": "M65-bayes-parity-ignores-cut",
      "why": "Bayesian tier classifies by total parity instead of cut parity",
      "find": "    x = particular\n    z[bin(x & cut_mask).count(\"1\") & 1] += mass(x)",
      "replace": "    x = particular\n    z[bin(x).count(\"1\") & 1] += mass(x)",
      "must_fail": "test_matches_brute_force_on_random_instances",
      "target": "bayes_coset",
      "outcome": "killed"
    },
    {
      "id": "M297-bayes-parity-ignores-cut-in-the-walk",
      "why": "the Gray-code walk bins by TOTAL parity instead of cut parity, so every term but the seed lands in the wrong coset",
      "find": "        x ^= kernel[changed.bit_length() - 1]\n        z[bin(x & cut_mask).count(\"1\") & 1] += mass(x)",
      "replace": "        x ^= kernel[changed.bit_length() - 1]\n        z[bin(x).count(\"1\") & 1] += mass(x)",
      "must_fail": "test_matches_brute_force_on_random_instances",
      "target": "bayes_coset",
      "outcome": "killed"
    },
    {
      "id": "M66-bayes-kernel-guard-dropped",
      "why": "oversize kernels walked instead of refused",
      "find": "if k > KERNEL_LIMIT:",
      "replace": "if False:",
      "must_fail": "test_kernel_guard_refuses_oversize",
      "target": "bayes_coset",
      "outcome": "killed"
    },
    {
      "id": "M63-oracle-drops-parity",
      "why": "coset oracle ignores path parity: both cosets return the same optimum",
      "find": "q = p ^ addp",
      "replace": "q = p",
      "must_fail": "test_matches_brute_force_on_every_syndrome_of_the_path_graph",
      "target": "coset_oracle",
      "outcome": "killed"
    },
    {
      "id": "M64-robustness-uncapped",
      "why": "robustness radius emitted without the w_min/2 cap",
      "find": "return min(rho, wm / 2)",
      "replace": "return rho",
      "must_fail": "test_refusals",
      "target": "robustness",
      "outcome": "killed"
    },
    {
      "id": "M59-exact-lp-ratio-test-max",
      "why": "simplex ratio test takes the largest step instead of the smallest",
      "find": "if best_t is None or t < best_t or (t == best_t and i < enter):",
      "replace": "if best_t is None or t > best_t or (t == best_t and i < enter):",
      "must_fail": "test_known_tiny_optimum",
      "target": "exact_lp",
      "outcome": "killed"
    },
    {
      "id": "M60-exact-lp-stops-early",
      "why": "optimality declared while a multiplier is still negative",
      "find": "if lam[pos] < 0 and (leave_idx is None or i < leave_idx):",
      "replace": "if lam[pos] < -1 and (leave_idx is None or i < leave_idx):",
      "must_fail": "test_ulp_scale_optimum_is_exact",
      "target": "exact_lp",
      "outcome": "killed"
    },
    {
      "id": "M62-repair-unflagged",
      "why": "a certified repair shipping without repaired=True",
      "find": "repaired=True,",
      "replace": "repaired=False,",
      "must_fail": "test_certify_or_repair_repairs_a_provably_suboptimal_correction",
      "target": "certifying_decoder",
      "outcome": "killed"
    },
    {
      "id": "M0-weak-key-mint",
      "why": "the historical defect: minting with a publicly-derivable key (bytes(32) seed)",
      "find": "if pub_hex in WEAK_PUBKEYS and not allow_weak_key:",
      "replace": "if False:",
      "must_fail": "test_genesis_0_all_zeros_seed_is_refused_at_mint",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M28-doc-gate-skips-every-line",
      "why": "the prose scanner silently matching nothing, so stale headlines pass",
      "find": "            if fenced:",
      "replace": "            if True:",
      "must_fail": "test_a_stale_headline_in_the_docs_is_actually_caught",
      "target": "doc_claims_gate",
      "outcome": "killed"
    },
    {
      "id": "M29-tautology-detector-blinded",
      "why": "a replica set of N identical runs no longer reported as a tautology",
      "find": "    return \"duplicate\"",
      "replace": "    return \"distinct\"",
      "must_fail": "test_duplicate_replicas_are_flagged_but_missing_pivots_are_not",
      "target": "verify_closures",
      "outcome": "killed"
    },
    {
      "id": "M30-blind-decoder-reports-anyway",
      "why": "an inert-herald decoder emitting V_E = 1.000 instead of refusing",
      "find": "    if not herald_influences:",
      "replace": "    if False:",
      "must_fail": "test_a_blind_decoder_refuses_instead_of_returning_one_point_zero",
      "target": "erasure_decoder",
      "outcome": "killed"
    },
    {
      "id": "M31-conditional-dem-ignores-the-herald",
      "why": "the per-pattern DEM no longer conditions on which heralds fired",
      "find": "ms = sorted({q for (rr, q) in sites if rr == r and fired.get((rr, q))})",
      "replace": "ms = sorted({q for (rr, q) in sites if rr == r and False})",
      "must_fail": "test_a_fired_erasure_becomes_a_free_edge_and_a_silent_one_vanishes",
      "target": "ve_reweighted",
      "outcome": "killed"
    },
    {
      "id": "M32-blind-arm-mispriced",
      "why": "the blind arm decoding with eps instead of the 0.75*eps marginal",
      "find": "out.append(\"DEPOLARIZE1\", [q], 0.75 * eps)",
      "replace": "out.append(\"DEPOLARIZE1\", [q], eps)",
      "must_fail": "test_the_blind_arm_gets_the_exact_marginal_not_the_raw_rate",
      "target": "h1_reweighted",
      "outcome": "killed"
    },
    {
      "id": "M33-epistemic-channels-unenforced",
      "why": "check_channels admitting any Evidence into any channel",
      "find": "        if ev.role is not need:",
      "replace": "        if False:",
      "must_fail": "test_a_herald_cannot_enter_the_observation_channel",
      "target": "evidence",
      "outcome": "killed"
    },
    {
      "id": "M34-admissibility-emits-unconsumed",
      "why": "emit() no longer refusing when side information is not consumed",
      "find": "        if not m[\"A_causal_reachability\"].satisfied:",
      "replace": "        if False:",
      "must_fail": "test_an_unconsumed_input_refuses_not_reports",
      "target": "admissibility",
      "outcome": "killed"
    },
    {
      "id": "M91-checker-readmits-node-potentials",
      "why": "node potentials re-permitted: the mixed-dual break reopens and a weight-20 correction certifies as optimal against a true optimum of 2",
      "find": "    if cert.node_potentials:",
      "replace": "    if False:",
      "must_fail": "test_the_legacy_checker_now_refuses_the_forgery",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M36-checker-skips-dual-feasibility",
      "why": "dual feasibility no longer tested, so an inflated dual certifies anything",
      "find": "        if lhs > w + tol:",
      "replace": "        if False:",
      "must_fail": "test_refuses_an_infeasible_dual",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M37-checker-ignores-odd-degree-set",
      "why": "odd-degree set no longer compared to the syndrome",
      "find": "    if odd != fired:",
      "replace": "    if False:",
      "must_fail": "test_refuses_when_odd_degree_set_disagrees_with_the_syndrome",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M38-checker-accepts-a-gap",
      "why": "a positive primal-dual gap no longer refused",
      "find": "    if gap > 1e-6 or not proven:",
      "replace": "    if False:",
      "must_fail": "test_refuses_a_feasible_dual_that_does_not_reach_the_primal",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M39-moat-admits-even-sets",
      "why": "balls with an EVEN intersection with T admitted as dual variables",
      "find": "            if len(members & T) % 2 == 1:",
      "replace": "            if True:",
      "must_fail": "test_moat_producer_never_emits_an_even_set_dual",
      "target": "moat_dual",
      "outcome": "killed"
    },
    {
      "id": "M40-engine-admits-even-sets",
      "why": "MoatEngine drops the T-odd admissibility rule the reference enforces",
      "find": "        odd = (memb[:, t_idx].sum(axis=1) % 2) == 1",
      "replace": "        odd = memb[:, t_idx].sum(axis=1) >= 0",
      "must_fail": "test_engine_agrees_with_the_reference_producer",
      "target": "moat_dual",
      "outcome": "killed"
    },
    {
      "id": "M41-battery-audit-cannot-say-no",
      "why": "assert_all_false stops reporting an attack that is not actually heavier",
      "find": "                bogus.append((name, \"not actually heavier than a known correction\"))",
      "replace": "                pass",
      "must_fail": "test_the_battery_detects_a_bogus_attack",
      "target": "forgeries",
      "outcome": "killed"
    },
    {
      "id": "M42-battery-drops-the-stealth-attacks",
      "why": "the strictly-suboptimal (syndrome-consistent) attacks stop being generated",
      "find": "        if _odd(cand) == T and sum(W[k] for k in cand) > primal + 1e-9:",
      "replace": "        if False:",
      "must_fail": "test_the_battery_still_contains_the_hardest_attack_class",
      "target": "forgeries",
      "outcome": "killed"
    },
    {
      "id": "M43-ladder-accepts-without-checking",
      "why": "a ladder rung reports success without the checker agreeing",
      "find": "            if r.accepted:",
      "replace": "            if True:",
      "must_fail": "test_escalating_the_ladder_never_certifies_a_worse_correction",
      "target": "certifying_decoder",
      "outcome": "killed"
    },
    {
      "id": "M44-cs-family-splits-foreign-trees",
      "why": "atomic tree absorption dropped; emitted sets slice foreign trees",
      "find": "            if x in tree_of and tree_of[x] != home and x not in A:",
      "replace": "            if False:",
      "must_fail": "test_cs_family_sets_cut_the_correction_exactly_once",
      "target": "moat_growth",
      "outcome": "killed"
    },
    {
      "id": "M45-blossom-drops-boundary-moats",
      "why": "boundary-containing moats dropped instead of emitted as complements",
      "find": "        if BOUNDARY in S:\n            S = allv - S",
      "replace": "        if BOUNDARY in S:\n            return",
      "must_fail": "test_blossom_candidates_express_boundary_crossing_moats",
      "target": "moat_growth",
      "outcome": "killed"
    },
    {
      "id": "M46-qldpc-dual-sign-inverted",
      "why": "the qLDPC dual load computed with inverted facet signs",
      "find": "            coef = -y if i in Fs else y",
      "replace": "            coef = y if i in Fs else -y",
      "must_fail": "test_qldpc_certificate_sound_aligned_and_unfooled",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M47-qldpc-feasibility-unchecked",
      "why": "per-variable dual feasibility skipped: inflated duals certify anything",
      "find": "        if ld - mu.get(i, 0.0) > w.get(i, 0.0) + tol:",
      "replace": "        if False:",
      "must_fail": "test_an_inflated_dual_cannot_certify_a_heavier_error",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M48-qldpc-syndrome-not-recomputed",
      "why": "claimed error's syndrome taken on faith instead of recomputed",
      "find": "        if len(e & supports[j]) % 2 != syn[j]:\n            return None, None, QldpcCheckResult(",
      "replace": "        if False:\n            return None, None, QldpcCheckResult(",
      "must_fail": "test_a_non_correction_with_matching_weight_is_refused",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M298-qldpc-bnb-target-takes-the-syndrome-on-faith",
      "why": "the branch-and-bound target stops recomputing the claimed error's syndrome, so the SEARCH accepts a non-correction even where the final validator would have caught it",
      "find": "        if len(e & supports[j]) % 2 != syn[j]:\n            return None, QldpcCheckResult(",
      "replace": "        if False:\n            return None, QldpcCheckResult(",
      "must_fail": "test_the_TREE_path_recomputes_the_syndrome_too",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M299-qldpc-exact-takes-the-syndrome-on-faith",
      "why": "`check_qldpc_exact` -- the entry point a stranger calls -- stops recomputing the syndrome of the error it is handed",
      "find": "            if len(e & supports[j]) % 2 != syn[j]:\n                return QldpcCheckResult(",
      "replace": "            if False:\n                return QldpcCheckResult(",
      "must_fail": "test_the_EXACT_path_recomputes_the_syndrome_too",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M49-qldpc-redundant-parity-forgotten-exact",
      "why": "redundant-check parity not XORed from syndrome bits (exact path)",
      "find": "            for r in rows:\n                sup = sup ^ supports[r]\n                par ^= syn[r]",
      "replace": "            for r in rows:\n                sup = sup ^ supports[r]",
      "must_fail": "test_redundant_check_parity_is_XORED_from_the_syndrome_bits_exact",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M49b-qldpc-redundant-parity-forgotten-float",
      "why": "redundant-check parity not XORed from syndrome bits (float path)",
      "find": "        for r in rows:\n            sup = sup ^ supports[r]\n            par ^= syn[r]",
      "replace": "        for r in rows:\n            sup = sup ^ supports[r]",
      "must_fail": "test_the_three_cycle_pseudocodeword_closes_and_certifies",
      "target": "qldpc_check",
      "outcome": "killed"
    },
    {
      "id": "M50-exact-tier-separates-even-cuts",
      "why": "Padberg-Rao scan admits T-even sides; the exact tier goes inert",
      "find": "        if len(set(side) & Tp) % 2 == 1:",
      "replace": "        if len(set(side) & Tp) % 2 == 0:",
      "must_fail": "test_exact_tier_equals_the_oracle_and_survives_the_repack",
      "target": "tjoin_exact",
      "outcome": "killed"
    },
    {
      "id": "M51-exact-tier-drops-boundary-sides",
      "why": "boundary-containing GH sides discarded instead of complemented",
      "find": "        if BOUNDARY in S:\n            S = allv - S\n        if not S or BOUNDARY in S:\n            return None\n        return S",
      "replace": "        if BOUNDARY in S:\n            return None\n        if not S:\n            return None\n        return S",
      "must_fail": "test_exact_tier_equals_the_oracle_and_survives_the_repack",
      "target": "tjoin_exact",
      "outcome": "killed"
    },
    {
      "id": "M52-exact-tier-stale-duals",
      "why": "final re-solve deleted: duals stale against the pool on early exit",
      "find": "    if pool and len(duals) != len(pool):",
      "replace": "    if False:",
      "must_fail": "test_round_capped_run_still_resolves_its_final_pool",
      "target": "tjoin_exact",
      "outcome": "killed"
    },
    {
      "id": "M53-provenance-sweep-blinded",
      "why": "every number treated as traced; orphan claims sail through",
      "find": "                if tok in truth or tok in wl:\n                    continue\n                orphans.append((rel, ln, tok, line.strip()[:90]))\n            for m in RATE.finditer(line):",
      "replace": "                if True:\n                    continue\n                orphans.append((rel, ln, tok, line.strip()[:90]))\n            for m in RATE.finditer(line):",
      "must_fail": "test_provenance_gate_catches_a_planted_orphan",
      "target": "claims_provenance_gate",
      "outcome": "killed"
    },
    {
      "id": "M1-weak-key-verify",
      "why": "accepting a passport signed with a publicly-derivable key",
      "find": "checks[\"key_not_weak\"] = pub not in WEAK_PUBKEYS",
      "replace": "checks[\"key_not_weak\"] = True",
      "must_fail": "test_genesis_0_weak_signed_passport_is_rejected_at_verify",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M2-no-provenance",
      "why": "the old schema: provenance outside the signed core, so a result can be relabelled",
      "find": "\"provenance\": provenance.to_core(),",
      "replace": "",
      "must_fail": "test_swapping_the_backend_breaks_the_digest",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M3-lattice-violation",
      "why": "letting ACHIEVED exceed CLAIMED (breaks ACHIEVED = min(CLAIMED, REVERIFIABLE))",
      "find": "achieved_final = max(0, min(int(claimed), int(achieved))) if valid else 0",
      "replace": "achieved_final = max(0, int(achieved)) if valid else 0",
      "must_fail": "test_claiming_less_than_you_can_prove_is_honoured",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M4-digest-not-checked",
      "why": "accepting a tampered core because the digest is not recomputed",
      "find": "checks[\"digest\"] = recomputed == passport[\"digest\"]",
      "replace": "checks[\"digest\"] = True",
      "must_fail": "test_tampering_with_the_claim_breaks_the_digest",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M6-false-proof",
      "why": "a certified lower bound that exceeds the true distance -- a FALSE PROOF",
      "find": "if best_w is not None and lb >= best_w:\n            wit =",
      "replace": "if True:\n            wit =",
      "must_fail": "test_certified_bound_never_exceeds_true_distance",
      "target": "isd",
      "outcome": "killed"
    },
    {
      "id": "M7-stabilizer-as-logical",
      "why": "INVERTING the logical/stabilizer filter -- returns a stabilizer as the witness",
      "find": "keep = ~ls.is_stabilizer_batch(V)",
      "replace": "keep = ls.is_stabilizer_batch(V)",
      "must_fail": "test_proof_agrees_with_ibm_on_exact_codes",
      "target": "isd",
      "outcome": "killed"
    },
    {
      "id": "M8-reconstruction-unchecked",
      "why": "returning a code whose k disagrees with the published catalogue",
      "find": "if expect_k is not None and code.k != expect_k:",
      "replace": "if False:",
      "must_fail": "test_reconstruction_is_faithful_to_the_catalogue",
      "target": "gbb",
      "outcome": "killed"
    },
    {
      "id": "M9-deficiency-ignored",
      "why": "charging nothing for rank deficiency -- inflates the Zimmermann bound into a FALSE PROOF",
      "find": "info.contribution = max(0, p + 1 - info.deficiency)",
      "replace": "info.contribution = max(0, p + 1)",
      "must_fail": "test_certified_bound_never_exceeds_a_distance_ibm_proved",
      "target": "bz",
      "outcome": "killed"
    },
    {
      "id": "M10-enumeration-incomplete",
      "why": "dropping the Y option per qubit -- the enumeration misses operators it claims to have covered",
      "find": "t = np.indices((3,) * p).reshape(p, -1).T if p else np.zeros((1, 0), int)",
      "replace": "t = np.indices((2,) * p).reshape(p, -1).T if p else np.zeros((1, 0), int)",
      "must_fail": "test_enumeration_is_exhaustive_at_its_stated_depth",
      "target": "bz",
      "outcome": "killed"
    },
    {
      "id": "M11-weight-ignores-Z",
      "why": "counting only the X half as qubit weight -- every weight the engine reports is wrong",
      "find": "return popcount(V[..., :nwh] | V[..., nwh:], xp)",
      "replace": "return popcount(V[..., :nwh], xp)",
      "must_fail": "test_split_packing_roundtrips_and_weighs_correctly",
      "target": "bz",
      "outcome": "killed"
    },
    {
      "id": "M12-keeps-stabilizers",
      "why": "inverting the stabilizer filter -- the certificate's witness is a stabilizer, not a logical",
      "find": "keep = par.astype(bool)",
      "replace": "keep = ~par.astype(bool)",
      "must_fail": "test_certificate_is_self_contained_and_the_witness_is_logical",
      "target": "bz",
      "outcome": "killed"
    },
    {
      "id": "M13-no-catchup",
      "why": "letting a set contribute a bound without enumerating its shallow depths",
      "find": "todo = [(i, q) for i in active for q in range(swept[i] + 1, p + 1)]",
      "replace": "todo = [(i, q) for i in active for q in range(p, p + 1)]",
      "must_fail": "test_a_set_only_contributes_a_bound_it_enumerated_every_level_for",
      "target": "bz",
      "outcome": "killed"
    },
    {
      "id": "M14-mitm-drops-a-split",
      "why": "skipping a (h1,h2) split -- the collision sweep silently stops being exhaustive",
      "find": "if h1 + h2 > W + 1:",
      "replace": "if h1 + h2 > W + 1 or h2 == 1:",
      "must_fail": "test_mitm_withdraws_its_negative_claim_if_any_split_went_unswept",
      "target": "mitm",
      "outcome": "killed"
    },
    {
      "id": "M15-mitm-skips-stabilizer-rejection",
      "why": "accepting a stabilizer as a logical -- MITM reports a distance that is not one",
      "find": "        if ls.is_stabilizer(pack(v[None, :])[0]):\n            return None",
      "replace": "        if False:\n            return None",
      "must_fail": "test_mitm_returns_a_genuine_logical_not_a_stabilizer",
      "target": "mitm",
      "outcome": "killed"
    },
    {
      "id": "M16-sat-one-anchor-only",
      "why": "checking a single translation anchor -- UNSAT no longer covers every operator",
      "find": "    for case in (0, 1):\n        cnf, xv, zv = _build_instance(code, W, case)\n        with Solver(",
      "replace": "    for case in (0,):\n        cnf, xv, zv = _build_instance(code, W, case)\n        with Solver(",
      "must_fail": "test_sat_unsat_is_refused_unless_both_anchor_cases_were_decided",
      "target": "satdist",
      "outcome": "killed"
    },
    {
      "id": "M17-sat-drops-cardinality",
      "why": "omitting the weight bound -- SAT answers a different question than the one asked",
      "find": "    card = CardEnc.atmost(lits=wv, bound=W, vpool=pool, encoding=EncType.seqcounter)",
      "replace": "    card = CardEnc.atmost(lits=wv, bound=len(wv), vpool=pool, encoding=EncType.seqcounter)",
      "must_fail": "test_sat_matches_brute_force_both_directions",
      "target": "satdist",
      "outcome": "killed"
    },
    {
      "id": "M18-router-accepts-unwitnessed-target",
      "why": "certifying a bound with no verified witness -- the anti-tautology guard removed",
      "find": "dec.refusal = \"no verified witness at the claimed bound -- refusing a target without evidence\"",
      "replace": "dec.witness_weight = upper_bound; witness = np.zeros(2 * n, dtype=np.uint8)",
      "must_fail": "test_router_refuses_a_target_it_cannot_witness",
      "target": "certify",
      "outcome": "killed"
    },
    {
      "id": "M5-floats-allowed",
      "why": "permitting floats in the signed core (cross-language formatting divergence)",
      "find": "raise TypeError(\n            \"floats are not permitted",
      "replace": "return obj\n    if False:\n        raise TypeError(\n            \"floats are not permitted",
      "must_fail": "test_floats_are_refused_in_the_core",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M19-coverage-always-admissible",
      "why": "the Negative Claim Admission Contract rubber-stamping every claim",
      "find": "admissible = complete and corpus_can_trigger_target is not False",
      "replace": "admissible = True",
      "must_fail": "test_a_missing_partition_refuses_the_claim",
      "target": "coverage",
      "outcome": "killed"
    },
    {
      "id": "M20-coverage-empty-schedule-passes",
      "why": "an empty required set counted as complete coverage -- the all([]) vacuity",
      "find": "complete = (not omitted) and (not a_missing) and bool(req or a_req)",
      "replace": "complete = (not omitted) and (not a_missing)",
      "must_fail": "test_an_empty_schedule_cannot_support_a_negative_claim",
      "target": "coverage",
      "outcome": "killed"
    },
    {
      "id": "M21-spectrum-always-complete",
      "why": "multiplicity declared complete below its own weight -- the retracted 18x",
      "find": "return max(0, int(self.completed_depth))",
      "replace": "return 10**9",
      "must_fail": "test_spectrum_below_its_own_distance_is_partial_not_complete",
      "target": "coverage",
      "outcome": "killed"
    },
    {
      "id": "M22-ledger-unswept-set-contributes",
      "why": "crediting a bound to an information set that was never enumerated",
      "find": "return sum(max(0, int(depths.get(i, -1)) + 1 - int(d))",
      "replace": "return sum(max(0, int(depths.get(i, 10**9)) + 1 - int(d))",
      "must_fail": "test_an_unenumerated_set_earns_nothing",
      "target": "ledger",
      "outcome": "killed"
    },
    {
      "id": "M23-transversal-never-refuses",
      "why": "a transversal certifier that certifies everything, witness or not",
      "find": "if _is_logical(code, v, rankH, Hs):",
      "replace": "if False:",
      "must_fail": "test_it_REFUSES_when_a_logical_fits_inside_the_hot_set",
      "target": "transversal",
      "outcome": "killed"
    },
    {
      "id": "M24-survival-ignores-amplitude",
      "why": "declaring a mixture observable from the SPECTRUM alone -- the overclaim a reviewer caught: at dwell 100 lambda2/lambda1 is 0.98 while the second amplitude is 1.4e-3 and the hazard is flat to 0.7%",
      "find": "observable_mixture=bool(variation > 1.05 and amp > 1e-3),",
      "replace": "observable_mixture=True,",
      "must_fail": "test_FAST_switching_is_NOT_observable_despite_a_two_moded_spectrum",
      "target": "survival",
      "outcome": "killed"
    },
    {
      "id": "M25-lattice-permits-overclaim",
      "why": "letting a claim be presented ABOVE its evidence -- the forgery, unblocked",
      "find": "return int(claimed) > int(reverifiable)",
      "replace": "return False",
      "must_fail": "test_the_forgery_as_arithmetic",
      "target": "lattice",
      "outcome": "killed"
    },
    {
      "id": "M26-guard-layer-c-blind",
      "why": "Layer C accepting a replayed syndrome from another round",
      "find": "if sd != expected.syndrome_digest:",
      "replace": "if False:",
      "must_fail": "test_layer_c_catches_a_replayed_syndrome_that_A_and_B_both_pass",
      "target": "guard",
      "outcome": "killed"
    },
    {
      "id": "M27-modelgate-defaults-to-admit",
      "why": "an unregistered (model, quantity) pair silently ADMITTED -- the real rubber-stamp risk, since refusal-by-default IS the contract",
      "find": "f\"no admission on record for ({model}, {quantity}) \"\n                             f\"-- default is REFUSE, not 'probably fine'\")",
      "replace": "f\"no admission on record\") if False else Admission(model, quantity, True, \"STAMPED\")",
      "must_fail": "test_an_unknown_pair_is_refused_not_defaulted",
      "target": "modelgate",
      "outcome": "killed"
    },
    {
      "id": "M55-a5-certifies-a-superset",
      "why": "A5 accepts a block measuring more than its stabilizer",
      "find": "    if measured != set(terminals):",
      "replace": "    if not set(terminals) <= measured:",
      "must_fail": "test_routing_through_a_FOREIGN_data_qubit_is_refused",
      "target": "heavyhex_transpiler",
      "outcome": "killed"
    },
    {
      "id": "M56-a5-ignores-dirty-scratch",
      "why": "A5 accepts a block that leaves scratch qubits holding data",
      "find": "        elif val[q] & data:",
      "replace": "        elif False:",
      "must_fail": "test_a_block_that_leaves_scratch_dirty_is_refused",
      "target": "heavyhex_transpiler",
      "outcome": "killed"
    },
    {
      "id": "M57-a6-accepts-a-stabilizer-as-the-symmetry",
      "why": "A6 picks a 'conserved quantity' inside the stabilizer group",
      "find": "        if gf2_in_rowspace(xb, xp, v, n):",
      "replace": "        if False:",
      "must_fail": "test_conserved_quantity_lies_outside_the_stabilizer_group",
      "target": "symmetry_decoding",
      "outcome": "killed"
    },
    {
      "id": "M215-weight-corruption-is-a-no-op",
      "why": "the complementarity control stops being a control",
      "find": "            args[0] = min(0.49, args[0] * factor)",
      "replace": "            args[0] = args[0]",
      "must_fail": "test_weight_corruption_actually_changes_a_weight",
      "target": "l2prime",
      "outcome": "killed"
    },
    {
      "id": "M210-receipt-skips-admissibility",
      "why": "verify_receipt accepting a packing over rows this coset never satisfied",
      "find": "    if not check_rows_admissible(syndrome, rows, p):",
      "replace": "    if False:",
      "must_fail": "test_forgery_wrong_parity_with_honest_rebinding",
      "target": "coset_lower_check",
      "outcome": "killed"
    },
    {
      "id": "M211-bnb-value-need-not-be-attained",
      "why": "verify_bnb accepting a claimed optimum no closed leaf reaches",
      "find": "    if value not in attained:",
      "replace": "    if False:",
      "must_fail": "test_forged_value_is_refused",
      "target": "coset_bnb_check",
      "outcome": "killed"
    },
    {
      "id": "M212-bnb-prune-always-holds",
      "why": "verify_bnb accepting a subtree bound weaker than the claim",
      "find": "    if forced + bound < value:",
      "replace": "    if False:",
      "must_fail": "test_a_too_weak_prune_is_refused",
      "target": "coset_bnb_check",
      "outcome": "killed"
    },
    {
      "id": "M213-proofrank-trusts-the-stated-rank",
      "why": "verify_proofrank accepting a rank the span does not support",
      "find": "    if receipt.get(\"proof_rank\") != rank:",
      "replace": "    if False:",
      "must_fail": "test_proofrank_receipt_round_trips_and_refuses_forgeries",
      "target": "logical_contrast",
      "outcome": "killed"
    },
    {
      "id": "M214-radius-divides-by-zero-uncertainty",
      "why": "fixed_witness_radius mishandling an invulnerable competitor",
      "find": "    if denom == 0:",
      "replace": "    if False:",
      "must_fail": "test_no_uncertainty_on_the_difference_means_invulnerable",
      "target": "robust_radius",
      "outcome": "killed"
    },
    {
      "id": "M100-cx-propagates-x-but-not-z",
      "why": "CX drops its Z rule: every Z fault before a CX loses its partner",
      "find": "A[:, t] ^= A[:, c]\n                A[:, nq + c] ^= A[:, nq + t]",
      "replace": "A[:, t] ^= A[:, c]",
      "must_fail": "test_every_signature_matches_injected_ground_truth",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M101-cx-z-rule-points-the-wrong-way",
      "why": "CX Z rule reversed: control and target exchanged",
      "find": "A[:, nq + c] ^= A[:, nq + t]",
      "replace": "A[:, nq + t] ^= A[:, nq + c]",
      "must_fail": "test_every_signature_matches_injected_ground_truth",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M102-cz-couples-a-qubit-to-itself",
      "why": "CZ reading its own column: the two-qubit correlation disappears",
      "find": "A[:, nq + b] ^= A[:, a]",
      "replace": "A[:, nq + b] ^= A[:, b]",
      "must_fail": "test_each_unexercised_gate_agrees_with_the_injection_oracle",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M103-h-stops-exchanging-x-and-z",
      "why": "H as identity on the Pauli frame",
      "find": "\"H\": (\"swap\",), \"H_XZ\": (\"swap\",),",
      "replace": "\"H\": (), \"H_XZ\": (\"swap\",),",
      "must_fail": "test_the_single_qubit_table_matches_stims_own_definition",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M104-s-and-sqrt-x-swap-their-column-ops",
      "why": "S given SQRT_X's action: a plausible, wrong Clifford table",
      "find": "\"S\": (\"z^=x\",), \"SQRT_Z\": (\"z^=x\",),",
      "replace": "\"S\": (\"x^=z\",), \"SQRT_Z\": (\"z^=x\",),",
      "must_fail": "test_the_single_qubit_table_matches_stims_own_definition",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M105-mr-stops-resetting",
      "why": "measure-and-reset losing its reset: faults leak across the boundary",
      "find": "        if name in _MEASURE_RESET:\n            # Backward order",
      "replace": "        if False:\n            # Backward order",
      "must_fail": "test_every_signature_matches_injected_ground_truth",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M106-mr-resets-only-the-x-component",
      "why": "half a reset: the Z component survives a fresh qubit",
      "find": "            A[:, q] = 0\n            A[:, nq + q] = 0\n        for r in rec_rows",
      "replace": "            A[:, q] = 0\n        for r in rec_rows",
      "must_fail": "test_every_signature_matches_injected_ground_truth",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M107-merge-convention-reverts-to-p-over-m",
      "why": "depolarizing channels split as p/m instead of 1-(1-p)^(1/m)",
      "find": "return 1.0 - (1.0 - p) ** (1.0 / m)",
      "replace": "return p / m",
      "must_fail": "test_the_merge_convention_is_not_p_over_m",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M108-y-basis-measurement-loses-its-z-component",
      "why": "MY treated as MX: half of a Y-basis record disappears",
      "find": "            if basis in (\"Z\", \"Y\"):",
      "replace": "            if basis in (\"Z\",):",
      "must_fail": "test_each_unexercised_gate_agrees_with_the_injection_oracle",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M109-unknown-instruction-skipped-not-refused",
      "why": "an unrecognised gate silently ignored, shrinking |F|",
      "find": "        raise Refused(_why_refused(name))",
      "replace": "        pass",
      "must_fail": "test_an_unknown_instruction_is_refused_rather_than_ignored",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M110-fault-order-and-signature-order-desync",
      "why": "faults reversed but signatures not: every leaf names the wrong fault",
      "find": "    faults.reverse()\n    sigs.reverse()",
      "replace": "    faults.reverse()",
      "must_fail": "test_every_signature_matches_injected_ground_truth",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M111-every-leaf-hashes-to-a-constant",
      "why": "the Merkle leaf ignoring its content: one root for every input",
      "find": "        stack.append((0, leaf_hash(leaf)))",
      "replace": "        stack.append((0, sha256_hex(_NODE)))",
      "must_fail": "test_the_root_changes_when_any_leaf_changes",
      "target": "dem_cert",
      "outcome": "killed"
    },
    {
      "id": "M112-sdk-evidence-admitted-as-hardware",
      "why": "the source filter removed: documentation admits plans",
      "find": "if v[\"source\"] == HARDWARE}",
      "replace": "if v[\"source\"] in (HARDWARE, SDK)}",
      "must_fail": "test_an_sdk_sourced_field_can_never_be_admitted",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M113-capability-manifest-never-expires",
      "why": "a capability measured in March admitted in September",
      "find": "if now > manifest[\"probe_expiry\"]:",
      "replace": "if False:",
      "must_fail": "test_an_expired_manifest_is_refused_like_a_stale_calibration",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M114-a-floor-compared-as-a-ceiling",
      "why": "at_least inverted: an unusable backend admitted silently",
      "find": "    if direction == \"at_least\":\n        return (have >= want,",
      "replace": "    if direction == \"at_least\":\n        return (have <= want,",
      "must_fail": "test_requirement_directions_are_not_reversible",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M115-a-ceiling-compared-as-a-floor",
      "why": "at_most inverted: a slow backend admitted for a latency-bound plan",
      "find": "    if direction == \"at_most\":\n        return (have <= want,",
      "replace": "    if direction == \"at_most\":\n        return (have >= want,",
      "must_fail": "test_requirement_directions_are_not_reversible",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M116-a-value-may-carry-no-probe",
      "why": "a manifest entry with no probe behind it accepted as measured",
      "find": "if not isinstance(entry[\"probe\"], str) or not entry[\"probe\"]:",
      "replace": "if False:",
      "must_fail": "test_a_value_with_no_probe_named_is_malformed",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M117-the-deleted-boolean-returns",
      "why": "supports_dynamic_control_flow readmitted as an ignored key",
      "find": "for banned, why in BANNED.items():",
      "replace": "for banned, why in {}.items():",
      "must_fail": "test_the_deleted_boolean_cannot_come_back",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M118-a-hardware-claim-needs-no-job",
      "why": "hardware_executable accepted without a job id: a TYPED manifest",
      "find": "if entry[\"source\"] == HARDWARE and not entry.get(\"job_id\"):",
      "replace": "if False:",
      "must_fail": "test_a_hardware_claim_that_cannot_name_its_job_is_malformed",
      "target": "capability",
      "outcome": "killed"
    },
    {
      "id": "M119-offline-probe-labels-itself-as-hardware",
      "why": "the free SDK run claiming hardware_executable for every field",
      "find": "\"source\": SDK,\n                                   \"probe\"",
      "replace": "\"source\": HARDWARE,\n                                   \"probe\"",
      "must_fail": "test_the_offline_manifest_ACCEPTS_NOTHING",
      "target": "capability_probe",
      "outcome": "killed"
    },
    {
      "id": "M150-dem-floor-never-caps",
      "why": "a receipt certified PROVEN against a model nobody graded",
      "find": "    if dem_level >= min_dem_level:\n        return frontier",
      "replace": "    if True:\n        return frontier",
      "must_fail": "test_a_DEM_below_A3_caps_the_receipt_at_REPLAY",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M151-passport-block-admits-an-overclaim",
      "why": "a block issued for a claim the frontier does not support",
      "find": "    if supported is None or claimed_level > supported:",
      "replace": "    if False:",
      "must_fail": "test_a_DEM_below_A3_caps_the_receipt_at_REPLAY",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M152-minting-trusts-the-producers-block",
      "why": "build_core accepting a hand-built block that overclaims",
      "find": "        if int(claimed_level) > supported:",
      "replace": "        if False:",
      "must_fail": "test_the_minting_path_REFUSES_an_overclaim_even_from_a_handbuilt_block",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M153-two-claims-may-disagree",
      "why": "the block and the passport allowed to claim different levels",
      "find": "        if claimed_in_block != int(claimed_level):",
      "replace": "        if False:",
      "must_fail": "test_two_numbers_for_one_claim_are_refused",
      "target": "passport",
      "outcome": "killed"
    },
    {
      "id": "M154-unknown-tier-defaults-instead-of-refusing",
      "why": "an unrecognised producer tier silently graded",
      "find": "    if tier not in TIER_EVIDENCE:",
      "replace": "    if False:",
      "must_fail": "test_an_unrecognised_tier_is_REFUSED_not_defaulted",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M155-missing-robustness-margin-treated-as-present",
      "why": "a float dual with no eps_q < rho answer graded PROVEN anyway",
      "find": "        if rho_exceeds_quantisation is None:",
      "replace": "        if False:",
      "must_fail": "test_a_float_dual_with_NO_ANSWER_about_the_margin_is_refused",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M156-only-the-first-leg-composes",
      "why": "every leg after the first dropped from the frontier",
      "find": "    for leg in legs[1:]:\n        out = out.alt(Frontier.leg(leg.level, leg.beta_ppb))",
      "replace": "    for leg in []:\n        out = out.alt(Frontier.leg(leg.level, leg.beta_ppb))",
      "must_fail": "test_the_legs_are_composed_by_FRECHET_and_the_block_says_so",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M157-replay-tier-still-demands-a-margin",
      "why": "a tier that is not an optimality proof refused over eps_q < rho",
      "find": "    if level is not L.PROVEN:",
      "replace": "    if False:",
      "must_fail": "test_a_tier_that_is_not_an_optimality_proof_is_not_asked_at_all",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M158-derivation-returns-the-wrong-arithmetic",
      "why": "the derived arithmetic wrong, so a proven tier is refused",
      "find": "        exact_arithmetic = known",
      "replace": "        exact_arithmetic = False",
      "must_fail": "test_the_arithmetic_is_DERIVED_not_asked_of_the_caller",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M159-caller-may-assert-exact-over-a-float-tier",
      "why": "a float-checked tier graded on the caller's word that it is exact",
      "find": "    elif exact_arithmetic and known is False:",
      "replace": "    elif False:",
      "must_fail": "test_the_overclaim_branch_names_what_would_make_it_live",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M160-admission-certificate-believed-not-re-derived",
      "why": "a DEM certificate's own headline trusted over its rungs",
      "find": "        if stated is not None and int(stated) != int(level):",
      "replace": "        if False:",
      "must_fail": "test_a_certificate_that_contradicts_its_own_rungs_is_REFUSED",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M161-empty-ladder-grades-a-model",
      "why": "a certificate with no rungs accepted as an admission",
      "find": "        if not rungs:",
      "replace": "        if False:",
      "must_fail": "test_an_empty_ladder_cannot_grade_a_model",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M162-unverified-rungs-reported-as-verified",
      "why": "a re-derivation from unopened artifacts labelled verified",
      "find": "        if dem_level.get(\"evidence_verified\"):",
      "replace": "        if True:",
      "must_fail": "test_UNVERIFIED_rungs_are_named_differently_from_verified_ones",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M163-floor-computed-from-the-callers-number",
      "why": "the DEM cap taken from the asserted level, not the re-derived one",
      "find": "passport_block(dem_floor(compose(legs), dem_int),",
      "replace": "passport_block(dem_floor(compose(legs), 5),",
      "must_fail": "test_a_re_derived_LOW_grade_makes_the_floor_bite",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M144-frontier-monotonicity-unchecked",
      "why": "a frontier that TIGHTENS with grade accepted as well-formed",
      "find": "            if hi < lo:",
      "replace": "            if False:",
      "must_fail": "test_a_frontier_that_TIGHTENS_with_grade_is_malformed",
      "target": "evidence_frontier",
      "outcome": "killed"
    },
    {
      "id": "M145-invalid-leg-still-contributes",
      "why": "an INVALID leg donating a bound instead of contributing nothing",
      "find": "        if not valid:\n            return cls.nothing()",
      "replace": "        if False:\n            return cls.nothing()",
      "must_fail": "test_an_INVALID_leg_contributes_nothing_anywhere",
      "target": "evidence_frontier",
      "outcome": "killed"
    },
    {
      "id": "M146-leg-supports-above-its-own-grade",
      "why": "a leg lending its bound to grades it cannot be checked at",
      "find": "        return cls(tuple(beta_ppb if lv <= level else PPB for lv in LEVELS))",
      "replace": "        return cls(tuple(beta_ppb for lv in LEVELS))",
      "must_fail": "test_a_leg_supports_NOTHING_above_its_own_grade",
      "target": "evidence_frontier",
      "outcome": "killed"
    },
    {
      "id": "M147-composed-frontier-discarded",
      "why": "a composed frontier thrown away and re-synthesised from scalars",
      "find": "        if self.composed is not None:\n            return self.composed",
      "replace": "        if False:\n            return self.composed",
      "must_fail": "test_the_composed_frontier_survives_a_CHAIN",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M148-mask-check-may-silently-not-run",
      "why": "a requested mask-replica check skipped for want of masks",
      "find": "    if check_mask_replica and masks_read is None:",
      "replace": "    if False:",
      "must_fail": "test_a_REQUESTED_mask_check_that_cannot_run_is_a_REFUSAL",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M149-accumulator-bound-to-any-table",
      "why": "a commit accepted against a table the accumulator never read",
      "find": "    if acc.table is not table:",
      "replace": "    if False:",
      "must_fail": "test_an_accumulator_built_against_a_DIFFERENT_table_is_refused",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M120-or-multiplies-instead-of-frechet",
      "why": "OR takes the naive product: redundancy becomes free",
      "find": "        return Frontier(tuple(min(a, b)\n                              for a, b in zip(self.beta, other.beta)))",
      "replace": "        return Frontier(tuple(min(PPB, (a * b) // PPB)\n                              for a, b in zip(self.beta, other.beta)))",
      "must_fail": "test_redundant_decoder_checks_earn_NOTHING_over_the_better_one",
      "target": "evidence_frontier",
      "outcome": "killed"
    },
    {
      "id": "M121-partial-independence-witness-accepted",
      "why": "a witness covering part of the shared ancestry silently believed",
      "find": "    elif witness is not None and shared:",
      "replace": "    elif False:",
      "must_fail": "test_a_PARTIAL_witness_is_refused_rather_than_pro_rated",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M122-a-witness-may-raise-a-bound",
      "why": "a wrong witness allowed to report a WEAKER bound than Frechet",
      "find": "            out.append(min(frechet, candidate))",
      "replace": "            out.append(candidate)",
      "must_fail": "test_a_witness_may_LOWER_a_bound_and_never_RAISE_it",
      "target": "evidence_frontier",
      "outcome": "killed"
    },
    {
      "id": "M123-domain-intersection-ignored-on-and",
      "why": "a code-capacity result chained onto a circuit-level one",
      "find": "    if a.noise_class != b.noise_class:\n        raise Unsound(\n            f\"cannot chain",
      "replace": "    if False:\n        raise Unsound(\n            f\"cannot chain",
      "must_fail": "test_composing_across_noise_classes_is_refused",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M124-blind-leg-not-capped",
      "why": "a check with no construction_witness presented as PROVEN",
      "find": "        if self.construction_witness:\n            return self.level\n        return min(self.level, L.REPLAY)",
      "replace": "        return self.level",
      "must_fail": "test_a_leg_with_no_construction_witness_is_CAPPED_not_dropped",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M125-rule-of-three-floor-dropped",
      "why": "a measured leg claiming below 3/n with zero misses",
      "find": "        return max(self.beta_ppb, self.floor_ppb)",
      "replace": "        return self.beta_ppb",
      "must_fail": "test_a_measured_leg_carries_the_rule_of_three_floor",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M126-bridge-rounds-down",
      "why": "the bridge truncating instead of rounding up",
      "find": "    return min(PPB, -(-num // p_a_ppb))",
      "replace": "    return min(PPB, num // p_a_ppb)",
      "must_fail": "test_the_bridge_rounds_UP_because_a_bound_that_rounds_down_is_not_one",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M127-beta-accepts-a-float",
      "why": "a float admitted into a composed bound",
      "find": "        if not isinstance(self.beta_ppb, int) or isinstance(self.beta_ppb, bool):",
      "replace": "        if False:",
      "must_fail": "test_beta_must_be_an_integer_count_of_parts_per_billion",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M128-ladder-takes-the-max-not-the-run",
      "why": "a gap stepped over: A0+A3 reported as A3",
      "find": "    level = 0\n    while level + 1 <= MAX_LEVEL and (level + 1) in have:\n        level += 1",
      "replace": "    level = max(have)",
      "must_fail": "test_a_gap_CAPS_the_ladder_rather_than_being_stepped_over",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M129-graphlike-cap-removed",
      "why": "a graphlike decomposition admitted at A3 with no certified error",
      "find": "    if decomposition == \"graphlike\" and decomposition_error_log10_micro is None:",
      "replace": "    if False:",
      "must_fail": "test_graphlike_is_capped_at_A2_without_a_certified_error",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M130-cap-allowed-to-raise-a-level",
      "why": "the decomposition cap rounding UP to its own ceiling",
      "find": "        capped = min(level, 2)",
      "replace": "        capped = 2",
      "must_fail": "test_the_cap_never_RAISES_a_level",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M131-a-rung-may-name-no-artifact",
      "why": "a level claimed with nothing a stranger could open",
      "find": "        if not self.artifact:",
      "replace": "        if False:",
      "must_fail": "test_a_rung_with_no_artifact_is_refused",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M132-unparsed-dem-still-graded",
      "why": "a DEM that has not been shown to parse graded anyway",
      "find": "    if 0 not in have:",
      "replace": "    if False:",
      "must_fail": "test_a_dem_that_does_not_even_parse_admits_nothing",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M133-unknown-decomposition-passed-through",
      "why": "an unrecognised decomposition neither capped nor refused",
      "find": "    elif decomposition not in (\"undecomposed\", \"graphlike\"):",
      "replace": "    elif False:",
      "must_fail": "test_an_unrecognised_decomposition_is_refused_not_passed_through",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M140-device-fit-never-expires",
      "why": "a held-out fit taken in March still admitting in September",
      "find": "    stale = device_rungs_expired(rungs, now)\n    if stale and level > CIRCUIT_LEVEL_CEILING:",
      "replace": "    stale = device_rungs_expired(rungs, now)\n    if False:",
      "must_fail": "test_an_expired_device_rung_caps_the_ladder_at_the_circuit_ceiling",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M141-circuit-rungs-made-to-expire",
      "why": "an exhaustive fault-signature proof discarded because a date passed",
      "find": "        if r.level > CIRCUIT_LEVEL_CEILING and r.expires_on",
      "replace": "        if r.expires_on",
      "must_fail": "test_an_expiry_on_a_CIRCUIT_level_rung_is_inert",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M142-expiry-comparison-inverted",
      "why": "the date comparison reversed: in-date rungs expire and stale ones do not",
      "find": "                  and now > r.expires_on)",
      "replace": "                  and now < r.expires_on)",
      "must_fail": "test_a_device_rung_still_in_date_admits_normally",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M143-missing-clock-treated-as-in-date",
      "why": "a caller who supplied no clock silently given a pass",
      "find": "    if now is None:\n        return []",
      "replace": "    if True:\n        return []",
      "must_fail": "test_an_expired_device_rung_caps_the_ladder_at_the_circuit_ceiling",
      "target": "dem_admission",
      "outcome": "killed"
    },
    {
      "id": "M134-mask-dropped-from-the-edge-tag",
      "why": "the observable mask left out of H(e || w || mask || endpoints)",
      "find": "body = (f\"{self.eid}|{self.weight}|{self.mask}|\"",
      "replace": "body = (f\"{self.eid}|{self.weight}|\"",
      "must_fail": "test_the_TAG_ALONE_covers_the_mask",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M135-mask-replica-never-consulted",
      "why": "the dedicated replica check skipped: the P1 class goes dark",
      "find": "    if check_mask_replica and masks_read is not None:",
      "replace": "    if False:",
      "must_fail": "test_WITHOUT_the_mask_replica_the_check_is_vacuous_on_masks",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M136-empty-active-set-passes",
      "why": "a decode that read no edge committing successfully",
      "find": "    if not acc.touched:",
      "replace": "    if False:",
      "must_fail": "test_reading_no_edge_at_all_is_a_REFUSAL_not_a_pass",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M137-unprovisioned-edge-silently-skipped",
      "why": "an edge outside the provisioned table dropped from the reference",
      "find": "        if ref is None or edge.tag() != ref:",
      "replace": "        if ref is not None and edge.tag() != ref:",
      "must_fail": "test_an_edge_outside_the_provisioned_table_is_refused",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M138-detector-never-fires",
      "why": "the sticky fault bit pinned False: every corruption commits",
      "find": "            self.fault_seen = True",
      "replace": "            self.fault_seen = False",
      "must_fail": "test_EVERY_single_bit_flip_on_the_active_subset_is_caught",
      "target": "abft_l0",
      "outcome": "killed"
    },
    {
      "id": "M164-search-charged-for-disagreement-not-causation",
      "why": "delta_S counts A != E, so a heuristic that got lucky is billed",
      "find": "        return {\"delta_S\": self.loss_a - self.loss_e,",
      "replace": "        return {\"delta_S\": int(self.a != self.e),",
      "must_fail": "test_search_is_NOT_charged_for_being_accidentally_right",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M165-irreducible-risk-reads-q-not-min",
      "why": "R_I = q instead of min(q, 1-q): confidence read as ambiguity",
      "find": "        return min(self.q, 1 - self.q)",
      "replace": "        return self.q",
      "must_fail": "test_R_I_is_min_q_1_minus_q_and_is_derived_not_accepted",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M166-telescoping-identity-never-checked",
      "why": "assert_identity cannot raise: terms from different shots pass",
      "find": "    if total != shot.loss_a:",
      "replace": "    if False:",
      "must_fail": "test_terms_that_came_from_DIFFERENT_shots_are_caught_by_the_sum",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M167-certain-and-wrong-not-counted",
      "why": "a zero-risk shot that was WRONG stops being a refutation",
      "find": "            if s.loss_b:",
      "replace": "            if False:",
      "must_fail": "test_a_model_that_called_it_impossible_and_was_wrong_is_REFUTED",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M168-zero-variance-shots-enter-the-statistic",
      "why": "a certain shot is counted as a Z_M observation",
      "find": "            continue                 # zero variance: not a Z observation",
      "replace": "            pass",
      "must_fail": "test_certain_shots_do_not_DESTROY_Z_M_for_everything_else",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M169-cube-collapses-the-true-class",
      "why": "the cube drops Y: 8 cells reported as the contingency cube",
      "find": "    for a in (0, 1) for e in (0, 1) for b in (0, 1) for y in (0, 1))",
      "replace": "    for a in (0, 1) for e in (0, 1) for b in (0, 1) for y in (0,))",
      "must_fail": "test_all_sixteen_cells_are_present_even_at_zero",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M172-a-refutation-is-ranked-as-if-it-were-a-Z",
      "why": "worst_slice stops promoting an outright refutation",
      "find": "    if refuted:",
      "replace": "    if False:",
      "must_fail": "test_worst_slice_ranks_an_OUTRIGHT_REFUTATION_above_any_Z",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M173-every-shot-lands-in-one-slice",
      "why": "by() ignores the key: conditioning silently becomes global",
      "find": "        groups.setdefault(key(s), []).append(s)",
      "replace": "        groups.setdefault(None, []).append(s)",
      "must_fail": "test_a_LOCAL_fault_is_louder_in_its_slice_than_globally",
      "target": "error_budget",
      "outcome": "killed"
    },
    {
      "id": "M202-r-B-bracketed-at-its-endpoints",
      "why": "the non-monotone peak at q=1/2 is missed",
      "find": "    top = HALF if q_lo <= HALF <= q_hi else max(ends)",
      "replace": "    top = max(ends)",
      "must_fail": "test_r_B_over_a_STRADDLING_interval_peaks_in_the_INTERIOR",
      "target": "enclosure_budget",
      "outcome": "killed"
    },
    {
      "id": "M203-delta-M-bracket-does-not-cross",
      "why": "the delta_M interval is narrowed by pairing ends the same way",
      "find": "        out.delta_M_lo += l_b - r_hi\n        out.delta_M_hi += l_b - r_lo",
      "replace": "        out.delta_M_lo += l_b - r_lo\n        out.delta_M_hi += l_b - r_hi",
      "must_fail": "test_delta_M_pairs_its_LOW_end_with_r_B_s_HIGH_end",
      "target": "enclosure_budget",
      "outcome": "killed"
    },
    {
      "id": "M204-an-undecidable-shot-is-budgeted-anyway",
      "why": "a straddling enclosure is treated as decided",
      "find": "        if b is None:\n            out.undecided += 1\n            continue",
      "replace": "        if b is None:\n            b = 0",
      "must_fail": "test_an_UNDECIDED_shot_is_counted_and_EXCLUDED",
      "target": "enclosure_budget",
      "outcome": "killed"
    },
    {
      "id": "M200-compose-never-routes-through-the-algebra",
      "why": "the minting path composes without the domain refusal",
      "find": "    if len(legs) > 1 and all(d is not None for d in declared):",
      "replace": "    if False:",
      "must_fail": "test_a_CROSS_DOMAIN_or_is_REFUSED_on_the_minting_path",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M201-the-OR-accepts-two-different-domains",
      "why": "alt() stops refusing a disjunction across noise classes",
      "find": "    if a.noise_class != b.noise_class:\n        raise Unsound(\n            f\"cannot OR",
      "replace": "    if False:\n        raise Unsound(\n            f\"cannot OR",
      "must_fail": "test_a_CROSS_DOMAIN_or_is_REFUSED_on_the_minting_path",
      "target": "evidence_algebra",
      "outcome": "killed"
    },
    {
      "id": "M198-the-l0-guard-is-never-invoked",
      "why": "a supplied L0 table is accepted and never checked",
      "find": "        if l0_table is not None:\n            self._l0_verify(edges, l0_table, l0_masks)",
      "replace": "        if False:\n            self._l0_verify(edges, l0_table, l0_masks)",
      "must_fail": "test_a_flipped_OBSERVABLE_MASK_refuses_the_decoder",
      "target": "certifying_decoder",
      "outcome": "killed"
    },
    {
      "id": "M199-the-l0-commit-verdict-is-ignored",
      "why": "commit_check runs and its refusal is dropped",
      "find": "        if not ok:\n            raise ConfigurationFault(",
      "replace": "        if False:\n            raise ConfigurationFault(",
      "must_fail": "test_a_changed_WEIGHT_refuses_the_decoder",
      "target": "certifying_decoder",
      "outcome": "killed"
    },
    {
      "id": "M181-a-grade-for-another-model-is-accepted",
      "why": "dem_binding stops checking WHICH model the certificate graded",
      "find": "        if str(decoded_against) not in covers:",
      "replace": "        if False:",
      "must_fail": "test_a_grade_earned_by_a_DIFFERENT_model_is_REFUSED",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M182-an-unnamed-dem-reads-as-bound",
      "why": "the no-root case is reported as a binding",
      "find": "    return (\"certificate_unnamed_dem\",\n            \"the certificate records no `dem_root`, so WHICH model it \"",
      "replace": "    return (\"certificate_bound\",\n            \"the certificate records no `dem_root`, so WHICH model it \"",
      "must_fail": "test_a_certificate_with_NO_root_cannot_be_bound_to_anything",
      "target": "soundness_budget",
      "outcome": "killed"
    },
    {
      "id": "M183-the-published-admission-names-no-model",
      "why": "dem_cert_run stops recording which models it graded",
      "find": "    return ([c[\"fault_root\"] for c in certs],",
      "replace": "    return (None,",
      "must_fail": "test_the_PRODUCER_of_the_identity_is_what_the_test_calls",
      "target": "dem_cert_run",
      "outcome": "killed"
    },
    {
      "id": "M179-sigma-keeps-the-log-likelihood-scale",
      "why": "normalised() stops dividing by the mean",
      "find": "    m = sum(vals) / len(vals)",
      "replace": "    m = 1.0",
      "must_fail": "test_normalised_weights_have_mean_one",
      "target": "certificate_robustness_corpus",
      "outcome": "killed"
    },
    {
      "id": "M180-ties-are-scored-as-zero-radius-certificates",
      "why": "a tie between the two cosets is treated as a proof",
      "find": "        if w0 == w1:\n            ties += 1\n            continue",
      "replace": "        if False:\n            ties += 1\n            continue",
      "must_fail": "test_the_corpus_run_produces_radii_and_no_flip_at_the_measured_sigma",
      "target": "certificate_robustness_corpus",
      "outcome": "killed"
    },
    {
      "id": "M177-the-logical-cut-names-the-other-boundary",
      "why": "logical_cut_edges returns the opposite end from truth_column",
      "find": "    return frozenset(_lc_key(t * (d - 1) + (d - 2), BOUNDARY)\n                     for t in range(r + 1))",
      "replace": "    return frozenset(_lc_key(t * (d - 1), BOUNDARY)\n                     for t in range(r + 1))",
      "must_fail": "test_an_error_on_the_LAST_data_qubit_flips_the_declared_cut",
      "target": "rep_code_cert",
      "outcome": "killed"
    },
    {
      "id": "M178-delta-S-reported-zero-for-refused-shots",
      "why": "an unknown E is counted as a certified zero",
      "find": "            if res.accepted:\n                obs_stats[\"delta_S_certified_zero\"] += 1\n            else:\n                obs_stats[\"delta_S_unknown\"] += 1",
      "replace": "            obs_stats[\"delta_S_certified_zero\"] += 1",
      "must_fail": "test_delta_S_is_UNDETERMINED_when_any_scored_shot_was_refused",
      "target": "rep_code_cert",
      "outcome": "killed"
    },
    {
      "id": "M174-a-clean-cell-can-never-be-found-guilty",
      "why": "check_localization stops testing the configurations it did not plant",
      "find": "        elif abs(share[\"delta_M\"]) > tol[\"delta_M\"]:",
      "replace": "        elif False:",
      "must_fail": "test_the_check_FAILS_when_the_fault_is_not_where_it_is_CLAIMED",
      "target": "error_budget_ledger",
      "outcome": "killed"
    },
    {
      "id": "M175-a-model-fault-may-be-billed-to-the-decoder",
      "why": "the mismatched cell stops checking that delta_S and delta_O stay put",
      "find": "            for k in (\"delta_S\", \"delta_O\"):",
      "replace": "            for k in ():",
      "must_fail": "test_the_CHECK_objects_when_a_model_fault_reaches_delta_S",
      "target": "error_budget_ledger",
      "outcome": "killed"
    },
    {
      "id": "M176-the-bayes-tier-is-told-the-true-rate",
      "why": "coset masses computed under p_true instead of the declared p_model",
      "find": "        z0, z1, st = coset_masses(checks, [p_model] * n_vars, syn_bits,",
      "replace": "        z0, z1, st = coset_masses(checks, [p_true] * n_vars, syn_bits,",
      "must_fail": "test_a_planted_MODEL_fault_lands_in_delta_M_in_the_right_cell",
      "target": "error_budget_ledger",
      "outcome": "killed"
    },
    {
      "id": "M170-forgery-arm-stops-being-a-forgery",
      "why": "the mixed y+z object loses its node potentials",
      "find": "        node_potentials={0: 9.0, 1: 9.0, 2: -10.0},",
      "replace": "        node_potentials={},",
      "must_fail": "test_the_forgery_is_refused_BY_NAME_not_incidentally",
      "target": "dual_forgery",
      "outcome": "killed"
    },
    {
      "id": "M171-positive-control-stops-proving-optimality",
      "why": "the accepted control no longer has to attain the primal",
      "find": "    tight = (got.accepted and got.primal == Fraction(2)",
      "replace": "    tight = (got.accepted and got.primal == Fraction(20)",
      "must_fail": "test_the_challenge_can_ACCEPT_so_a_stuck_checker_cannot_pass",
      "target": "dual_forgery",
      "outcome": "killed"
    },
    {
      "id": "M184-enclosure-discard-is-free",
      "why": "discarded states are dropped without being priced",
      "find": "                if r:\n                    slack += val * r",
      "replace": "                if False:\n                    slack += val * r",
      "must_fail": "test_the_bracket_contains_the_truth_on_random_models",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M185-enclosure-residual-demand-ignored",
      "why": "every discard priced as if it owed nothing",
      "find": "        k = demand(cov, bin(dem).count(\"1\"))",
      "replace": "        k = 0",
      "must_fail": "test_the_checker_reproduces_the_producer_through_the_wire",
      "target": "coset_enclosure_check",
      "outcome": "killed"
    },
    {
      "id": "M186-enclosure-parity-family-not-disjoint",
      "why": "odd-parity factors multiplied over overlapping mechanism sets",
      "find": "    C = _independent(demand, adj) if demand else []",
      "replace": "    C = [i for i in range(len(adj)) if demand >> i & 1]",
      "must_fail": "test_the_bracket_contains_the_truth_on_random_models",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M187-enclosure-closure-stops-enforcing",
      "why": "a closing detector no longer has to match the syndrome",
      "find": "                if (dmask & close) != want:",
      "replace": "                if False:",
      "must_fail": "test_the_bracket_contains_the_truth_on_random_models",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M188-enclosure-untouched-detector-ignored",
      "why": "an unreachable syndrome bit is no longer infeasible",
      "find": "        if c not in last and syn[c]:",
      "replace": "        if False:",
      "must_fail": "test_an_untouched_detector_demanding_a_flip_is_INFEASIBLE",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M189-enclosure-checker-stops-comparing",
      "why": "the checker endorses a winner without beating the rivals",
      "find": "        if all(c == best or LO[best] > LO[c] + slack for c in cells):",
      "replace": "        if True:",
      "must_fail": "test_the_checker_reproduces_the_producer_through_the_wire",
      "target": "coset_enclosure_check",
      "outcome": "killed"
    },
    {
      "id": "M190-enclosure-tie-becomes-a-decision",
      "why": "a straddling bracket accepted on equality",
      "find": "            if c != best and not LO[best] > HI[c]:",
      "replace": "            if c != best and not LO[best] >= HI[c]:",
      "must_fail": "test_a_tie_is_an_AMBIGUITY_not_a_decision",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M191-enclosure-forgery-stops-being-false",
      "why": "the digest-break forgery no longer edits the model",
      "find": "        d[\"mechanisms\"][0][\"p\"] = \"1/2\"",
      "replace": "        pass",
      "must_fail": "test_every_forgery_is_refused_and_the_battery_says_which",
      "target": "enclosure_forgery",
      "outcome": "killed"
    },
    {
      "id": "M192-selection-bound-sums-confidence",
      "why": "the batch bound sums p_lo instead of its shortfall",
      "find": "        return sum((_up(1 - s.p_lo) for s in self.accepted), Fraction(0))",
      "replace": "        return sum((_up(s.p_lo) for s in self.accepted), Fraction(0))",
      "must_fail": "test_S1_sums_the_shortfalls_and_rounds_OUTWARD",
      "target": "certified_selection",
      "outcome": "killed"
    },
    {
      "id": "M193-selection-ignores-the-admission-floor",
      "why": "a bound reported for a model below the DEM floor",
      "find": "    if dem_level < min_dem_level:",
      "replace": "    if False:",
      "must_fail": "test_a_model_below_the_admission_floor_is_REFUSED",
      "target": "certified_selection",
      "outcome": "killed"
    },
    {
      "id": "M194-selection-scores-the-refusals",
      "why": "shots the enclosure refused are counted as accepted",
      "find": "        (sel.accepted if s.p_lo >= t else sel.rejected).append(s)",
      "replace": "        sel.accepted.append(s)",
      "must_fail": "test_a_threshold_moves_the_bound_and_the_discard_rate_together",
      "target": "certified_selection",
      "outcome": "killed"
    },
    {
      "id": "M195-selection-accepts-a-float-posterior",
      "why": "a float p_lo admitted instead of an exact Fraction",
      "find": "        if not isinstance(self.p_lo, Fraction):",
      "replace": "        if False:",
      "must_fail": "test_a_float_or_out_of_range_posterior_is_REFUSED",
      "target": "certified_selection",
      "outcome": "killed"
    },
    {
      "id": "M196-enclosure-one-cell-contract-decides",
      "why": "a single-cell contract accepted instead of refused",
      "find": "    if len(cells) < 2:",
      "replace": "    if False:",
      "must_fail": "test_a_ONE_CELL_contract_is_REFUSED_because_it_decides_nothing",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M197-enclosure-work-guard-removed",
      "why": "an unbounded model accepted instead of refused",
      "find": "    if len(mechs) > MAX_MECHANISMS:",
      "replace": "    if False:",
      "must_fail": "test_an_UNTRUSTED_document_cannot_ask_for_a_gigabyte",
      "target": "coset_enclosure",
      "outcome": "killed"
    },
    {
      "id": "M290-a-prerequisite-outside-the-ledger-stops-being-refused",
      "why": "closure is dropped, so a gate may name work that has no row and therefore no cost -- the exact shape of the defect this field was added for, reintroduced one level up",
      "find": "            if req not in index:",
      "replace": "            if req in index and False:",
      "must_fail": "test_a_prerequisite_that_is_not_a_row_is_REFUSED",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M291-a-prerequisite-cycle-loops-instead-of-refusing",
      "why": "A-needs-B-needs-A recurses until the interpreter stops it, instead of naming the cycle -- a ledger that prices nothing while appearing to compute",
      "find": "            if req in stack:",
      "replace": "            if False:",
      "must_fail": "test_a_prerequisite_cycle_is_REFUSED_not_looped",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M292-finished-work-is-charged-forever",
      "why": "the CLOSED filter is dropped, so the ready cost bills every prerequisite ever completed and overstates the remaining board as badly as the original omission understated it",
      "find": "            if done.get(req) != \"CLOSED\" and req not in seen:",
      "replace": "            if req not in seen:",
      "must_fail": "test_a_CLOSED_prerequisite_drops_out_of_the_ready_cost",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M293-the-ready-cost-stops-composing",
      "why": "every gate reports its ISOLATED price again, so `E1` reads 'minutes' with an engineer-week in front of it",
      "find": "    if not chain:",
      "replace": "    if True:",
      "must_fail": "test_E1_cannot_be_priced_without_E0",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M294-the-document-check-inspects-no-rows",
      "why": "every row is skipped, so the prerequisite check returns no findings whether the document names its preconditions or not",
      "find": "        if not r[\"unmet_prerequisites\"]:",
      "replace": "        if True:",
      "must_fail": "test_the_document_check_FAILS_when_the_row_hides_its_prerequisite",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M295-the-rendered-table-prints-the-isolated-cost",
      "why": "the ledger a reader actually reads reverts to the number that hides the prerequisite, while the JSON stays correct -- drift between the artifact and the document it is supposed to fix",
      "find": "            cost = r[\"ready_cost\"]",
      "replace": "            cost = r[\"cost\"]",
      "must_fail": "test_the_rendered_row_names_the_open_prerequisite",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M300-the-rms-to-probability-conversion-loses-its-factor",
      "why": "eps^2 = 4p becomes eps^2 = p, so the floor is quadrupled and the comparison drifts back toward the units error that made the model look unphysical at every photon number",
      "find": "    return eps * eps / 4.0",
      "replace": "    return eps * eps",
      "must_fail": "test_the_units_conversion_is_not_cosmetic",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M301-n0-enters-the-group-linearly",
      "why": "the square root is dropped from k = C*SNR_0/sqrt(n_0), so two devices the reduction says are identical stop agreeing and the one-group claim silently becomes false",
      "find": "    return abs(commutator) * snr_0 / math.sqrt(n_0)",
      "replace": "    return abs(commutator) * snr_0 / n_0",
      "must_fail": "test_n_0_enters_only_through_the_square_root",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M302-a-commuting-observable-is-floored-at-zero",
      "why": "the QND refusal is removed, so [sigma_z, N] = 0 yields a WAY floor of 0.0 -- a bound no measurement can violate, sitting under a gate whose job is to fail when the mechanism does not work",
      "find": "    if commutator == 0.0:\n        raise WayRefusal(\n            \"|<[A,N]>| = 0: the observable commutes with the conserved \"\n            \"quantity, so WAY prices NOTHING here (the QND dispersive \"\n            \"case). Refusing rather than returning a floor of 0\")",
      "replace": "    if False:\n        raise WayRefusal(\n            \"|<[A,N]>| = 0: the observable commutes with the conserved \"\n            \"quantity, so WAY prices NOTHING here (the QND dispersive \"\n            \"case). Refusing rather than returning a floor of 0\")",
      "must_fail": "test_a_COMMUTING_observable_is_REFUSED_not_floored_at_zero",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M303-a-window-is-reported-above-K_MAX",
      "why": "the no-window refusal is removed, so a device whose group exceeds K_MAX gets an interval anyway -- an unphysical model handed a validity domain",
      "find": "    if gap(MU_STAR) <= 0.0:",
      "replace": "    if False:",
      "must_fail": "test_a_group_above_K_MAX_REFUSES_rather_than_returning_an_interval",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M304-mu-star-solves-the-wrong-stationarity-condition",
      "why": "the factor of 2 in 2*Phi(-mu) = mu*phi(mu) is dropped, so mu* is no longer the maximiser of g and K_MAX -- the single number GA-11 reduces to -- is wrong for every device",
      "find": "        return 2.0 * _phi(-m) - m * _pdf(m)",
      "replace": "        return _phi(-m) - m * _pdf(m)",
      "must_fail": "test_mu_star_is_the_stationary_point_of_g",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M305-the-bisection-accepts-a-bracket-with-no-root",
      "why": "the sign-change guard is removed, so bisection returns a midpoint from a bracket containing no root and reports it as an edge of the physical window",
      "find": "    if (flo > 0) == (fhi > 0):",
      "replace": "    if False:",
      "must_fail": "test_a_bracket_with_no_sign_change_is_REFUSED",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M306-the-marginal-price-loses-its-sign",
      "why": "d p_W / d n_bar becomes positive, so the pricing rule says photons BUY error -- and the elasticity that is supposed to be exactly -1 becomes +1",
      "find": "    return -floor_probability(commutator, n_bar) / n_bar",
      "replace": "    return floor_probability(commutator, n_bar) / n_bar",
      "must_fail": "test_the_marginal_price_is_negative_and_matches_a_finite_difference",
      "target": "way",
      "outcome": "killed"
    },
    {
      "id": "M307-the-proxy-check-inspects-no-rows",
      "why": "every row is skipped, so a gate fed by a stand-in for its own subject can sit in the ledger with the criterion's experiment implied and never run",
      "find": "        if not r.get(\"proxy\"):",
      "replace": "        if True:",
      "must_fail": "test_the_proxy_check_FAILS_when_the_row_hides_it",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M308-a-proxy-row-passes-without-declaring-it",
      "why": "the row is inspected and the demand dropped, so a proxy declared in the registry never has to appear in the document anyone reads",
      "find": "                if \"PROXY\" not in line:",
      "replace": "                if False:",
      "must_fail": "test_the_proxy_check_FAILS_when_the_row_hides_it",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M309-a-changed-failure-count-reproduces",
      "why": "the per-arm comparison is dropped, so GA-3's recorded failure reproduces no matter what the arms now do",
      "find": "        if got != r[\"failures_total\"]:",
      "replace": "        if False:",
      "must_fail": "test_a_CHANGED_failure_count_is_caught_and_NAMED",
      "target": "ga3_voi_gate",
      "outcome": "killed"
    },
    {
      "id": "M310-a-flipped-verdict-reproduces",
      "why": "the headline comparison is dropped, so a FAILED gate whose verdict silently flips to passed still reports that it reproduces its recorded result",
      "find": "        if not same:",
      "replace": "        if False:",
      "must_fail": "test_a_FLIPPED_verdict_is_caught",
      "target": "ga3_voi_gate",
      "outcome": "killed"
    },
    {
      "id": "M311-a-vanished-headline-is-treated-as-agreement",
      "why": "a key absent from the fresh run stops being a difference, so deleting the number instead of matching it reads as a reproduction",
      "find": "        if b is None:",
      "replace": "        if False:",
      "must_fail": "test_a_MISSING_headline_key_is_caught_not_treated_as_equal",
      "target": "ga3_voi_gate",
      "outcome": "killed"
    },
    {
      "id": "M312-the-roadmap-position-pattern-matches-nothing",
      "why": "the asterisk tolerance is dropped, so the BOLDED spelling the README actually uses stops matching and the position claim goes unchecked exactly as it did for eight gates",
      "find": "    (re.compile(r\"\\b(\\d+)\\s+of\\s+(\\d+)\\*{0,2}\\s+roadmap\\s+gates\\b\", re.I),",
      "replace": "    (re.compile(r\"\\b(\\d+)\\s+of\\s+(\\d+)\\s+roadmap\\s+gates\\b\", re.I),",
      "must_fail": "test_the_roadmap_POSITION_is_checked_and_the_pattern_can_FIRE",
      "target": "doc_claims_gate",
      "outcome": "killed"
    },
    {
      "id": "M313-an-absent-roadmap-artifact-licenses-any-position",
      "why": "the shape guard is removed, so a roadmap.json missing its counts raises instead of reporting UNCHECKABLE -- or worse, a partial artifact supplies a position nothing derived",
      "find": "    if \"closed\" not in d or \"gates\" not in d:",
      "replace": "    if False:",
      "must_fail": "test_a_MALFORMED_roadmap_artifact_yields_UNCHECKABLE_not_a_position",
      "target": "doc_claims_gate",
      "outcome": "killed"
    },
    {
      "id": "M314-the-superseded-figure-exemption-loses-a-spelling",
      "why": "the `N of M` form is dropped from the exemption, so a correctly-qualified historical citation is flagged and the next restamp destroys the record to satisfy the gate",
      "find": "                            or re.search(rf\"\\b{tn}\\s+of\\s+{td}\\b\", line)):",
      "replace": "                            or False):",
      "must_fail": "test_the_superseded_figure_exemption_speaks_BOTH_spellings",
      "target": "doc_claims_gate",
      "outcome": "killed"
    },
    {
      "id": "M315-a-crash-is-announced-as-the-tools-own-verdict",
      "why": "the death check is dropped from the verdict branch, so an exit 1 carrying a traceback is reported as a documented refusal -- a fact about free RAM read as a soundness finding",
      "find": "    if not is_pytest and 0 < code < 126 and not looks_like_a_death(output):",
      "replace": "    if not is_pytest and 0 < code < 126:",
      "must_fail": "test_a_CRASH_that_exits_1_is_not_reported_as_a_verdict",
      "target": "gate_all",
      "outcome": "killed"
    },
    {
      "id": "M316-every-refusal-is-reclassified-as-a-death",
      "why": "`looks_like_a_death` returns True for any output, so a deliberate refusal with a legible message is reported as a crash and the reader is sent somewhere confidently wrong",
      "find": "    return any(mark in output for mark in _DEATH_MARKS)",
      "replace": "    return True",
      "must_fail": "test_the_SAME_exit_code_with_a_clean_refusal_is_still_a_VERDICT",
      "target": "gate_all",
      "outcome": "killed"
    },
    {
      "id": "M317-an-absent-output-is-treated-as-a-crash",
      "why": "the empty-output guard is removed, so `run()`'s streamed nodes -- which capture nothing -- would every one be classified from a None it cannot read",
      "find": "    if not output:",
      "replace": "    if False:",
      "must_fail": "test_the_death_marks_are_reachable_and_do_not_match_ordinary_prose",
      "target": "gate_all",
      "outcome": "killed"
    },
    {
      "id": "M318-a-cost-column-may-contradict-its-own-artifacts-clock",
      "why": "the ratio test is dropped, so a gate priced at 40 CPU-h whose artifact records 1.7 seconds passes unremarked and a reader takes the campaign to have been run",
      "find": "                    and declared / seconds > COST_RATIO_LIMIT",
      "replace": "                    and False",
      "must_fail": "test_a_cost_column_that_contradicts_the_artifacts_own_clock_is_CAUGHT",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M319-a-cost-naming-no-duration-is-given-one",
      "why": "`declared_seconds` stops returning None for a cost with no time unit, so rows claiming no duration are compared against a number nobody wrote",
      "find": "    if not m:\n        return None",
      "replace": "    if not m:\n        return 1.0",
      "must_fail": "test_a_cost_that_names_no_TIME_cannot_be_contradicted",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M320-a-boolean-is-read-as-a-runtime",
      "why": "the bool guard is removed from `recorded_seconds`, so a field like `passed: true` is read as 1.0 second and invents a contradiction with any hourly cost",
      "find": "        if k in _TIME_KEYS and isinstance(v, (int, float)) \\\n                and not isinstance(v, bool):",
      "replace": "        if k in _TIME_KEYS and isinstance(v, (int, float)):",
      "must_fail": "test_recorded_seconds_reads_nested_and_ignores_non_numbers",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M321-qubit-rounds-forgets-the-ancillas",
      "why": "physical qubits drops to d^2, halving every arm's bill and flattering whichever arm sits longest at large d -- which is the switching arm under test",
      "find": "    return 2 * d * d - 1",
      "replace": "    return d * d",
      "must_fail": "test_qubit_count_includes_the_ANCILLAS",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M322-the-seam-is-shortened-below-the-corpus-floor",
      "why": "the seam lasts min(d_lo,d_hi) rounds instead of max, so the hole in the protection closes sooner than the corpus says it can and switching looks affordable",
      "find": "    seam_rounds = hi * cost.seam_slack",
      "replace": "    seam_rounds = lo * cost.seam_slack",
      "must_fail": "test_the_seam_is_protected_at_the_SMALLER_distance",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M323-a-sub-floor-seam-slack-is-accepted",
      "why": "the seam_slack floor is removed, so a caller can price a switch with a seam shorter than the corpus's own minimum",
      "find": "        if self.seam_slack < 1.0:",
      "replace": "        if False:",
      "must_fail": "test_a_SEAM_SHORTER_than_the_corpus_floor_is_REFUSED",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M324-the-posterior-underflows-on-a-long-run",
      "why": "the log-sum shift is dropped, so a long run underflows to zero mass and the posterior becomes uniform exactly when the evidence is strongest",
      "find": "        w = [moved[j] * math.exp(lls[j] - top) for j in range(k)]",
      "replace": "        w = [moved[j] * math.exp(lls[j]) for j in range(k)]",
      "must_fail": "test_a_long_run_does_not_underflow_to_a_uniform_posterior",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M325-an-impossible-syndrome-record-steers-the-policy",
      "why": "more fires than detectors stops being refused, so a corrupted feed becomes evidence the trigger acts on",
      "find": "        if n < 0 or c < 0 or c > n:",
      "replace": "        if False:",
      "must_fail": "test_an_IMPOSSIBLE_observation_is_REFUSED",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M326-an-up-switch-needs-no-evidence-at-all",
      "why": "the economic gate on a monotone-SAFE up-switch is removed, so the uniform prior alone drives 3 -> 5 -> 7 in the first two rounds, paying seams for protection nothing justified",
      "find": "        return want if posterior[best] >= up_confidence else current",
      "replace": "        return want",
      "must_fail": "test_SAFE_is_not_the_same_as_FREE",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M327-protection-is-stripped-on-no-evidence",
      "why": "the down-switch hysteresis is removed, so one noisy round can strip a patch of distance -- and the seam means it pays twice to put it back",
      "find": "        return want if posterior[best] >= hysteresis else current",
      "replace": "        return want",
      "must_fail": "test_moving_DOWN_requires_evidence",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M328-a-one-pair-bootstrap-reports-an-interval",
      "why": "the pair-count guard is removed, so a degenerate interval around a mean of nothing can satisfy the CI clause",
      "find": "    if n < 2:",
      "replace": "    if False:",
      "must_fail": "test_a_bootstrap_over_one_pair_is_REFUSED",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M329-the-cost-bar-drops-to-zero",
      "why": "the 15% cost-saving bar becomes 0%, so any saving at all -- including noise -- clears the criterion's second clause",
      "find": "COST_BAR = 0.15",
      "replace": "COST_BAR = 0.0",
      "must_fail": "test_advantage_reports_BOTH_bars_and_the_disjunction",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M330-the-band-map-over-provisions",
      "why": "the derived map takes the first distance that FAILS the budget rather than the first that meets it, so every band asks for more protection than it needs and every switch is a seam paid for nothing",
      "find": "            if -math.log(1.0 - 2.0 * e) <= budget:",
      "replace": "            if -math.log(1.0 - 2.0 * e) >= budget:",
      "must_fail": "test_the_map_picks_the_SMALLEST_sufficient_distance",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M331-the-per-round-budget-ignores-the-horizon",
      "why": "the error budget stops being divided by the number of rounds, so a long trajectory is allowed the same per-round error as a short one and the map under-provisions everywhere",
      "find": "    budget = -math.log(2.0 * target - 1.0) / rounds",
      "replace": "    budget = -math.log(2.0 * target - 1.0)",
      "must_fail": "test_the_map_tightens_as_the_horizon_lengthens",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M332-an-arm-that-cannot-reach-the-target-is-ranked-anyway",
      "why": "the target filter is dropped, so the CHEAPEST arm wins even when it never delivers the required correctness -- the defect that made d=3 optimal out to 8,000 rounds by scoring its failures as half-successes",
      "find": "    ok = {k: v for k, v in candidates.items() if v[0] >= target}",
      "replace": "    ok = dict(candidates)",
      "must_fail": "test_an_arm_that_cannot_REACH_the_target_is_excluded_not_ranked",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M333-a-coin-flip-counts-as-a-completion",
      "why": "the floor check accepts a target at or below 0.5, so a fully decohered qubit's 50% guess rate satisfies the criterion",
      "find": "    return p > COIN_FLIP",
      "replace": "    return True",
      "must_fail": "test_a_COIN_FLIP_is_not_a_verified_completion",
      "target": "code_trajectory",
      "outcome": "killed"
    },
    {
      "id": "M334-an-unclassified-gate-is-silently-transparent",
      "why": "the unknown-gate refusal is removed, so a gate whose commutation nobody declared is counted as transparent and every run containing it is too long",
      "find": "        if self.name not in CLIFFORD | NON_CLIFFORD | MEASURE:",
      "replace": "        if False:",
      "must_fail": "test_an_UNCLASSIFIED_gate_is_REFUSED_not_assumed_transparent",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M335-the-correction-cone-stops-growing",
      "why": "a two-qubit Clifford no longer spreads the pending correction, so a blocking gate on the coupled qubit is missed and the transparent run runs past the point it must stop",
      "find": "            cone.update(g.qubits)          # the correction spreads",
      "replace": "            pass",
      "must_fail": "test_the_correction_SPREADS_through_two_qubit_cliffords",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M336-R_eff-counts-every-boundary-not-the-blocked-ones",
      "why": "R_eff stops distinguishing blocked boundaries from transparent ones, so the reaction-depth KILL is keyed on a count that has nothing to do with whether the decoder was ever waited on",
      "find": "    return sum(1 for b in trace if b.blocked_by is not None) / n_t",
      "replace": "    return len(trace) / n_t",
      "must_fail": "test_R_eff_counts_only_the_BLOCKED_boundaries",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M337-an-arbitrary-rotation-is-counted-as-free",
      "why": "RZ stops counting as magic, understating N_T -- the very denominator the reaction-depth KILL divides by",
      "find": "    return sum(1 for g in gates if g.is_blocking)",
      "replace": "    return sum(1 for g in gates if g.name in ('T', 'TDG'))",
      "must_fail": "test_RZ_counts_as_magic",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M338-the-PROMOTE-threshold-drops-to-zero",
      "why": "f(63) >= 0.80 becomes f(63) >= 0.0, so every circuit PROMOTES and the pre-registered table's best outcome is unconditional",
      "find": "    if f63 >= 0.80:",
      "replace": "    if f63 >= 0.0:",
      "must_fail": "test_a_MID_BAND_circuit_does_NOT_promote",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M339-the-greedy-baseline-stops-counting-blocks",
      "why": "the published greedy partition stops starting a block at each non-Clifford, so the baseline the DAG-reordering KILL compares against is not the published rule",
      "find": "        if g.is_blocking:\n            blocks += 1",
      "replace": "        if False:\n            blocks += 1",
      "must_fail": "test_the_greedy_baseline_starts_a_block_at_every_non_clifford",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M340-a-structural-P_stall-zero-reads-as-measured",
      "why": "the too-few-blocked guard is removed, so P_stall = 0.0 from a trace where NOTHING could have stalled reads as a measured rate clearing the conditional row's <= 0.02 bar",
      "find": "    if len(blocked) < 2:",
      "replace": "    if False:",
      "must_fail": "test_P_stall_of_zero_from_TOO_FEW_BLOCKED_says_it_is_structural",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M341-a-failed-artifact-is-reported-as-absent",
      "why": "verdict_of collapses FAILED into ABSENT, so a gate that RAN and recorded a kill falls through to BUILT-UNMEASURED and the roadmap says we wrote some code and never measured it",
      "find": "        return FAILED",
      "replace": "        return ABSENT",
      "must_fail": "test_a_FAILED_artifact_is_FAILED_and_NOT_absent",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M342-a-partial-need-not-say-what-blocks-it",
      "why": "the why_not_closed refusal is removed, so any failing gate can be demoted to RAN-PARTIAL by adding one word to its artifact, and the ledger goes on deriving the softer status honestly",
      "find": "    if not str(doc.get(\"why_not_closed\", \"\")).strip():",
      "replace": "        if False:",
      "must_fail": "test_a_PARTIAL_with_NO_REASON_is_REFUSED",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M343-a-partial-outranks-a-failure",
      "why": "the FAILED branch stops firing, so a gate holding both a tested-and-failed criterion and a blocked one reports the blocked one -- the weaker fact",
      "find": "        if any(x == FAILED for x in v.values()):",
      "replace": "        if False:",
      "must_fail": "test_a_FAILURE_beside_a_PARTIAL_reports_the_FAILURE",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M344-a-corpus-with-no-passed-field-reads-as-failed",
      "why": "an artifact that renders no verdict -- a corpus, a catalogue, a receipt -- stops counting as evidence, which would demote every gate closed by one of them",
      "find": "    if not isinstance(doc, dict) or \"passed\" not in doc:",
      "replace": "    if False:",
      "must_fail": "test_an_artifact_with_NO_VERDICT_is_evidence_not_a_failure",
      "target": "roadmap_gate",
      "outcome": "killed"
    },
    {
      "id": "M345-an-undeclared-correction-target-defaults-to-the-measured-qubit",
      "why": "correction_support stops refusing an undeclared measurement and falls back to the qubit that was MEASURED -- which in a measurement-based uncomputation is the one place the correction is guaranteed not to land, and is what made every routing width read 1.0 and left E1's conditional row unresolvable",
      "find": "    if g.fixup is UNDECLARED:",
      "replace": "    if False:",
      "must_fail": "test_an_UNDECLARED_correction_target_is_REFUSED_not_defaulted",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M346-the-cone-is-seeded-from-the-measured-qubit-again",
      "why": "the causal cone is seeded from the qubits the measurement acted on rather than the qubits its correction lands on, so a blocking gate on the corrected qubit is invisible and one on the measured qubit blocks spuriously",
      "find": "    cone = set(q_j)",
      "replace": "    cone = set(gates[start].qubits)",
      "must_fail": "test_the_CONE_IS_SEEDED_FROM_THE_FIXUP_not_the_measured_qubit",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M347-a-terminal-readout-is-counted-as-a-boundary",
      "why": "a measurement that conditions no correction is admitted as a boundary, padding the denominator of every f(k) with records whose segment length means nothing",
      "find": "            if g.is_measurement and correction_support(g)]",
      "replace": "            if g.is_measurement]",
      "must_fail": "test_a_TERMINAL_READOUT_is_not_a_boundary",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M348-right-censored-boundaries-are-counted-as-at-risk",
      "why": "f_of_k_at_risk stops dropping boundaries that sit closer to the end of the circuit than k gates, so it becomes the naive statistic it exists to correct -- and f(63) goes back to measuring circuit LENGTH",
      "find": "        at_risk = [b for b in trace if n_gates - 1 - b.index >= k]",
      "replace": "        at_risk = list(trace)",
      "must_fail": "test_the_AT_RISK_estimate_drops_boundaries_that_COULD_NOT_run_k",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M349-an-empty-at-risk-denominator-returns-zero-not-None",
      "why": "an at-risk fraction over an empty denominator returns 0.0 instead of None, so a band nobody sampled reads as a measured zero -- and the 'f(63) < 0.50 -> NOT A KILL' row fires on it",
      "find": "            out[k] = (None, 0)",
      "replace": "            out[k] = (0.0, 0)",
      "must_fail": "test_an_EMPTY_at_risk_denominator_is_None_not_a_number",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M350-epsilon-is-normalised-by-the-whole-circuit-again",
      "why": "epsilon_available divides each boundary by the WHOLE circuit instead of the gates actually after it, so it collapses back into the naive epsilon it exists to correct and both readings are depressed by boundary POSITION rather than by blocking",
      "find": "    shares = [b.segment / (n_gates - 1 - b.index)",
      "replace": "    shares = [b.segment / n_gates",
      "must_fail": "test_EPSILON_has_the_SAME_censoring_flaw_and_the_SAME_correction",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M351-the-modular-rounds-stop-reusing-their-registers",
      "why": "the swap between rounds is dropped, so each round is independent and the family stops exhibiting the ONE property it exists to test -- a correction pending from one round reaching the next round's magic",
      "find": "            out.append(Gate(\"SWAP\", (a[i], b[i])))",
      "replace": "            pass",
      "must_fail": "test_the_MODULAR_family_REUSES_its_registers",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M352-the-sign-bits-correction-shrinks-to-the-sign-qubit",
      "why": "the sign measurement declares its correction on the qubit that was measured rather than on the register the conditional addition acts upon, collapsing |R| and returning this family to the mis-rooting that made E1's conditional row unresolvable",
      "find": "    out.append(Gate(\"MZ\", (sign,), fixup=tuple(b)))",
      "replace": "    out.append(Gate(\"MZ\", (sign,), fixup=(sign,)))",
      "must_fail": "test_the_SIGN_BIT_correction_spans_the_whole_target_register",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M353-the-substituted-family-claims-the-papers-variant",
      "why": "the standard modular-exponentiation block stops declaring that it is NOT arXiv:2505.15917's optimised variant, so every number computed on it silently becomes a claim about a construction nobody in this repository can check",
      "find": "        \"built as the STANDARD square-and-multiply block (Beauregard's \"",
      "replace": "        \"the modular exponentiation block of arXiv:2505.15917, \"",
      "must_fail": "test_the_SUBSTITUTED_family_never_claims_the_paper",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M354-the-shared-carry-chain-drops-its-last-position",
      "why": "_ripple_add stops emitting the final CX, changing BOTH the plain adder and every modular adder built on it -- the exact drift that factoring the chain was meant to make impossible",
      "find": "    out.append(Gate(\"CX\", (a[-1], b[-1])))",
      "replace": "    pass",
      "must_fail": "test_the_ADDER_gate_lists_are_PINNED_across_the_refactor",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M355-the-mixture-second-reading-stops-firing",
      "why": "a width spread past the limit no longer produces a second reading, so one f(63) is reported for two classes that read 1.000 and 0.000 -- and the only engineering conclusion here, WHICH corrections stay transparent, disappears into a mean",
      "find": "    if spread is not None and spread > MIXTURE_SPREAD_LIMIT:",
      "replace": "    if False:",
      "must_fail": "test_the_MIXTURE_is_REPORTED_not_averaged_away",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M356-the-width-breakdown-stops-splitting-by-width",
      "why": "boundaries are bucketed by a constant rather than by |Q_j|, so the breakdown has one class, no spread is ever detectable and the detector reports a single population on every circuit",
      "find": "        w = len(b.qubits)",
      "replace": "        w = 1",
      "must_fail": "test_the_aggregate_f63_HIDES_TWO_OPPOSITE_populations",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M357-an-unresolvable-width-class-votes-in-the-spread",
      "why": "the minimum-at-risk filter is dropped, so a class of two or three boundaries -- standard error ~0.35 -- can set the spread and report sampling noise as a bimodal population",
      "find": "            if r[\"at_risk\"] >= MIXTURE_MIN_AT_RISK",
      "replace": "            if True",
      "must_fail": "test_an_UNRESOLVABLE_class_is_EXCLUDED_from_the_spread",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M358-a-single-width-class-reports-a-spread-of-zero",
      "why": "with fewer than two resolvable classes the spread returns 0.0 instead of None, so 'nothing could be compared' reads as 'measured, and the population is uniform'",
      "find": "    return max(vals) - min(vals) if len(vals) >= 2 else None",
      "replace": "    return max(vals) - min(vals) if len(vals) >= 2 else 0.0",
      "must_fail": "test_a_SINGLE_population_reports_NO_spread",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M359-a-blocked-correction-is-counted-as-dead",
      "why": "the never-blocked clause is dropped, so a correction the decoder WAS waited on counts toward the dead fraction -- the one number that is supposed to mean 'never had to be resolved'",
      "find": "               if b.blocked_by is None and not obs.intersection(b.cone))",
      "replace": "               if not obs.intersection(b.cone))",
      "must_fail": "test_a_BLOCKED_correction_is_never_counted_dead",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M360-the-dead-fraction-guesses-the-observables",
      "why": "the undeclared-observables refusal is removed, so the row reports a fraction derived from a guess about what the circuit is FOR -- and 'none' would report every correction dead",
      "find": "    if observables is None:\n        raise ObservablesUndeclared(",
      "replace": "    if False:\n        raise ObservablesUndeclared(",
      "must_fail": "test_the_DEAD_row_REFUSES_to_guess_the_observables",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M361-a-lower-bound-below-the-bar-is-reported-as-a-failure",
      "why": "the sub-bar branch stops saying UNRESOLVED, so a bound that undercounts BY CONSTRUCTION is used to refute the claim it cannot refute -- 'I could not see it' reported as 'it is not there'",
      "find": "                 f\"UNRESOLVED: the computable LOWER BOUND is {dead:.3f}, \"",
      "replace": "                 f\"FAILED: the dead fraction is {dead:.3f}, \"",
      "must_fail": "test_the_DEAD_row_reports_UNRESOLVED_not_FAILED_below_the_bar",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M362-the-sixth-row-goes-missing-again",
      "why": "the D1 row stops being appended when no observables are declared, so the table silently answers five questions while claiming six -- and an absent row reads as a row that passed",
      "find": "             \"UNMEASURED: no final observables were declared for this \"",
      "replace": "             \"\" if True else \"",
      "must_fail": "test_a_census_with_NO_observables_says_UNMEASURED_not_zero",
      "target": "transparency",
      "outcome": "killed"
    },
    {
      "id": "M363-abort-retry-does-not-repay-the-magic-it-redoes",
      "why": "a retried block stops recharging its T gates, so abort-retry buys a lower logical error with time alone and dominates every policy for a reason that is an accounting omission",
      "find": "    return _axes(\"abort_retry\", p_L_kept, (w.rounds + stall) * attempts,\n                 w.t_count * attempts,",
      "replace": "    return _axes(\"abort_retry\", p_L_kept, (w.rounds + stall) * attempts,\n                 w.t_count,",
      "must_fail": "test_abort_retry_is_charged_for_the_MAGIC_it_redoes",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M364-D7-speculates-without-paying-magic",
      "why": "the expected magic charge stops accumulating, so D7 buys time for nothing and strictly dominates plain deferral -- the exact free lunch the T-count axis exists to price",
      "find": "            charge += expected",
      "replace": "            pass",
      "must_fail": "test_D7_pays_MAGIC_for_the_time_it_saves",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M365-D7-speculates-regardless-of-the-posterior",
      "why": "the speculate/stall comparison is removed, so D7 speculates on a coin-flip posterior at a platform where the magic costs more than the stall it avoids",
      "find": "        if expected * magic < stall_saved:",
      "replace": "        if True:",
      "must_fail": "test_D7_declines_to_speculate_when_the_posterior_is_BAD",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M366-undo-fixup-redo-rolls-back-a-fixed-shallow-depth",
      "why": "the rollback depth stops being the decoder latency, so Undo-Fixup-Redo is billed for undoing less than it executed and never-waiting looks nearly free",
      "find": "    rollback = blocked * p_mis_mean * w.rollback_rounds",
      "replace": "    rollback = blocked * p_mis_mean",
      "must_fail": "test_the_ROLLBACK_DEPTH_is_the_decoder_latency_not_the_block_length",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M367-a-tie-counts-as-strict-dominance",
      "why": "dominance stops requiring a strict improvement on any axis, so two identical policies each dominate the other and the Pareto front empties",
      "find": "    return any(u < v - DOMINANCE_EPS for u, v in zip(xa, xb))",
      "replace": "    return True",
      "must_fail": "test_strict_dominance_needs_BETTER_on_one_and_NOT_WORSE_on_any",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M368-dominance-ignores-the-axes-a-policy-is-worse-on",
      "why": "the worse-on-any-axis veto is dropped, so a policy that is cheaper in spacetime dominates one that is cheaper in magic -- an exchange rate nobody declared, applied silently",
      "find": "    if any(u > v + DOMINANCE_EPS for u, v in zip(xa, xb)):",
      "replace": "    if False:",
      "must_fail": "test_the_front_keeps_INCOMPARABLE_policies",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M369-INV2-accepts-a-speculation-with-no-D7-record",
      "why": "the speculation/record count comparison is removed, so a policy may depend on a live bit with no D7 record at all -- which is precisely what INV-2 forbids",
      "find": "        if spec and c.policy == \"deferral_d7\" and len(c.d7_records) != spec:",
      "replace": "        if False:",
      "must_fail": "test_INV2_catches_a_speculation_with_NO_RECORD",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M370-repair-debt-is-judged-by-its-mean-not-its-minima",
      "why": "the debt test compares window MEANS instead of MINIMA, so a queue that drains fully every few rounds but bursts higher reads as growing without bound",
      "find": "    return min(debt_by_round[half:]) > min(debt_by_round[:half]) + tol",
      "replace": "    return (sum(debt_by_round[half:]) / len(debt_by_round[half:])\n            > sum(debt_by_round[:half]) / len(debt_by_round[:half]) + tol)",
      "must_fail": "test_the_REPAIR_DEBT_kill_is_reachable_and_calibrated_both_ways",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M371-eps-is-reported-as-a-probability-not-an-RMS-error",
      "why": "the eps = 2 sqrt(p) conversion is dropped, so a probability is reported in the units of A -- the exact error that made the WAY model read 'unphysical at every scale' the first time",
      "find": "        \"eps_empirical\": 2.0 * math.sqrt(p_emp),",
      "replace": "        \"eps_empirical\": p_emp,",
      "must_fail": "test_eps_is_an_RMS_ERROR_IN_UNITS_OF_A_not_a_probability",
      "target": "ga11_hardware_iq",
      "outcome": "killed"
    },
    {
      "id": "M372-the-empirical-error-is-replaced-by-the-model-one",
      "why": "the counted misassignments are replaced by the Gaussian prediction, so the 527x disagreement that IS this module's result becomes exactly 1.0 and the device looks textbook",
      "find": "    p_emp = (wrong0 + wrong1) / (n0 + n1)",
      "replace": "    p_emp = p_model",
      "must_fail": "test_a_NON_GAUSSIAN_TAIL_is_what_the_ratio_detects",
      "target": "ga11_hardware_iq",
      "outcome": "killed"
    },
    {
      "id": "M373-a-qubit-that-did-not-read-out-is-not-refused",
      "why": "identical centroids stop being refused, so a dead readout divides by zero separation or reports an error of zero -- an instrument that says a broken qubit is a flawless one",
      "find": "    if sep == 0:",
      "replace": "    if False:",
      "must_fail": "test_analyse_REFUSES_a_qubit_that_did_not_read_out",
      "target": "ga11_hardware_iq",
      "outcome": "killed"
    },
    {
      "id": "M374-too-few-shots-are-scored-anyway",
      "why": "the minimum-shot refusal is removed, so a discrimination error with no resolution is computed and written into an artifact",
      "find": "    if iq0.size < 32 or iq1.size < 32:",
      "replace": "        if False:",
      "must_fail": "test_analyse_REFUSES_too_few_shots",
      "target": "ga11_hardware_iq",
      "outcome": "killed"
    },
    {
      "id": "M375-engine-disagreement-is-resolved-instead-of-raised",
      "why": "two engines returning different answers for the SAME instance stops being a refusal, so the portfolio silently picks one -- which is exactly the laundering the module's docstring says must never happen: one of those engines is broken and that is the finding, not the vote",
      "find": "    if len(answers) > 1:",
      "replace": "    if False:",
      "must_fail": "test_a_SPLIT_is_REFUSED_and_never_voted_on",
      "target": "distance_portfolio",
      "outcome": "killed"
    },
    {
      "id": "M376-a-YES-with-an-unverifiable-witness-is-accepted",
      "why": "the portfolio-boundary re-check is removed, so an engine may answer YES with a witness that does not validate -- the gap the second check exists to close, and it costs microseconds",
      "find": "        if not ok:\n            raise Disagreement(",
      "replace": "        if False:\n            raise Disagreement(",
      "must_fail": "test_a_YES_with_an_UNVERIFIABLE_witness_is_REFUSED",
      "target": "distance_portfolio",
      "outcome": "killed"
    },
    {
      "id": "M377-an-uninterruptible-solver-is-given-a-budget-anyway",
      "why": "a solver that cannot be interrupted stops being refused, so `budget_s` becomes a comment and any bound applied to it is a claim about a timeout that never fired",
      "find": "        raise BudgetNotEnforceable(",
      "replace": "        pass  # noqa",
      "must_fail": "test_a_SOLVER_THAT_CANNOT_BE_STOPPED_is_REFUSED_not_humoured",
      "target": "distance_portfolio",
      "outcome": "killed"
    },
    {
      "id": "M378-a-mechanism-rate-outside-zero-one-is-accepted",
      "why": "the probability-range refusal is dropped, so a 'rate' of 1.7 or -0.2 enters the parity model and every downstream likelihood is computed from a number that is not a probability",
      "find": "            raise ValueError(\"a mechanism rate outside [0, 1]\")",
      "replace": "            pass",
      "must_fail": "test_a_rate_OUTSIDE_zero_one_is_refused",
      "target": "latent_parity",
      "outcome": "killed"
    },
    {
      "id": "M379-a-rate-vector-of-the-wrong-length-is-broadcast",
      "why": "the rates-vs-mechanisms shape check is removed, so numpy broadcasts a wrong-length rate vector and the model fits a different number of mechanisms than it was given",
      "find": "            raise ValueError(f\"{p.shape} rates for {A.shape[0]} mechanisms\")",
      "replace": "            pass",
      "must_fail": "test_a_rate_vector_of_the_WRONG_LENGTH_is_refused_not_broadcast",
      "target": "latent_parity",
      "outcome": "killed"
    },
    {
      "id": "M380-the-node-budget-is-never-enforced",
      "why": "the branch-and-bound node ceiling stops raising, so a search that ran out of budget returns a certificate for a bound it never finished proving -- an unbounded claim from a bounded search",
      "find": "            raise RuntimeError(f\"exceeded {max_nodes} nodes\")",
      "replace": "            pass",
      "must_fail": "test_the_NODE_BUDGET_is_enforced_and_refuses_rather_than_certifying",
      "target": "branch_certificate",
      "outcome": "killed"
    },
    {
      "id": "M381-a-node-with-no-branch-and-no-dual-is-accepted",
      "why": "a tree node carrying neither a branching variable nor a dual stops being refused, so a certificate with an unjustified step checks out -- the step that proves nothing is the one a forger would insert",
      "find": "            raise ValueError(\"no branching variable and no dual: the \"",
      "replace": "            pass  # noqa",
      "must_fail": "test_the_checker_refuses_malformed_documents",
      "target": "branch_certificate",
      "outcome": "killed"
    },
    {
      "id": "M382-a-float-dual-is-accepted-into-an-exact-certificate",
      "why": "the exact-pair type refusal is dropped, so a floating-point dual enters a certificate whose whole value is that it is EXACT -- and weak duality checked in floats is an inequality about rounding, not about the cut",
      "find": "    raise TypeError(\"dual values must be exact {num,den} pairs, not floats\")",
      "replace": "    return Fraction(0, 1)",
      "must_fail": "test_exact_cut_forbids_float_dual_values",
      "target": "matching_cert_strict",
      "outcome": "killed"
    },
    {
      "id": "M383-a-zero-denominator-becomes-a-fraction",
      "why": "the zero-denominator refusal stops firing, so a dual value with den = 0 is constructed and every comparison downstream is against an undefined quantity",
      "find": "            raise ValueError(\"zero denominator\")\n        return Fraction(int(value[\"num\"]), den)",
      "replace": "            pass\n        return Fraction(int(value[\"num\"]), den or 1)",
      "must_fail": "test_a_ZERO_DENOMINATOR_is_refused_in_the_MAPPING_form",
      "target": "matching_cert_strict",
      "outcome": "killed"
    },
    {
      "id": "M384-a-disagreeing-certified-rank-is-accepted",
      "why": "the certified-rank agreement check is removed, so the reduction is built on a span the closure certificate does not describe -- and every question below it becomes a question about a different subspace, which the module's own message says",
      "find": "    if expect_certified_rank is not None and rank_S != expect_certified_rank:",
      "replace": "    if False:",
      "must_fail": "test_a_DISAGREEING_certified_rank_is_REFUSED",
      "target": "quotient_reduction",
      "outcome": "killed"
    },
    {
      "id": "M385-a-rank-exceeding-r-is-not-refused",
      "why": "`rank_S > r` stops being refused, so the reduction proceeds with more independent directions than the codimension admits and the question count it derives is meaningless",
      "find": "    if rank_S > r:",
      "replace": "    if False:",
      "must_fail": "test_a_rank_ABOVE_r_is_REFUSED",
      "target": "quotient_reduction",
      "outcome": "killed"
    },
    {
      "id": "M386-a-pin-may-name-a-file-absent-from-its-commit",
      "why": "the not-in-commit refusal is dropped, so a pin can name bytes that were never in the immutable commit it cites -- and a pin to bytes nobody can retrieve is a hash with no referent",
      "find": "        raise NotTracked(\n            f\"{path} is not in {commit[:8]}; a pin may only name bytes \"",
      "replace": "        return b\"\"  # noqa\n        raise NotTracked(\n            f\"{path} is not in {commit[:8]}; a pin may only name bytes \"",
      "must_fail": "test_a_pin_naming_a_commit_that_lacks_the_file_is_refused",
      "target": "git_pin",
      "outcome": "killed"
    },
    {
      "id": "M387-a-pin-need-not-say-what-it-is-for",
      "why": "the empty-role refusal stops firing, so a pin is created that cannot say what it binds -- and a binding with no stated role cannot state what its own staleness would MEAN, which is the only thing a pin is for",
      "find": "    if not role or not role.strip():",
      "replace": "    if False:",
      "must_fail": "test_a_pin_must_say_what_it_is_FOR",
      "target": "git_pin",
      "outcome": "killed"
    },
    {
      "id": "M388-a-non-repository-still-yields-a-head-commit",
      "why": "the not-a-repository refusal is removed, so `head_commit` returns whatever the failed git call left in stdout and every pin below it cites a commit that does not exist",
      "find": "        raise NotTracked(\"not a git repository, so nothing can be pinned\")",
      "replace": "        return \"0\" * 40",
      "must_fail": "test_blob_bytes_and_head_commit_refuse_outside_a_repository",
      "target": "git_pin",
      "outcome": "killed"
    },
    {
      "id": "M389-a-plan-may-require-a-field-that-does-not-exist",
      "why": "the unknown-field refusal is dropped, so a plan can require something the manifest cannot express -- and a requirement that can NEVER be refused for lacking it is a requirement that does nothing, which is the module's own wording",
      "find": "        if n.field not in FIELDS:",
      "replace": "        if False:",
      "must_fail": "test_a_requirement_must_name_a_REAL_field",
      "target": "capability_plan",
      "outcome": "killed"
    },
    {
      "id": "M390-a-requirement-need-not-state-why",
      "why": "the empty-reason refusal stops firing, so a requirement enters with no rationale -- and only a requirement whose reason is stated can ever be safely DROPPED, which is the entire purpose of a plan-specific set",
      "find": "        if not n.because.strip():",
      "replace": "        if False:",
      "must_fail": "test_a_requirement_must_state_WHY",
      "target": "capability_plan",
      "outcome": "killed"
    },
    {
      "id": "M391-an-enum-requirement-outside-its-domain-is-accepted",
      "why": "the enum-domain check is removed, so a plan demands a value the field can never take and the resulting verdict is a comparison against a string nothing produces",
      "find": "        if kind == \"enum\" and n.want not in ENUM_DOMAIN[n.field]:",
      "replace": "        if False:",
      "must_fail": "test_an_ENUM_requirement_outside_its_domain_is_refused",
      "target": "capability_plan",
      "outcome": "killed"
    },
    {
      "id": "M392-the-majority-vote-direction-is-inverted",
      "why": "the decode reports a logical error whenever the majority reads ZERO, so a clean run is scored as an almost total failure -- p_L 2.6e-4 becomes 0.99974, and Lambda is a ratio of two numbers that both mean their own opposite",
      "find": "    majority = (counts_fin.sum(axis=1) * 2 > d)",
      "replace": "    majority = (counts_fin.sum(axis=1) * 2 < d)",
      "must_fail": "test_the_MAJORITY_DIRECTION_is_not_reversible",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M393-zero-shots-are-scored-as-a-zero-error-rate",
      "why": "the empty-sample refusal is dropped, so a run that returned no shots at all reports p_L = 0 -- a perfect logical error rate recorded in the evidence for a measurement that never happened",
      "find": "        raise ValueError(\"no shots to score\")",
      "replace": "        pass",
      "must_fail": "test_ZERO_shots_is_REFUSED_not_scored_as_zero",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M394-a-zero-error-rate-claims-zero-relative-error",
      "why": "the infinite relative error on a zero-count rate becomes 0.0, so a run that saw nothing reads as the TIGHTEST measurement in the artifact rather than the least informative one",
      "find": "            \"rel_stderr\": (se / p) if p > 0 else float(\"inf\"),",
      "replace": "            \"rel_stderr\": (se / p) if p > 0 else 0.0,",
      "must_fail": "test_a_ZERO_rate_reports_INFINITE_relative_error_not_zero",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M395-any-number-of-events-counts-as-resolved",
      "why": "the >= 25 event floor for calling a rate resolved is removed, so the ONE logical error seen at d=5 would promote the point estimate of Lambda from UNRESOLVED to 26 -- a ten-fold overstatement of the device's suppression, published as a measurement",
      "find": "            \"resolved\": bool(errors >= 25)}",
      "replace": "            \"resolved\": True}",
      "must_fail": "test_the_RESOLUTION_is_reported_and_gates_on_the_COUNT",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M396-a-lambda-bound-is-computed-from-an-unresolved-numerator",
      "why": "the requirement that the NUMERATOR be resolved is dropped, so a ratio of two unresolved rates -- noise over a confidence limit -- is published as a 95% lower bound on Lambda",
      "find": "        if lo[\"resolved\"]:",
      "replace": "        if True:",
      "must_fail": "test_a_BOUND_needs_a_RESOLVED_numerator",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M397-the-poisson-limit-becomes-a-normal-approximation",
      "why": "the exact Garwood limit is replaced by k + 1.96*sqrt(k), which at the k <= 5 this experiment actually lands on understates the upper limit by more than half (2.96 against 4.74 at k=1) and therefore OVERSTATES the lower bound on Lambda -- the error points in the flattering direction",
      "find": "    return float(0.5 * chi2.ppf(0.95, 2 * (k + 1)))",
      "replace": "    return float(k + 1.96 * (k ** 0.5))",
      "must_fail": "test_the_TABLE_calibrates_the_COMPUTATION",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M398-a-zero-numerator-rate-is-allowed-to-form-a-ratio",
      "why": "the zero-numerator refusal is dropped, so a distance that saw no errors at all -- nothing to suppress -- yields a ratio rather than a stated reason, and a division by the smaller rate reports suppression the data cannot show",
      "find": "        if lo[\"p_L\"] <= 0:",
      "replace": "        if False:",
      "must_fail": "test_a_ZERO_numerator_is_REFUSED_with_its_reason",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M399-the-reported-lambda-bound-is-the-weakest-step",
      "why": "the aggregate lower bound takes min instead of max across steps, discarding the strongest claim the data supports -- each step's bound is independently valid, so reporting the smallest understates what was measured",
      "find": "            max(bounds) if bounds else None)",
      "replace": "            min(bounds) if bounds else None)",
      "must_fail": "test_EVERY_STEP_is_reported_and_the_bound_is_the_STRONGEST",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M400-an-unreachable-kill-row-reports-itself-reachable",
      "why": "the reachability test flips to > 0.5, so a KILL whose threshold no posterior can reach -- p_mis cannot exceed one half -- is reported as a live check, and E2's sensitivity table says the opposite of what it measures at every lambda",
      "find": "    return d7_decline_threshold(d, t_d_rounds, lam) < 0.5",
      "replace": "    return d7_decline_threshold(d, t_d_rounds, lam) > 0.5",
      "must_fail": "test_a_THRESHOLD_ABOVE_ONE_HALF_is_a_KILL_THAT_CANNOT_FIRE",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M401-the-decline-threshold-inverts-its-own-lambda-dependence",
      "why": "lambda moves from the denominator to the numerator of the decline threshold, so MORE suppression would demand MORE confidence to decline -- the sign of the whole surcharge argument reversed, while every individual verdict still returns a plausible boolean",
      "find": "            / (lam * D7_MAGIC_PER_SPECULATION * magic_cost_qubit_rounds(d)))",
      "replace": "            * lam / (D7_MAGIC_PER_SPECULATION * magic_cost_qubit_rounds(d)))",
      "must_fail": "test_the_threshold_FALLS_as_lambda_RISES",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M402-the-sensitivity-table-keeps-only-the-reachable-rows",
      "why": "rows whose KILL cannot fire are filtered out of the sensitivity, so the table shows only the lambdas that make the row look live -- the simulated value, which is the one E2 actually used and the one that makes it dark, disappears from the artifact entirely",
      "find": "        out.append({\"source\": name, \"lambda\": lam,",
      "replace": "        if thr >= 0.5:\n            continue\n        out.append({\"source\": name, \"lambda\": lam,",
      "must_fail": "test_the_SENSITIVITY_reports_every_candidate_and_never_picks_one",
      "target": "bakeoff",
      "outcome": "killed"
    },
    {
      "id": "M403-the-router-goes-back-to-grepping-the-lane-s-note",
      "why": "the MITM lane's proof obligation is read out of English again instead of from its boolean. The lane's TIME-OUT note says 'time budget exhausted; NO exhaustive claim at any W', which CONTAINS the substring -- so a sweep that ran out of budget and proved nothing certifies the distance, and the router reports decided=True on a bound nothing established",
      "find": "        if cert.exhaustive:",
      "replace": "        if \"exhaustive\" in cert.note:",
      "must_fail": "test_a_TIMED_OUT_mitm_lane_MUST_NOT_CERTIFY_THE_BOUND",
      "target": "certify",
      "outcome": "killed"
    },
    {
      "id": "M404-a-completed-sweep-always-claims-exhaustiveness",
      "why": "the exhaustiveness flag stops depending on whether a lighter operator was FOUND, so a sweep that found one -- which improves the bound rather than proving it -- is published as a completed negative result",
      "find": "                           exhaustive=not found)",
      "replace": "                           exhaustive=True)",
      "must_fail": "test_mitm_matches_brute_force_both_directions",
      "target": "mitm",
      "outcome": "killed"
    },
    {
      "id": "M405-a-failed-T-join-test-declares-the-syndrome-consistent",
      "why": "the odd-degree refusal declares syndrome_consistent=True, so a correction whose odd-degree set does NOT equal the syndrome is published in a degraded receipt whose own text claims the correction explains the syndrome -- a false statement in an artifact, from the one place that actually knows the answer",
      "find": "            syndrome_consistent_declared=False)",
      "replace": "            syndrome_consistent_declared=True)",
      "must_fail": "test_the_T_JOIN_verdict_is_a_FIELD_not_a_phrase_in_the_reason",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M406-an-unasked-T-join-question-answers-itself-yes",
      "why": "the third state is removed: an input rejected BEFORE the T-join test ever ran reports syndrome_consistent=True rather than None, so a malformed certificate the checker never got far enough to evaluate is credited with explaining its syndrome",
      "find": "        got = self.checks.get(\"t_join_valid\")\n        return bool(got) if got is not None else None",
      "replace": "        return True",
      "must_fail": "test_a_MALFORMED_input_leaves_the_verdict_UNASKED",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M407-ladder-escalation-goes-back-to-grepping-refusal-prose",
      "why": "the structured only_obstruction_is_gap result is discarded and the decoder again searches the human-readable refusal reason; a wording-only edit can then stop a useful escalation or repeat an expensive one whose actual obstruction cannot be repaired",
      "find": "            if not r.only_obstruction_is_gap:",
      "replace": "            if \"NOT PROVEN OPTIMAL\" not in r.reason:",
      "must_fail": "test_ladder_escalation_is_a_field_not_a_refusal_phrase",
      "target": "certifying_decoder",
      "outcome": "killed"
    },
    {
      "id": "M408-a-refusal-that-never-set-the-flag-claims-to-be-gap-only",
      "why": "the escalation flag defaults to True, so a refusal that never set it -- a malformed document, an odd-degree mismatch -- is read as an unclosed gap and the ladder pays three rungs for a defect no candidate family can repair. A default must be the answer that is safe when nobody spoke",
      "find": "    only_obstruction_is_gap: bool = False",
      "replace": "    only_obstruction_is_gap: bool = True",
      "must_fail": "test_the_gap_flag_DEFAULTS_to_the_non_escalating_state",
      "target": "matching_cert",
      "outcome": "killed"
    },
    {
      "id": "M409-an-even-distance-is-scored-instead-of-refused",
      "why": "the even-distance refusal is dropped, so a repetition code with no majority is decoded anyway and every tie is broken silently in one direction. This guard exists because a mutant swapping > for >= was EQUIVALENT under odd d and could not be killed; the precondition is what makes the comparison's strictness a property rather than an accident of parity",
      "find": "    if d % 2 == 0:",
      "replace": "    if False:",
      "must_fail": "test_an_EVEN_distance_has_no_majority_and_is_REFUSED",
      "target": "hardware_lambda",
      "outcome": "killed"
    },
    {
      "id": "M410-a-dual-above-its-primal-is-clamped-not-refused",
      "why": "weak duality forbids a dual above its primal, so seeing one means the packing is infeasible or the two sides disagree about the edge weights -- this repository caught exactly that at d=7. Clamping turns a producer defect into a quietly optimistic measurement: wrong, and silent",
      "find": "    if bound > primal + 1e-9:",
      "replace": "    if False:",
      "must_fail": "test_a_dual_ABOVE_its_primal_is_REFUSED_not_clamped",
      "target": "certified_weight",
      "outcome": "killed"
    },
    {
      "id": "M411-the-device-floor-averages-only-the-CERTIFIED-shots",
      "why": "refused shots are dropped from the average. They are the HARD shots -- more error, weaker packing -- so the reported floor falls, and it falls FURTHER every time the decoder gets worse. A measurement that improves when the machinery degrades is the survivorship error in its purest form",
      "find": "        ivs.append(iv)",
      "replace": "        if iv.certified:\n            ivs.append(iv)",
      "must_fail": "test_REFUSED_shots_are_counted_and_their_exclusion_would_FLATTER",
      "target": "certified_weight",
      "outcome": "killed"
    },
    {
      "id": "M412-a-left-skewed-sample-still-claims-a-reliable-95-percent",
      "why": "the shape check is removed, so a sample whose measured coverage is 0.640 against a nominal 0.95 is published beside a 95% label. MEASURED at 400 trials: left-skewed weights under-cover at every sample size, and neither the bootstrap nor the normal approximation repairs it",
      "find": "    reliable = skew is None or skew > LEFT_SKEW_UNRELIABLE",
      "replace": "    reliable = True",
      "must_fail": "test_a_LEFT_SKEWED_sample_marks_the_population_bound_UNRELIABLE",
      "target": "certified_weight",
      "outcome": "killed"
    },
    {
      "id": "M413-an-undeclared-weight-convention-assumes-log-likelihood",
      "why": "an undeclared convention defaults to the likelihood reading, so a run under UNIFORM weights -- which is what the live hardware lane actually used -- publishes a statement about PROBABILITY computed from a count of edges",
      "find": "                 convention: str = UNDECLARED,",
      "replace": "                 convention: str = LOG_LIKELIHOOD,",
      "must_fail": "test_an_UNDECLARED_convention_is_UNCHECKABLE_not_assumed",
      "target": "certified_weight",
      "outcome": "killed"
    },
    {
      "id": "M414-the-reliability-drops-the-factor-of-two",
      "why": "reliability becomes 1 - p instead of 1 - 2p, so every shot is debiased by the wrong amount and the estimate is biased towards zero by a factor no number of shots removes",
      "find": "    return min(math.tanh(gap / 2.0), MAX_RELIABILITY)",
      "replace": "    return min(1.0 - 1.0 / (1.0 + math.exp(gap)), MAX_RELIABILITY)",
      "must_fail": "test_the_reliability_is_EXACTLY_one_minus_twice_the_error_rate",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M415-every-shot-is-weighted-the-same",
      "why": "the inverse-variance weight becomes a constant, so the estimator reports a variance reduction it did not achieve and every error bar downstream is too small",
      "find": "    return (a * a) / (1.0 - a * a)",
      "replace": "    return 1.0",
      "must_fail": "test_a_CONFIDENT_shot_OUTVOTES_a_doubtful_one",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M416-a-zero-confidence-shot-is-given-a-full-vote",
      "why": "a shot the decoder had no opinion about is handed unit weight instead of zero, so pure noise joins the average with the same authority as a near-certain shot",
      "find": "    if a <= MIN_RELIABILITY:\n        return 0.0",
      "replace": "    if a <= MIN_RELIABILITY:\n        return 1.0",
      "must_fail": "test_a_ZERO_CONFIDENCE_shot_contributes_NOTHING_to_the_answer",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M417-the-predicted-rate-is-evaluated-at-the-bins-mean-gap",
      "why": "the per-shot predicted rate is replaced by the rate at the bin's MEAN gap. The logistic is convex, so this under-predicts the error rate -- MEASURED at +0.0070 on the [2,4) bin, a third of the materiality bar -- and every honestly calibrated run reads as OVERCONFIDENT, a systematic false positive in exactly the direction this module exists to flag",
      "find": "        pred = c[\"pred_sum\"] / c[\"n\"]        # mean of p(g), not p(mean g)",
      "replace": "        pred = predicted_rate(mean_gap)",
      "must_fail": "test_an_HONEST_model_is_reported_CONSISTENT",
      "target": "dem_calibration",
      "outcome": "killed"
    },
    {
      "id": "M418-calibration-is-judged-on-the-p-value-alone",
      "why": "the effect-size bar is dropped, so a chi-square on a large run calls a deviation in the fourth decimal MISCALIBRATED. True, useless, and the fastest way to get a real gate switched off",
      "find": "    elif significant and material:",
      "replace": "    elif significant:",
      "must_fail": "test_a_HUGE_sample_does_not_call_a_TINY_deviation_MISCALIBRATED",
      "target": "dem_calibration",
      "outcome": "killed"
    },
    {
      "id": "M419-bins-too-thin-to-tell-a-rate-from-noise-are-scored-anyway",
      "why": "the minimum-per-bin floor is removed, so a cell holding three shots contributes its 0-or-1 rate to the chi-square and the verdict is decided by sampling noise",
      "find": "        if c[\"n\"] < MIN_PER_BIN:",
      "replace": "        if False:",
      "must_fail": "test_THIN_bins_are_EXCLUDED_and_the_exclusion_is_REPORTED",
      "target": "dem_calibration",
      "outcome": "killed"
    },
    {
      "id": "M420-the-calibration-error-loses-its-sign",
      "why": "the signed error becomes an absolute one, so the report can say the model and the device disagree but not WHICH WAY. Overconfident means every downstream logical error rate is optimistic; pessimistic means performance is being left unclaimed. Without the sign there is nowhere to look",
      "find": "        signed_num += c[\"n\"] * (obs - pred)",
      "replace": "        signed_num += c[\"n\"] * abs(obs - pred)",
      "must_fail": "test_a_PESSIMISTIC_model_is_caught_with_the_OTHER_sign",
      "target": "dem_calibration",
      "outcome": "killed"
    },
    {
      "id": "M421-calibration-power-counts-only-what-the-model-EXPECTED",
      "why": "power is granted on expected events alone. Expected events are computed FROM the model, and an overconfident model understates exactly that -- so a bin with five REAL errors against 1.25 expected is scored powerless and the run reads UNCHECKABLE. MEASURED on ibm_marrakesh: that is the bin showing the device four times noisier than its own calibration claims",
      "find": "            or c[\"wrong\"] >= MIN_EXPECTED_EVENTS)",
      "replace": "            or False)",
      "must_fail": "test_power_counts_OBSERVED_events_not_only_EXPECTED_ones",
      "target": "dem_calibration",
      "outcome": "killed"
    },
    {
      "id": "M422-zero-observed-errors-becomes-a-zero-error-rate",
      "why": "the Jeffreys debias rate becomes the MLE, so a bin with no observed errors claims p = 0 -- certainty derived from having seen nothing go wrong -- and hands itself unbounded weight. MEASURED: the ibm_marrakesh top bin held 3,896 clean shots, which support p <= 7.7e-4 and not zero",
      "find": "        p_hat = (k + 0.5) / (n + 1.0)                 # Jeffreys",
      "replace": "        p_hat = k / n",
      "must_fail": "test_ZERO_observed_errors_does_NOT_become_certainty",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M423-the-weights-use-the-point-rate-instead-of-the-upper-limit",
      "why": "the weights stop using the one-sided upper limit and use the point estimate, so confidence is overstated and every error bar downstream is too small. The debias and the weights want OPPOSITE errors, and collapsing them to one rate is how a clamped model produced +1.3033 on hardware",
      "find": "        w = (a_cons * a_cons) / (1.0 - a_cons * a_cons)",
      "replace": "        w = (a_pt * a_pt) / (1.0 - a_pt * a_pt)",
      "must_fail": "test_the_DEBIAS_and_the_WEIGHTS_use_DIFFERENT_rates_on_purpose",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M424-the-measured-calibration-map-stops-being-monotone",
      "why": "the running maximum is dropped, so sampling noise in a thin bin can give a HIGH-gap bin a worse measured error rate than a low-gap one -- inverting the weighting the estimator exists to apply, on exactly the shots it trusts most",
      "find": "        running_hat = max(running_hat, out[b][\"p_hat\"])",
      "replace": "        running_hat = out[b][\"p_hat\"]",
      "must_fail": "test_the_map_is_MONOTONE_because_confidence_cannot_rise_with_doubt",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M425-the-out-of-range-check-goes-back-to-an-absolute-epsilon",
      "why": "the self-falsification check stops scaling with the error bar, so an estimate 0.28 sigma past the boundary -- a value pinned at the edge of its own range -- is flagged exactly like one 22.5 sigma out. A check that cannot tell sampling noise from a broken model fires on every honest run and gets switched off",
      "find": "    return abs(value) > 1.0 + OUT_OF_RANGE_SIGMA * se + 1e-12",
      "replace": "    return abs(value) > 1.0 + 1e-12",
      "must_fail": "test_out_of_range_is_RELATIVE_to_the_error_bar",
      "target": "soft_estimator",
      "outcome": "killed"
    },
    {
      "id": "M426-a-negative-result-stops-reporting-what-it-could-have-seen",
      "why": "the 3-sigma sensitivity becomes zero, so 'no order effect found' no longer carries the size of the effect that would have been invisible. MEASURED on ibm_marrakesh: r1=+0.022 against a sensitivity of 0.047, so the honest sentence is 'nothing above 0.05 was found' and the mutant's sentence is 'the device is stable' -- a claim 4,000 shots cannot support",
      "find": "sensitivity=3.0 * sd,",
      "replace": "sensitivity=0.0,",
      "must_fail": "test_every_result_REPORTS_the_smallest_effect_it_could_have_SEEN",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M427-an-undefined-correlation-is-returned-as-zero",
      "why": "a sample whose observations are all identical returns 0.0 instead of refusing. Zero is a real answer meaning 'no order dependence', and it is indistinguishable from 'the denominator was zero and nothing was measured' -- the dark gate shape, on the statistic every other reading here feeds from",
      "find": "        raise ExchangeabilityRefusal(\n            \"every observation is identical",
      "replace": "        return 0.0\n        raise ExchangeabilityRefusal(\n            \"every observation is identical",
      "must_fail": "test_a_CONSTANT_sample_is_REFUSED_not_reported_as_uncorrelated",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M428-the-null-is-built-without-shuffling",
      "why": "the shuffle is dropped, so every 'permutation' recomputes the statistic on the ORIGINAL order. The null collapses to a point at the observed value, its sd is zero, and the test can never reject anything -- while still printing a p-value and a null mean that look entirely ordinary",
      "find": "        rng.shuffle(work)",
      "replace": "        pass",
      "must_fail": "test_it_FINDS_a_drift_that_is_really_there",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M429-the-two-sided-test-only-counts-the-upper-tail",
      "why": "a two-sided request is scored as one-sided, so a device whose shots ANTI-correlate -- a decoder alternating, a duty cycle, a readout that alternates with its neighbour -- passes with a p-value near 1.0 while its order dependence is as strong as any drift",
      "find": "        hits = sum(1 for x in null if abs(x - mean) >= abs(observed - mean))",
      "replace": "        hits = sum(1 for x in null if x >= observed)",
      "must_fail": "test_it_FINDS_ANTI_correlation_that_a_ONE_SIDED_test_would_miss",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M430-the-minimum-sample-floor-is-removed",
      "why": "a permutation null over three points is reported as a test. With so few distinct orderings the statistic can take only a handful of values, so the p-value is quantised into near uselessness -- and an impossible measurement is dressed as a passed one",
      "find": "    if len(v) < MIN_OBSERVATIONS:",
      "replace": "    if False:",
      "must_fail": "test_a_TOO_SHORT_sample_is_REFUSED",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M431-the-block-scan-offers-a-combined-p-value",
      "why": "the refusal to combine correlated block sizes is replaced by a Fisher-style product, as though the four were independent experiments. On ibm_marrakesh all four leaned over-dispersed and none cleared its own bar; multiplied together they would read as a finding. This is how a non-result becomes a publication",
      "find": "            \"no_combined_p_value\": (",
      "replace": "            \"combined_p_value\": min(1.0, 4 * min(\n                [r[\"p_value\"] for r in out if \"p_value\" in r] or [1.0])),\n            \"no_combined_p_value\": (",
      "must_fail": "test_the_block_scan_REFUSES_to_combine_correlated_tests",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M432-the-permutation-seed-stops-reaching-the-generator",
      "why": "the seeded generator is replaced by an unseeded one, so the published p-value moves between runs of identical data. Every hash covering this artifact breaks, and the `--from-artifact` verifier -- which recomputes 18 published claims from stored shots -- starts reporting disagreements that are pure RNG",
      "find": "    rng = random.Random(seed)",
      "replace": "    rng = random.Random()",
      "must_fail": "test_the_same_seed_gives_the_SAME_p_value",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M433-block-dispersion-accepts-a-single-block",
      "why": "a block size that yields one block is scored instead of refused. The variance of a single mean is zero by construction, so any sample too short to test reports PERFECT stability -- the most confident possible answer from the least possible evidence",
      "find": "    if k < 2:",
      "replace": "    if k < 1:",
      "must_fail": "test_block_dispersion_needs_at_least_two_blocks",
      "target": "exchangeability",
      "outcome": "killed"
    },
    {
      "id": "M434-dominance-compares-floors-to-floors-instead-of-ceilings",
      "why": "a detector is called dominant when its floor merely beats the others' floors, ignoring that their ceilings tower over it. On ibm_marrakesh this names detector 26 the worst on the device when a pure renaming reorders the leaderboard -- the exact false finding this module was rewritten to refuse",
      "find": "        if all(s.lower > o.upper for o in shares if o.detector != s.detector))",
      "replace": "        if all(s.lower >= o.lower for o in shares if o.detector != s.detector))",
      "must_fail": "test_OVERLAPPING_intervals_identify_NO_worst_detector",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M435-an-envelope-over-a-single-labelling-is-accepted",
      "why": "the envelope stops requiring a second labelling, so it returns one solver's tie-breaking dressed in the language of invariance. One labelling is precisely the reading that was MEASURED to be an artifact",
      "find": "    if len(maps) < 2:",
      "replace": "    if len(maps) < 1:",
      "must_fail": "test_ONE_labelling_is_REFUSED_because_one_labelling_was_the_artifact",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M436-a-multi-detector-cut-raises-every-members-floor",
      "why": "weight proven ONCE across a cut is credited in full to each detector the cut touches, so a 3-detector set adds its multiplier three times. Floors then exceed the certified total they are meant to partition, and the hottest detector is whichever appears in the most crowded cuts",
      "find": "            out[det] = (lo + z if solo else lo, hi + z,",
      "replace": "            out[det] = (lo + z, hi + z,",
      "must_fail": "test_a_MULTI_detector_cut_raises_every_ceiling_and_no_floor",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M437-the-virtual-boundary-is-placed-on-the-chip",
      "why": "the decoding graph's boundary node is attributed like a real detector, which is what produced a `round -1` carrying 3.45 units on the first run. It is part of the proven bound and sits at no qubit; giving it a location puts weight on hardware that does not exist",
      "find": "            if det == boundary:",
      "replace": "            if False:",
      "must_fail": "test_the_BOUNDARY_is_kept_out_of_the_physical_map",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M438-the-detector-range-guard-stops-checking-the-low-end",
      "why": "the collapse guard checks only the upper bound again, so any negative index -- the boundary, or a map built for another circuit -- lands in a bucket instead of being refused. This is the exact half-guard that let `round -1` through",
      "find": "    if lowest < 0 or seen >= expected:",
      "replace": "    if seen >= expected:",
      "must_fail": "test_a_detector_OUTSIDE_the_circuit_is_REFUSED_at_BOTH_ends",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M439-labellings-that-disagree-on-the-total-are-combined",
      "why": "the invariance check is dropped, so maps of DIFFERENT problems are merged into one envelope. Relabelling cannot change the certified total, so a disagreement there means something other than the labels moved -- and every per-detector comparison afterwards is between two different experiments",
      "find": "    if spread > total_tol * max(1.0, abs(totals[0])):",
      "replace": "    if False:",
      "must_fail": "test_labellings_that_DISAGREE_on_the_total_are_REFUSED",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M440-a-detector-absent-from-one-labelling-keeps-its-floor",
      "why": "a detector that some labelling never named keeps the floor another labelling gave it, so the envelope reports as forced exactly the attribution the labellings disagreed about most. Contributing nothing under a labelling IS a floor of zero",
      "find": "            ranges[det] = (0.0, hi)",
      "replace": "            pass",
      "must_fail": "test_a_detector_ABSENT_from_one_labelling_gets_a_floor_of_ZERO",
      "target": "fragility_map",
      "outcome": "killed"
    },
    {
      "id": "M441-observations-that-could-not-refute-are-counted-as-support",
      "why": "NOT_TESTED is tallied as support, restoring the two-outcome count this module exists to replace. 3,424 empty-syndrome shots then vouch for every law about which detectors co-fire, and a detector that breaks its law a third of the time it is examined publishes at 0.995 confidence",
      "find": "        else:\n            untested += 1",
      "replace": "        else:\n            support += 1",
      "must_fail": "test_a_law_NOTHING_could_refute_is_VACUOUS_not_verified",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M442-a-vacuous-law-stops-being-told-from-a-thinly-tested-one",
      "why": "a law NOTHING put at risk stops reporting VACUOUS and reports UNDERPOWERED, collapsing 'the data cannot speak to this' into 'the data spoke and said little'. The first is a fact about a sample that never asked the question and calls for a different experiment; the second only asks for more shots. NOTE: the obvious mutation -- deleting the vacuity clause from `verified` -- is EQUIVALENT, because a vacuous law has zero support and already fails MIN_SUPPORT. That clause was dead code and was removed; `status` is where vacuity decides",
      "find": "        if self.vacuous:\n            return \"VACUOUS\"",
      "replace": "        if False:\n            return \"VACUOUS\"",
      "must_fail": "test_a_law_NOTHING_could_refute_is_VACUOUS_not_verified",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M443-confidence-is-taken-over-every-observation-not-the-risked-ones",
      "why": "confidence divides by all observations rather than the ones that tested the law, so it becomes a function of sample size. MEASURED: detector 26's 0.667 reads 0.995 under this, and the arithmetic is not wrong about any single shot -- the denominator simply never asked the question",
      "find": "        return self.support / self.opportunities if self.opportunities else 0.0",
      "replace": "        n = self.support + self.refutations + self.not_tested\n        return (self.support + self.not_tested) / n if n else 0.0",
      "must_fail": "test_confidence_is_over_RISKED_observations_only",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M444-a-single-observation-is-enough-to-establish-a-law",
      "why": "the minimum support drops to one, so a rule induced from a single event is published as a device law. That is a description of the event, and the operator this was ported from uses 2 for exactly this reason",
      "find": "MIN_SUPPORT = 2",
      "replace": "MIN_SUPPORT = 1",
      "must_fail": "test_silence_CANNOT_carry_a_law_to_verified",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M445-the-last-counterexample-replaces-the-first",
      "why": "each refutation overwrites the stored witness, so a law killed by shot 7 reports shot 900 instead. A kill is a claim, and the SMALLEST witness is the one a reader can check by hand",
      "find": "            if witness is None:",
      "replace": "            if True:",
      "must_fail": "test_the_FIRST_witness_is_kept_because_the_smallest_is_the_useful_one",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M446-every-refutation-is-marked-certified",
      "why": "a counterexample that carries no proof is labelled certified, erasing the one thing this module has that a game agent does not: the difference between an observation that might be noise and a witness an independent checker re-verifies",
      "find": "                certified = bool(w and w.get(\"proof\"))",
      "replace": "                certified = True",
      "must_fail": "test_a_kill_that_carries_a_PROOF_is_marked_as_certified",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M447-an-edge-the-correction-never-used-counts-as-support",
      "why": "a shot whose optimal correction never touched the edge is scored as evidence the edge is unnecessary. It is proof for THAT shot and no test of the law, and with 68 edges over 576 shots such shots dominate -- so every edge verifies on arithmetic about edges that were never on the table",
      "find": "        if edge not in set(used):\n            # Proven avoidable here, but the law was never at risk.\n            return Verdict.NOT_TESTED",
      "replace": "        if edge not in set(used):\n            return Verdict.UPHELD",
      "must_fail": "test_an_edge_the_correction_NEVER_USED_is_not_counted_as_support",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M448-two-different-laws-may-share-a-name",
      "why": "the duplicate-name guard is removed, so a REFUTED law and a VERIFIED one can be cited under one name and a reader cannot tell which was meant",
      "find": "    if len(set(names)) != len(names):",
      "replace": "    if False:",
      "must_fail": "test_two_laws_under_ONE_NAME_are_REFUSED",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M449-relabeling-comparison-accepts-two-disjoint-law-sets",
      "why": "the shared-name guard goes, so comparing two labellings with no law in common reports that every law survived relabelling. Vacuously true, and it certifies exactly the solver-dependence the comparison exists to catch",
      "find": "    if not shared:",
      "replace": "    if False:",
      "must_fail": "test_comparing_labellings_with_NO_shared_laws_is_REFUSED",
      "target": "device_laws",
      "outcome": "killed"
    },
    {
      "id": "M450-a-perturbed-optimum-off-the-face-is-accepted",
      "why": "the on-face check is dropped, so the first perturbed solve is reported whether or not 1'z == W*. The maximality argument holds ONLY on the face; off it the value is a point of some other problem, and it is published as the true extreme",
      "find": "            if total == wstar:",
      "replace": "            if True:",
      "must_fail": "test_never_landing_on_the_face_REFUSES_after_bounded_halvings",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M451-the-returned-point-is-not-re-verified-feasible",
      "why": "feasibility re-verification is skipped, so an infeasible point -- one that violates a packing constraint -- becomes a range end. exact_lp's own docstring says its word is never the acceptance evidence; this mutant makes it exactly that",
      "find": "            if not _verify_feasible(rows, rhs, z):\n                raise EnvelopeRefusal(\"solver returned an infeasible point\")",
      "replace": "            if False:\n                raise EnvelopeRefusal(\"solver returned an infeasible point\")",
      "must_fail": "test_an_INFEASIBLE_point_from_the_solver_is_REFUSED",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M452-the-dual-certificate-of-the-perturbed-optimum-is-not-checked",
      "why": "the weak-duality certificate is skipped, so 'max over the face' rests on the simplex having stopped rather than on u >= 0, A^T u >= c, w'u == c'z. A solver that stalls one pivot early would publish a range that is too narrow -- and narrow is the flattering direction",
      "find": "            if not _verify_optimal(rows, rhs, c, z, u):",
      "replace": "            if False:",
      "must_fail": "test_a_FORGED_dual_certificate_is_REFUSED",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M453-a-pool-exceeding-the-claimed-optimum-is-not-refused",
      "why": "the total > W* guard is removed, so a caller passing the wrong W* gets halvings until 'never landed' instead of the true diagnosis. The face {1'z = W*} does not exist when W* is not the optimum, and a refusal that names the wrong cause sends the reader to the wrong fix",
      "find": "            if total > wstar:",
      "replace": "            if False:",
      "must_fail": "test_a_pool_that_EXCEEDS_the_claimed_optimum_is_REFUSED",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M454-exact-dominance-accepts-a-floor-that-merely-EQUALS-a-ceiling",
      "why": "the dominance test becomes >=, so a detector whose floor equals a rival's ceiling is named worst. Equality means some feasible attribution has them level, which is the opposite of 'no re-attribution can dislodge it'",
      "find": "        if all(floors[d] > ceils[o] for o in detectors if o != d)))",
      "replace": "        if all(floors[d] >= ceils[o] for o in detectors if o != d)))",
      "must_fail": "test_aggregate_SUMS_ranges_EXACTLY_because_faces_are_independent",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M455-the-T-odd-filter-on-candidate-cuts-is-removed",
      "why": "even cuts enter the pool. An even cut is not a dual variable of the T-join LP, so the 'optimum' over the pool stops being a lower bound on anything; measured on the tied pair it lifts W* above the true 2.0. The conformance test against the engine's bound exists for exactly this",
      "find": "            if len(S & T) % 2 == 1:\n                seen.add(S)",
      "replace": "            if True:\n                seen.add(S)",
      "must_fail": "test_a_TIED_pair_gives_the_FULL_interval_at_each_endpoint",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M456-an-odd-syndrome-no-longer-adds-the-boundary-to-T",
      "why": "for an odd number of fired detectors the boundary stops joining T, so the ball {boundary} vanishes from the pool and the fired detector's attribution collapses from [0, 5] to the point [5, 5]. Narrower, confident, and wrong. NOTE the scope: the boundary is ONE source of non-uniqueness, not the source -- MEASURED on 148 even-|T| marrakesh shots with no boundary in T, 0 had a unique dual; nested balls around any centre trade freely",
      "find": "    if len(T) % 2 == 1:\n        T = T | {BOUNDARY}\n    adj = build_adj(edges)",
      "replace": "    adj = build_adj(edges)",
      "must_fail": "test_an_ODD_syndrome_adds_the_boundary_and_the_range_shows_it",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M457-a-group-functional-counts-a-cut-once-instead-of-per-member",
      "why": "phi_G(S) becomes [S & G non-empty] instead of |S & G|, so a cut holding two detectors of one round contributes half. The group range then no longer equals the sum of member attributions at any vertex, and the engine's own value falls OUTSIDE it -- the 'exact per-round range' would be ranging a different quantity from the per-detector table it is compared against",
      "find": "        phi = [len(S & G) for S in pool]",
      "replace": "        phi = [1 if (S & G) else 0 for S in pool]",
      "must_fail": "test_at_ONE_vertex_a_group_equals_the_SUM_of_its_members",
      "target": "dual_envelope",
      "outcome": "killed"
    },
    {
      "id": "M458-the-cross-check-against-the-envelope-is-dropped",
      "why": "the per-detector agreement between the algebraic verdict (phi in the row space of the implied equalities) and the ranged verdict (the envelope's is_point) is no longer enforced, so a wrong implied-equality set publishes 'determined' detectors the ranges contradict",
      "find": "        if bad:",
      "replace": "        if False:",
      "must_fail": "test_the_cross_check_refuses_a_disagreeing_envelope",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M459-complementary-slackness-no-longer-marks-a-tight-row",
      "why": "a row with u_e > 0 is no longer taken as tight on the whole face; it falls through to the exact range test and is still found, so the answer is the same and only the accounting moves -- which is exactly what the killer observes: free_by_slackness and range_tests on the segment",
      "find": "        if uu[i] > 0:                    # complementary slackness: tight on F",
      "replace": "        if False:                        # complementary slackness: tight on F",
      "must_fail": "test_the_segment_equalities_come_from_slackness_not_from_ranges",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M460-the-tight-row-test-reads-the-maximum-instead-of-the-minimum",
      "why": "a row is an implied equality iff its MINIMUM over the face equals the right-hand side; reading the maximum calls every row that is tight at the simplex's vertex an equality, adds it to M, and collapses a segment to a point -- the flattering direction",
      "find": "            if optimal_face_range(rows, rhs, rows[i], wstar).lo == rhs[i]:",
      "replace": "            if optimal_face_range(rows, rhs, rows[i], wstar).hi == rhs[i]:",
      "must_fail": "test_a_cap_tight_at_the_vertex_but_not_on_the_face_is_not_an_equality",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M461-the-zero-variable-test-reads-the-minimum-instead-of-the-maximum",
      "why": "a variable is zero on the face iff its MAXIMUM there is 0; reading the minimum (always 0 at the vertex where it is zero) declares it an equality, and the endpoint segment becomes a point with the later endpoint 'determined' at zero",
      "find": "            if optimal_face_range(rows, rhs, e_j, wstar).hi == 0:",
      "replace": "            if optimal_face_range(rows, rhs, e_j, wstar).lo == 0:",
      "must_fail": "test_a_segment_determines_the_sum_and_not_the_split",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M462-row-space-membership-is-inverted",
      "why": "'determined' and 'undetermined' swap: every floating detector is published as fixed and every fixed one as floating",
      "find": "    return rref(list(rows) + [list(phi)])[0] == rank",
      "replace": "    return rref(list(rows) + [list(phi)])[0] != rank",
      "must_fail": "test_in_rowspace_both_ways",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M463-the-undetermined-location-dimension-is-reported-as-zero",
      "why": "loc_free is fixed at 0, so k_loc reads as the full detector count on every shot: the dual appears to determine everything about location, which is the precise claim R5 refuted",
      "find": "        loc_free = rref(phiN)[0]",
      "replace": "        loc_free = 0",
      "must_fail": "test_a_segment_determines_the_sum_and_not_the_split",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M464-invisible-face-directions-are-not-counted",
      "why": "face directions that move weight between cuts without changing any detector's attribution are reported as 0, so dim F and loc_free are published as equal on every shot",
      "find": "invisible_dirs=dim_face - loc_free,",
      "replace": "invisible_dirs=0,",
      "must_fail": "test_face_directions_no_detector_sees_are_counted_as_invisible",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M465-single-detectors-are-published-as-combination-facts",
      "why": "the support threshold drops to 1, so every individually fixed detector is also listed as a 'combination' and the count of facts no member determines is inflated",
      "find": "            if len(sup) >= 2:",
      "replace": "            if len(sup) >= 1:",
      "must_fail": "test_a_never_charged_detector_is_a_single_not_a_combination",
      "target": "face_annihilator",
      "outcome": "killed"
    },
    {
      "id": "M472-the-burst-price-becomes-a-scale-again",
      "why": "the time-like weight is MULTIPLIED by the cost instead of replaced, so the per-round probability still sets the price -- which is the very assumption a burst violates, and the model quietly becomes the surrogate it was built to replace",
      "find": "    out = [(u, v, float(cost) if edge_family(u, v, positions) == \"time-like\" else w)",
      "replace": "    out = [(u, v, float(cost) * w if edge_family(u, v, positions) == \"time-like\" else w)",
      "must_fail": "test_the_burst_cost_is_a_price_not_a_scale",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M473-burst-edges-are-priced-by-their-span",
      "why": "the cost grows with the span, so a long chain is dear again and the one thing the model asserts -- a burst costs about one event whatever its length -- is gone while the edges remain",
      "find": "    return [(t * positions + pos, t2 * positions + pos, float(cost))",
      "replace": "    return [(t * positions + pos, t2 * positions + pos, float(cost) * (t2 - t))",
      "must_fail": "test_burst_edges_span_more_than_one_round_at_each_position",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M474-the-burst-model-touches-the-data-families",
      "why": "space-like and boundary weights are re-priced too, so the edges that carry the logical observable are flattened along with the harmless route -- the asymmetry the whole mechanism rests on disappears",
      "find": "    out = [(u, v, float(cost) if edge_family(u, v, positions) == \"time-like\" else w)",
      "replace": "    out = [(u, v, float(cost))",
      "must_fail": "test_the_burst_model_REPLACES_time_like_weights_and_leaves_data_alone",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M466-time-like-edges-are-recognised-one-round-off",
      "why": "the time-like test compares the index gap to positions + 1, so no edge is time-like and every measurement/idle edge falls to 'other' and is never scaled",
      "find": "    if abs(u - v) == positions:",
      "replace": "    if abs(u - v) == positions + 1:",
      "must_fail": "test_edge_families_by_index_arithmetic",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M467-an-unnamed-family-is-scaled-by-zero-instead-of-one",
      "why": "a family absent from the scales gets factor 0, so a partial scale set erases whole families of edges instead of leaving them alone",
      "find": "        s = scales.get(edge_family(u, v, positions), 1)",
      "replace": "        s = scales.get(edge_family(u, v, positions), 0)",
      "must_fail": "test_scales_touch_only_their_family_and_keep_exact_weights_exact",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M468-a-scale-set-missing-a-family-is-accepted",
      "why": "the completeness check is dropped, so 'time-like=0.5' parses as a complete set and the other two families are silently unscaled -- a set that reads as complete and is not",
      "find": "    if missing:",
      "replace": "    if False:",
      "must_fail": "test_parse_scales_requires_every_family_and_only_known_ones",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M469-the-synthetic-control-cannot-refute",
      "why": "the control returns UPHELD whenever the optimum is not tiny, so a small scale that WINS on a model that is right by construction -- the outcome that would say the gain is our decoder's, not the device's -- is published as confirmation",
      "find": "    if curve[small] <= at_one:",
      "replace": "    if False:",
      "must_fail": "test_the_control_REFUTES_a_small_scale_that_merely_TIES",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M470-the-degeneracy-kill-accepts-half-its-witness",
      "why": "the kill fires on the flip count alone, so an arm that stops predicting flips for some OTHER reason is recorded as the same degeneracy -- and the claim that the cut operator specifically destroys the decoder loses its second witness",
      "find": "    if flip_predictions == 0 and errors == true_flips:",
      "replace": "    if flip_predictions == 0:",
      "must_fail": "test_the_degeneracy_kill_needs_BOTH_of_its_witnesses",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M471-the-run-statistic-counts-edges-not-chains",
      "why": "the run length resets are dropped, so every time-like edge at a position counts toward one run and an isolated pair reads as a burst -- the signature that distinguishes a multi-round mechanism from ordinary measurement noise goes dark",
      "find": "            cur = cur + 1 if b == a + 1 else 1",
      "replace": "            cur = cur + 1",
      "must_fail": "test_longest_timelike_run_counts_consecutive_rounds_at_one_position",
      "target": "dem_families",
      "outcome": "killed"
    },
    {
      "id": "M475-the-radicand-loses-half-its-covariance",
      "why": "the two-point formula carries 2C where the derivation gives 4C, so every probability comes back systematically wrong while the graph still builds and the decoder still runs -- a calibration-free model that is quietly miscalibrated, which is worse than none",
      "find": "    v = 1 - 4 * c / den",
      "replace": "    v = 1 - 2 * c / den",
      "must_fail": "test_estimator_recovers_a_model_it_was_not_told",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M476-the-covariance-becomes-a-second-moment",
      "why": "C = <x_i x_j> + <x_i><x_j> instead of minus, so the estimator no longer measures CORRELATION at all -- it reports a large p for every pair of detectors that merely fire often, and an anti-correlated pair stops being rejected",
      "find": "    c = x_ij - x_i * x_j",
      "replace": "    c = x_ij + x_i * x_j",
      "must_fail": "test_an_anticorrelated_pair_estimates_below_the_floor_and_is_not_added",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M477-a-sub-floor-code-edge-is-dropped-again",
      "why": "a code edge the data prices below the floor is deleted rather than priced dear. That removes a logical cut and the observable node with it, and the decoder refuses the shots array outright -- the exact failure this module was written around",
      "find": "            if (i, j) not in code_pairs:",
      "replace": "            if True:",
      "must_fail": "test_a_code_edge_the_data_prices_at_nothing_is_floored_not_dropped",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M478-a-negative-estimate-reaches-the-logarithm",
      "why": "only None is rejected, so the small NEGATIVE p that sampling noise produces on an uncorrelated pair reaches log((1-p)/p), which has no value there. The floor branch is what makes the estimator safe on real data and this removes it",
      "find": "        if p is None or p <= pfloor:\n            # A code edge is PRICED at the floor",
      "replace": "        if p is None:\n            # A code edge is PRICED at the floor",
      "must_fail": "test_a_code_edge_the_data_prices_at_nothing_is_floored_not_dropped",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M479-the-boundary-edge-becomes-the-raw-marginal",
      "why": "each boundary edge is priced at the detector's own firing rate instead of the residue the marginal constraint leaves, so every pair mechanism is counted twice -- once as its edge and again as a boundary edge at both ends",
      "find": "            v = (1 - (1 - 2 * float(means[i])) / prod[i]) / 2\n            if v > pfloor:",
      "replace": "            v = float(means[i])\n            if v > pfloor:",
      "must_fail": "test_the_marginal_constraint_recovers_the_planted_boundary",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M480-the-multi-round-candidates-stop-one-span-short",
      "why": "the span range excludes max_span, so the longest-range correlation the model is willing to represent is silently one round shorter than the constant declares -- the artifact says span 6 and the graph carries 5",
      "find": "            for s in range(2, max_span + 1):",
      "replace": "            for s in range(2, max_span):",
      "must_fail": "test_candidate_pairs_span_exactly_two_through_max_span",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M483-the-difference-code-stops-cancelling-shared-noise",
      "why": "Gamma_- adds the covariance instead of subtracting it, so the code that PROTECTS against shared noise is scored as though shared noise hurt it. The selector then prefers the individually quiet independent pair over the correlated one -- precisely the mistake the physics exists to prevent",
      "find": "        return 2.0 * (cii + cjj - 2.0 * cij)",
      "replace": "        return 2.0 * (cii + cjj + 2.0 * cij)",
      "must_fail": "test_logical_rates_are_the_published_algebra",
      "target": "noise_correlation",
      "outcome": "killed"
    },
    {
      "id": "M484-the-crossover-claims-encoding-pays-when-it-never-does",
      "why": "the no-solution branch returns the low end of the sweep instead of None, so a regime where four operations spend the entire error budget before storage even begins reports a threshold of -0.999 and every correlation on earth looks good enough",
      "find": "    if gain(hi) <= 0:\n        return None",
      "replace": "    if gain(hi) <= 0:\n        return lo",
      "must_fail": "test_the_crossover_is_where_encoding_STARTS_to_pay",
      "target": "noise_correlation",
      "outcome": "killed"
    },
    {
      "id": "M485-the-selector-can-no-longer-decline-to-encode",
      "why": "the single-site option stops being the starting incumbent, so the selector must return a PAIR however bad it is -- and on the measured hardware correlations, which sit ~40x below the crossover, that is always the wrong answer",
      "find": "    best = best_single",
      "replace": "    best = (0.0, (\"difference\", 0, 1))",
      "must_fail": "test_the_selector_DECLINES_when_the_correlation_is_not_there",
      "target": "noise_correlation",
      "outcome": "killed"
    },
    {
      "id": "M486-the-correlation-becomes-a-coincidence-rate",
      "why": "the product of means is not subtracted, so two sites that merely fire OFTEN read as strongly correlated. Every pair on a noisy device would then clear the crossover and the selector would encode on noise that is not shared at all",
      "find": "        cov += (xt.T @ xt) / shots - np.outer(m, m)",
      "replace": "        cov += (xt.T @ xt) / shots",
      "must_fail": "test_a_COVARIANCE_is_not_a_COINCIDENCE_RATE",
      "target": "noise_correlation",
      "outcome": "killed"
    },
    {
      "id": "M487-the-extra-gate-error-is-charged-subtractively-again",
      "why": "the encode/decode cost comes off the error budget instead of multiplying the fidelity. That is the convention an independent implementation does NOT use; it shifts every horizon by 4e-3, and adopting it silently breaks agreement with the published curve this module was validated against",
      "find": "    fac = (1.0 - err_per_gate) ** extra_gates",
      "replace": "    fac = 1.0 - err_per_gate * extra_gates",
      "must_fail": "test_the_extra_gate_error_is_charged_MULTIPLICATIVELY",
      "target": "noise_correlation",
      "outcome": "killed"
    },
    {
      "id": "M481-the-graph-likeness-check-goes-silent",
      "why": "an unsatisfiable marginal stops being counted, so the check reports 0 on every run including the four where it should report 108 of 112 -- and the result reads as 'we measured the device's error model', which the same data refutes",
      "find": "            if v <= pfloor:\n                unsatisfiable += 1",
      "replace": "            if False:\n                unsatisfiable += 1",
      "must_fail": "test_graph_likeness_DETECTS_a_three_detector_mechanism",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M482-damping-scales-the-code-edges-too",
      "why": "the damping factor is applied before the code/added split, so the consistency arm also cheapens the DEM's own edges -- and the trade-off curve between generative accuracy and decoding stops being about the multi-round edges at all",
      "find": "            p = p * damping\n            if p <= pfloor:\n                continue\n            added += 1",
      "replace": "            added += 1",
      "must_fail": "test_damping_removes_the_added_edges_and_nothing_else",
      "target": "dem_measured",
      "outcome": "killed"
    },
    {
      "id": "M488-the-leak-rate-is-normalised-by-the-leaked-mass",
      "why": "lambda's M-step divides by the LEAKED occupancy instead of the healthy one, so a rare leak reads as near-certain; a fit that cannot recover a planted 0.03 has no business pricing a device",
      "find": "        lam = xi[:, :, 0, 1].sum() / max(g0[:, :-1].sum(), EPS)",
      "replace": "        lam = xi[:, :, 0, 1].sum() / max(g1[:, :-1].sum(), EPS)",
      "must_fail": "test_the_fitter_recovers_planted_parameters",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M489-the-leaked-emission-is-normalised-by-the-healthy-mass",
      "why": "ell's M-step divides by the HEALTHY occupancy, so the leaked state's firing rate collapses toward zero and the state stops meaning anything",
      "find": "        ell = _clip((g1 * xf).sum() / max(g1.sum(), EPS))",
      "replace": "        ell = _clip((g1 * xf).sum() / max(g0.sum(), EPS))",
      "must_fail": "test_the_fitter_recovers_planted_parameters",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M490-the-implied-round-profile-forgets-that-qubits-leak-in",
      "why": "the forward marginal only lets leakage decay, never accumulate, so the model's own round profile is flat while the data (and PAEMS) rise with the round -- H3 could never be read",
      "find": "        z = z * (1.0 - p.mu) + (1.0 - z) * p.lam",
      "replace": "        z = z * (1.0 - p.mu)",
      "must_fail": "test_the_implied_round_profile_matches_the_data",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M491-the-mixture-pricing-keeps-the-whole-healthy-probability",
      "why": "p' = p + g p_L instead of (1-g) p + g p_L: a fully leaked node prices its edges ABOVE the leaked probability, and the prereg's rule is not the rule that ran",
      "find": "        pp = _clip((1.0 - g) * p + g * pl)",
      "replace": "        pp = _clip(p + g * pl)",
      "must_fail": "test_reweighting_is_the_prereg_mixture",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M492-an-edge-takes-the-LESS-leaked-endpoint",
      "why": "g is the minimum endpoint posterior, so an edge into a leaked node from a healthy one is never repriced -- exactly the edges a leaked ancilla fires",
      "find": "        g = max(gamma_shot[n] for n in (u, v) if n >= 0)",
      "replace": "        g = min(gamma_shot[n] for n in (u, v) if n >= 0)",
      "must_fail": "test_reweighting_is_the_prereg_mixture",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M493-fifty-leaked-cells-are-enough-to-estimate-an-edge",
      "why": "the cell floor becomes 'any', so p_L is read off a handful of shots and the fallback the prereg wrote down never fires",
      "find": "        if n_cells >= min_cells and len(ends) == 2:",
      "replace": "        if n_cells > 0 and len(ends) == 2:",
      "must_fail": "test_a_few_leaked_cells_below_the_floor_still_fall_back",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M494-time-like-means-two-rounds-apart",
      "why": "the time-like test asks for a two-round gap, so the consecutive-round edge -- the measurement-error edge the leaked state should price at 0.5 -- is treated as space-like and keeps its healthy probability",
      "find": "        and abs(u - v) == positions",
      "replace": "        and abs(u - v) == 2 * positions",
      "must_fail": "test_timelike_is_same_position_consecutive_round",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M495-the-marginal-band-is-ten-times-wider",
      "why": "H2's 3-SE band becomes a 30-SE band, and a model whose implied rate is six times the data passes as generative",
      "find": "        pred = implied_rate(p, n_rounds)\n        for t in range(n_rounds):\n            n = t * positions + i\n            obs = float(det_test[:, n].mean())\n            se = math.sqrt(max(obs * (1.0 - obs), EPS) / S)\n            ok = abs(pred[t] - obs) <= k_se * se",
      "replace": "        pred = implied_rate(p, n_rounds)\n        for t in range(n_rounds):\n            n = t * positions + i\n            obs = float(det_test[:, n].mean())\n            se = math.sqrt(max(obs * (1.0 - obs), EPS) / S)\n            ok = abs(pred[t] - obs) <= 10.0 * k_se * se",
      "must_fail": "test_marginal_check_FAILS_a_model_that_is_wrong",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M496-em-stops-after-one-iteration",
      "why": "the convergence test is always true, so the 'fit' is the first M-step from the initialiser -- the parameters returned are the guess, dressed as an estimate",
      "find": "                      ell=ell, loglik=total, iterations=it)\n        if prev > -math.inf and abs(total - prev) <= tol * max(1.0, abs(prev)):",
      "replace": "                      ell=ell, loglik=total, iterations=it)\n        if it >= 1:",
      "must_fail": "test_the_fitter_recovers_planted_parameters",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M497-the-posterior-is-the-healthy-probability",
      "why": "posterior() returns P(healthy), so every downstream 'leaked' reading is inverted: the reweighting prices HEALTHY nodes as leaked",
      "find": "    g1, _xi, _ll = forward_backward(np.asarray(x, dtype=np.uint8), p)\n    return g1",
      "replace": "    g1, _xi, _ll = forward_backward(np.asarray(x, dtype=np.uint8), p)\n    return 1.0 - g1",
      "must_fail": "test_the_posterior_separates_leaked_cells_from_healthy_ones",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M498-the-sampler-swaps-leak-and-return",
      "why": "the generative check's transition draw uses mu to leak and lambda to return, so the answer key the fitter is validated against is a different process from the one it is told it is",
      "find": "        z = np.where(z == 1, (flip >= p.mu).astype(np.uint8),\n                     (flip < p.lam).astype(np.uint8))",
      "replace": "        z = np.where(z == 1, (flip >= p.lam).astype(np.uint8),\n                     (flip < p.mu).astype(np.uint8))",
      "must_fail": "test_the_fitter_recovers_planted_parameters",
      "target": "leakage_hmm",
      "outcome": "killed"
    },
    {
      "id": "M499-a-live-owner-no-longer-refuses-the-second-run",
      "why": "the liveness test is dropped, so a second mutation run starts beside a live one and deletes the tree it is mutating -- the 2026-08-25 fifty-minute loss, reinstated",
      "find": "        if owner > 0 and _pid_alive(owner):",
      "replace": "        if False:",
      "must_fail": "test_a_LIVE_owner_makes_the_second_run_REFUSE",
      "target": "mutation_sandbox",
      "outcome": "killed"
    },
    {
      "id": "M500-release-drops-a-lock-we-do-not-own",
      "why": "the ownership check on release is short-circuited, so a finishing run deletes a lock a later run wrote and hands the sandbox to a third",
      "find": "                lock.read_text(encoding=\"utf-8\").strip() == str(os.getpid()):",
      "replace": "                True:",
      "must_fail": "test_release_drops_only_OUR_lock",
      "target": "mutation_sandbox",
      "outcome": "killed"
    },
    {
      "id": "M501-our-own-lock-is-refused",
      "why": "the same process re-entering refuses its own lock as if it were a sibling's, so a legitimate re-claim inside one run wedges the gate",
      "find": "        if owner == os.getpid():\n            return",
      "replace": "        pass",
      "must_fail": "test_RECLAIMING_our_OWN_lock_is_not_a_refusal",
      "target": "mutation_sandbox",
      "outcome": "killed"
    },
    {
      "id": "M502-the-optimum-above-two-is-displaced",
      "why": "the closed form reads ln(V) / (2 mu), so every band recommended above the crossover is too wide by ln(V) - ln(V - 1) over 2 mu",
      "find": "    return math.log(erasure_value - 1.0) / (2.0 * mu)",
      "replace": "    return math.log(erasure_value) / (2.0 * mu)",
      "must_fail": "test_the_closed_form_is_a_STATIONARY_point_and_a_MINIMUM",
      "target": "arr",
      "outcome": "killed"
    },
    {
      "id": "M503-the-analytic-candidate-does-not-compete",
      "why": "optimal_band is reported but excluded from the candidates, so the returned optimum is the grid's and band 0 reappears on (2, 2.1)",
      "find": "    if not explicit and analytic is not None:",
      "replace": "    if False:",
      "must_fail": "test_a_MUTANT_that_drops_the_analytic_candidate_is_caught",
      "target": "arr",
      "outcome": "killed"
    },
    {
      "id": "M504-a-T-never-blocks",
      "why": "the X-component check is dropped, so a pending X passes a T and every boundary reads deferrable: the blocked fraction R34 reports as the mechanism's binding cost becomes zero",
      "find": "            for q in g.qubits:\n                if x[q]:\n                    raise Blocked(index, q)\n            return                              # diagonal: Z components pass",
      "replace": "            return                              # diagonal: Z components pass",
      "must_fail": "test_a_T_BLOCKS_an_X_component_and_PASSES_a_Z_component",
      "target": "pauli_defer",
      "outcome": "killed"
    },
    {
      "id": "M505-CX-control-and-target-swapped",
      "why": "x[t] ^= x[c] becomes x[c] ^= x[t], so an X on the control no longer spreads to the target and the support curve under-counts",
      "find": "    elif g.name == \"CX\":\n        c, t = g.qubits\n        x[t] ^= x[c]\n        z[c] ^= z[t]",
      "replace": "    elif g.name == \"CX\":\n        c, t = g.qubits\n        x[c] ^= x[t]\n        z[c] ^= z[t]",
      "must_fail": "test_a_WRONG_rule_would_be_caught_by_the_oracle",
      "target": "pauli_defer",
      "outcome": "killed"
    }
  ]
}